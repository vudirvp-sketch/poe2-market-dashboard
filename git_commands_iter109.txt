# Git commands for iter 109 delivery
# Run from your local poe2-market-dashboard repo root.

# 1. Stage all new and modified files
git add src/components/dashboard/mirror-divine-arb-tab.tsx
git add src/components/dashboard/dashboard-page.tsx
git add src/components/dashboard/dashboard-toolbar.tsx
git add src/components/dashboard/shortcuts-dialog.tsx
git add src/lib/i18n/locales/en.ts
git add src/lib/i18n/locales/ru.ts
git add src/lib/i18n/locales/ko.ts
git add src/lib/i18n/locales/zh.ts
git add STATUS.md
git add docs/MARKET_PLAYBOOK.md
git add AGENT_NAVIGATION.md
git add worklog.md
git add MERGE_INSTRUCTIONS_iter109.md
git add git_commands_iter109.txt

# 2. Verify what's staged
git status

# 3. Commit
git commit -m "iter 109: P7 Mirror/Divine Arb Detector UI tab (single-object card render)

- New: src/components/dashboard/mirror-divine-arb-tab.tsx — single-object
  card render (NOT a per-currency list — Mirror:Divine is ONE market).
  Layout: header (icon + title + subtitle + days selector 7/14/30/90 +
  refresh) → signal+action badges (2-col grid: SELL_MIRROR_BUY_DIVINE /
  SELL_DIVINE_BUY_MIRROR / NEUTRAL + EXECUTE_ARB / WATCH / HOLD) → hero
  metrics (3-col grid: current rate / z-score / deviation %) → profit +
  actionable row → 5-cell stats grid (mean/std/min/max/sample size) →
  rate sparkline (360x48 SVG, colored by signal) → footer. 5 graceful-
  degradation branches: offline / loading / error / no-data /
  insufficient-sample. Uses useQuery (30s staleTime, retry 1) bound to
  /api/flipper/mirror-divine-arb?days={days}.
- Modified: dashboard-page.tsx — added MirrorDivineArbTab lazy import +
  'mirror-divine-arb' to TAB_MAP at idx 12 (after weekly-patterns, before
  liquid-chain) + TabsContent block with ErrorBoundary. Tab count grew
  from 14 to 15. Click-only (no keyboard shortcut — outside 1-9+0 range).
- Modified: dashboard-toolbar.tsx — added ArrowUpDown icon import + new
  TabsTrigger between weekly-patterns and liquid-chain.
- Modified: shortcuts-dialog.tsx — updated iter-99 comment with iter 109
  entry (mirror-divine-arb at idx 12, click-only).
- Modified: src/lib/i18n/locales/{en,ru,ko,zh}.ts — 42 new keys × 4
  locales = 168 new strings. All 4 locales have parity (1185 keys total
  each). Keys: tabMirrorDivineArb, fallbackMirrorDivineArb, mirrorDivine*
  (
  SparklineLabel).
- Docs: STATUS.md (header iter 109 + Quick-Reference row), docs/
  MARKET_PLAYBOOK.md (§C.6 end-to-end DONE, §B P7 row, §D.2 row 5, §D.3
  iter 109 stop-point, §E), AGENT_NAVIGATION.md (header + new module
  row for mirror-divine-arb-tab.tsx), worklog.md (iter 109 entry,
  trimmed iter 107).

Verification: pytest 1218 green (no regression — backend untouched in
iter 109). 42 i18n keys × 4 locales parity verified programmatically
(check_iter109.py PASS). Bracket balance verified in all 4 modified
TS/TSX files. TAB_MAP / TabsContent / TabsTrigger / shortcuts-dialog
wiring all verified. tsc/jest not run (OOM-killer at npm install —
Known Issue since iter 99, requires 8GB+ RAM).

Stopping point: P7 end-to-end DONE. Next iter (iter 110) candidates:
TD-3/4/5/9 persistence gaps; P9 Phase-aware investment advisor (§C.7);
P10 Gold Map ROI (§C.8); tsc/jest regression (needs 8GB+ RAM)."

# 4. Push to remote
git push origin main

# 5. Verify the push
git log --oneline -1
git status
