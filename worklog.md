# Worklog — PoE2 Market Dashboard

> Append-only shared multi-agent work log. Each section starts with `---`.
> Old task history is in `git log`. Trimmed to last 2 iterations.

---

Task ID: iter-127
Agent: main
Task: iter 127 — TD-9 Phase 1 + P10 Phase 1 MVP. Per iter 126's stopping-point brief: ship two user-visible improvements in one iter (both low-to-medium risk, independent of each other). TD-9 Phase 1 = wire `price_history_short` from `snapshot.price_histories[-14:]` into `/flips` response (cheapest fix, no persistence needed, immediate UX win for FlipsTable sparkline). P10 Phase 1 MVP = new `gold-map-roi-tab.tsx` + `gold-map-roi-calculator.tsx` + localStorage + i18n × 4 locales + add to `TAB_MAP` at index 13 (click-only) — reuses existing `/api/flipper/triangular` (no backend route). Both ship in one iter per recommendation.

Work Log:
- Cloned repo. Read `STATUS.md` (iter 126 SHIPPED — DOC-2 design-docs, 111 warnings, 622 jest green), `worklog.md` (iter 125 + iter 126), `docs/design/TD-3-4-5-9-persistence-gaps-design.md` (full — Phase 1 §5.3 spec, §9 phasing, §10 open questions), `docs/design/P10-gold-map-roi-design.md` (full — §4.3 ROI formula, §5 placement, §6 inputs, §7 layout, §8 API surface, §9 i18n, §10 tests, §11 risks, §12 phasing).
- Read source files for context: `backend/api/routes_arbitrage.py` (1206 lines — `_build_flip_opportunities_sync` builds `FlipOpportunity` dataclass per pair; the async wrapper pre-extracts `price_histories_raw` into the `FlipComputeBundle` so it's already available to the sync function — TD-9 fix is just slicing the last 14 entries and attaching to the opp), `backend/api/response_models.py:FlipOpportunityData` (pydantic model — confirmed `SpeculationPriceHistoryPoint` is already defined in this file and used by other models as a forward reference), `backend/models/currency.py:FlipOpportunity` (dataclass — added `price_history_short: list[dict] = field(default_factory=list)` field with default), `backend/economy/speculation.py:214` (canonical slice pattern: `history[-MAX_HISTORY_POINTS:]` then `{"date": ts.isoformat(), "price": price}` — TD-9 mirrors this), `src/lib/types.ts:FlipOpportunity` (TS interface — added `priceHistoryShort?: SpeculationPriceHistoryPoint[]` field), `src/components/dashboard/flips-helpers.ts:deriveTrendSparklineData` (iter 94 synthetic shape — TD-9 keeps this as fallback for zero-history edge case), `src/components/dashboard/flips-table.tsx:448` (single Sparkline usage — swapped to new `getTrendSparklineData` helper), `src/components/dashboard/mirror-divine-arb-tab.tsx` (template for the new P10 tab — single-card layout + offline/error/loading states + refresh button), `src/components/dashboard/dashboard-page.tsx:TAB_MAP` (14 entries — confirmed click-only slots 11–14, added `gold-map-roi` at index 13), `src/components/dashboard/dashboard-toolbar.tsx` (TabsTrigger pattern — added new trigger with `MapPin` icon).
- **TD-9 Phase 1 implementation (backend):**
  - Added `_slice_price_history_short(history, *, max_points=14)` pure function in `routes_arbitrage.py` (returns `list[dict]` of `{"date": iso, "price": float}` oldest-first; empty list when input is empty; coerces price to float for JSON safety).
  - Added `price_history_short: list[dict] = field(default_factory=list)` field to `FlipOpportunity` dataclass in `backend/models/currency.py` (with comment linking to TD-9 + design doc).
  - Added `price_history_short: list[SpeculationPriceHistoryPoint]` field to `FlipOpportunityData` pydantic model in `backend/api/response_models.py` (with `default_factory=list` for backward compat — existing clients ignore the new field).
  - Wired the slice in `_build_flip_opportunities_sync`: `price_history_short=_slice_price_history_short(currency_price_history_timestamped.get(rate.currency_from, []))` — uses the `currency_from` side of the pair (matches `speculation.py` convention which keys on the single currency).
  - Added `"price_history_short": o.price_history_short` to the route response dict in `get_flip_opportunities` (line 770).
- **TD-9 Phase 1 implementation (frontend):**
  - Added `priceHistoryShort?: SpeculationPriceHistoryPoint[]` field to `FlipOpportunity` interface in `src/lib/types.ts`.
  - Added `TrendSparklineInput` interface + `getTrendSparklineData(input)` + `isTrendSparklineRealData(input)` helpers + `FLIPS_TREND_REAL_HISTORY_MIN_POINTS = 2` constant in `flips-helpers.ts`. `getTrendSparklineData` returns `input.priceHistoryShort.map(p => p.price)` when ≥2 points exist; otherwise falls back to `deriveTrendSparklineData(input.momentum, input.volatility)`. The fallback handles zero-history (early league / fresh listing) + the optional-field-omitted case.
  - Updated `flips-table.tsx` Sparkline cell: now uses `getTrendSparklineData({priceHistoryShort, momentum, volatility})` instead of `deriveTrendSparklineData(momentum, volatility)` directly. Tooltip switches between `flipsTrendTooltipReal` (real data) and `flipsTrendTooltip` (fallback) based on `isTrendSparklineRealData(opp)`. Removed the now-unused `deriveTrendSparklineData` import from `flips-table.tsx` (still exported from `flips-helpers.ts` for the fallback path + existing tests).
  - Added `flipsTrendTooltipReal` i18n key × 4 locales (en/ru/ko/zh) — describes the new real-data mode.
- **TD-9 Phase 1 tests:**
  - Backend: 8 new pytest tests in `tests/test_flips_integration.py` — `TestFlipsPriceHistoryShort` (3 integration tests: field always present, non-empty pass-through, always-a-list type check) + `TestSlicePriceHistoryShortHelper` (5 unit tests for the pure helper: empty/short/long/exactly-14/Decimal-coercion).
  - Frontend: 12 new jest tests in `src/__tests__/flips-helpers.test.ts` — `getTrendSparklineData` (7 cases: real data when ≥2 points, fallback when empty/undefined/null/1-point, real data at exactly-2-points threshold, constant value check) + `isTrendSparklineRealData` (5 cases: true at ≥2, false at empty/undefined/null/1).
- **P10 Phase 1 MVP implementation:**
  - Created `src/components/dashboard/gold-map-roi-calculator.tsx` (~620 lines). Exports: `GoldMapRoiCalculator` (React component), `computeRoi`, `pickBestCycleEndingInDiv`, `goldRateAgeDays`, `loadInputsFromLocalStorage`, `saveInputsToLocalStorage`, `defaultInputs`, `recommendationBadgeClass`, `recommendationLabelKey`, `recommendationIcon`, `RoiComputation`, `GoldMapRoiInputs`, `Recommendation` types, plus 8 exported constants (defaults + bounds + thresholds + staleness). ROI formula: `gold_in_div = gold/gold_per_div`; `best_cycle = pickBestCycleEndingInDiv(opps)` (cycles where `cycle[0] === "divine"`, sorted by `netProfitPct` desc); `multiplier = best_cycle ? 1 + netProfitPct/100 : 1`; `final_div = gold_in_div * multiplier`; `expected_div = final_div - map_cost`; `roi_pct = map_cost > 0 ? (expected_div/map_cost)*100 : Infinity`. Recommendation thresholds: AVOID (expected_div ≤ 0), MARGINAL (roi_pct < 50%), FARM (< 150%), STRONG_FARM (≥ 150%). Special case: map_cost=0 + expected_div > 0 → STRONG_FARM (infinite ROI). Inputs: 3 numeric fields (gold amount, map cost, gold per Div) with bounds validation + 7-day staleness warning on the gold rate (localStorage stores a timestamp alongside the value). Persistence: `poe2-gold-map-roi-inputs` localStorage key; loaded on mount, saved on every change; corrupt/missing JSON returns null (falls back to defaults). Pure functions exported for unit testing.
  - Created `src/components/dashboard/gold-map-roi-tab.tsx` (~165 lines). Wraps `useQuery` for `/api/flipper/triangular` (30s staleTime, retry:1, enabled: backendOnline). Renders: offline card when `!backendOnline`, error card with refresh button on `isError`, header card with title + subtitle + refresh button when online, "no cycles yet" amber notice when `dataAvailable === false` (calculator still renders), `<GoldMapRoiCalculator>` passing opportunities + isLoading + isError + backendOnline, fetched-at + cycle-count footer.
  - Updated `src/components/dashboard/dashboard-page.tsx`: added `GoldMapRoiTab` dynamic import (lazy-loaded with `TabSkeleton` fallback — same pattern as the other 14 tabs). Added `"gold-map-roi"` to `TAB_MAP` at index 13 (between `mirror-divine-arb` and `liquid-chain`). Updated the count comment from "15 in iter 109" → "16 in iter 127". Added `<TabsContent value="gold-map-roi">` branch wrapped in `<ErrorBoundary fallbackTitle={t("fallbackGoldMapRoi")}>`.
  - Updated `src/components/dashboard/dashboard-toolbar.tsx`: added `MapPin` icon import + `<TabsTrigger value="gold-map-roi">` between `mirror-divine-arb` and `liquid-chain` (click-only — no shortcut reallocation).
- **P10 Phase 1 i18n:** Added 33 new keys × 4 locales (en/ru/ko/zh) — `tabGoldMapRoi`, `fallbackGoldMapRoi`, `goldMapTitle`, `goldMapSubtitle`, `goldMapOffline`, `goldMapOfflineHint`, `goldMapLoading`, `goldMapError`, `goldMapRefresh`, `goldMapNoCyclesYet`, `goldMapFetchedAt`, `goldMapCycleCount`, `goldMapInputsTitle`, `goldMapGoldPerRun`, `goldMapMapCost`, `goldMapGoldPerDiv`, `goldMapRateStaleWarning`, `goldMapDefaultRateHint`, `goldMapInvalidGoldPerDiv`, `goldMapResultTitle`, `goldMapResultExpectedDiv`, `goldMapResultRoiPct`, `goldMapRecommendationLabel`, `goldMapRecommendationAvoid`, `goldMapRecommendationMarginal`, `goldMapRecommendationFarm`, `goldMapRecommendationStrongFarm`, `goldMapBreakdownTitle`, `goldMapBreakdownManualConversion`, `goldMapBreakdownBestCycle`, `goldMapBreakdownMapCost`, `goldMapBreakdownExpectedNet`, `goldMapMinStartWarning`, `goldMapNoCycleAvailable`, `goldMapNoCycleFallback`. (Slightly over the ~25 estimate — covers every UI string the component renders.) Russian-first copy per `PRODUCT_VISION.md` §3.1.
- **P10 Phase 1 tests:** 51 new jest tests across 2 new files:
  - `src/__tests__/gold-map-roi-calculator.test.ts` (40 tests): `defaultInputs` (2), `goldRateAgeDays` (5 + staleness constant), `pickBestCycleEndingInDiv` (6 — including undefined/empty/no-div-cycle/single-div-cycle/multiple-div-cycles/undefined-profit), `computeRoi` basic formula (5), `computeRoi` edge cases (8 — map_cost=0/expected_div≤0/gold_amount=0/gold_per_div=0/belowMinStart true/false/undefined/no-cycle), `computeRoi` recommendation thresholds (4 — 49%/50%/149%/150% boundary cases), `recommendationLabelKey` (1 — all 4 keys), `recommendationBadgeClass` (4 — all 4 colors), localStorage persistence (7 — round-trip/corrupt-missing-wrong-types/quota-exceeded/no-entry).
  - `src/__tests__/gold-map-roi-tab.test.tsx` (11 tests): renders offline card when backend offline, renders title + subtitle when online, renders calculator once data arrives, renders error card on fetch failure, renders "no cycles yet" notice when dataAvailable=false (calculator still renders), renders fetched-at footer when data available, calls /api/flipper/triangular when online, does NOT call when offline, renders refresh button when online.
- **Verification:**
  - `npx tsc --noEmit` → exit 0 (green).
  - `npx eslint .` → 111 warnings, 0 errors (exactly matches iter 125/126 baseline — no new warnings).
  - `npx jest --no-coverage` → 30 suites, 685 passed (was 622 baseline + 12 TD-9 + 51 P10 = 685). 0 failed.
  - `python3 -m pytest tests/ --ignore=tests/test_scheduler.py --ignore=tests/e2e -q` → 1274 passed, 0 failed. (Skipped test_scheduler per existing convention — needs `aiosqlite` which isn't installed in this env. Skipped e2e — needs Playwright runtime.)
- **Documentation updates:**
  - `STATUS.md`: header bump to iter 127 with concise summary. Updated "Design-docs" section table to show Phase 1 SHIPPED for both docs. Added TD-9/P10-Phase-1 closed-KI entry at the top of "Known Issues — closed (recent)". Updated TD-9 + P10 rows in technical-debt backlog to reflect Phase 1 SHIPPED + remaining phases deferred to iter 128+. Updated "FlipsTable Trend sparkline looks synthetic" row in Quick Reference from "by design (TD-9)" → "TD-9 fixed iter 127".
  - `worklog.md` (this entry) — trimmed iter 124 (oldest, now in `git log`), now shows iter 126 + iter 127 (last 2 iterations per header convention).

Stage Summary:
- **iter 127 SHIPPED — TD-9 Phase 1 + P10 Phase 1 MVP complete.** Two user-visible improvements in one iter as recommended. TD-9: FlipsTable "Trend" sparkline now renders REAL `price_history_short` (up to 14 most-recent price points) instead of the synthetic `momentum × volatility` shape. Synthetic shape kept as fallback for zero-history edge case (early league / fresh listing). P10: new "Gold ROI" tab (click-only, index 13) computes Castaway-run ROI = `(gold/gold_per_div) × (1 + best_3way_cycle.netProfitPct/100) − map_cost` with 4-tier recommendation flag (AVOID/MARGINAL/FARM/STRONG_FARM at 0/50/150%). 3 inputs persisted to localStorage with 7-day staleness warning. Reuses existing `/api/flipper/triangular` — NO new backend route.
- **Modified files (10):** `backend/api/response_models.py`, `backend/api/routes_arbitrage.py`, `backend/models/currency.py`, `tests/test_flips_integration.py`, `src/lib/types.ts`, `src/lib/i18n/locales/en.ts`, `src/lib/i18n/locales/ru.ts`, `src/lib/i18n/locales/ko.ts`, `src/lib/i18n/locales/zh.ts`, `src/components/dashboard/flips-helpers.ts`, `src/components/dashboard/flips-table.tsx`, `src/components/dashboard/dashboard-page.tsx`, `src/components/dashboard/dashboard-toolbar.tsx`, `STATUS.md`, `worklog.md`.
- **New files (4):** `src/components/dashboard/gold-map-roi-tab.tsx`, `src/components/dashboard/gold-map-roi-calculator.tsx`, `src/__tests__/gold-map-roi-calculator.test.ts`, `src/__tests__/gold-map-roi-tab.test.tsx`.
- **Test counts:** pytest 1274 green (was 1266 iter 125 → +8 new TD-9 tests). Jest 685 green (was 622 iter 126 → +12 TD-9 + +51 P10 = +63). tsc green. Lint 111 warnings (unchanged).
- **What was NOT done (intentionally deferred to iter 128+):**
  - **TD-4 Phase 2** (`market_spreads` table + write/read + route) — ~150 lines, medium risk, depends on Phase 1 patterns being established (now they are).
  - **TD-3 Phase 3** (`triangular_cycles` table + write/read + route) — ~180 lines, medium risk, depends on Phase 2.
  - **TD-5 Phase 4** (`daily_stats` table + backfill script + scheduler + route) — ~250 lines + script, medium-high risk, one-shot backfill op.
  - **P10 Phase 2** (trend chart) — depends on TD-3 Phase 3.
  - **TD-9 fallback removal** — `deriveTrendSparklineData` should be removed 2 iters after TD-9 ships (iter 129+) once production logs confirm no fallback path is hit (design doc §10 Q5).
  - **PoE2_Flipper_Canonical_Formulas.md trim** — conservative scope, contains active formula references, risky to touch.
- **Stopping point:** iter 127 = TD-9 Phase 1 + P10 Phase 1 MVP SHIPPED, all baselines preserved or improved. Next iter (iter 128) candidates in priority order: (a) **TD-4 Phase 2** (lowest risk of the remaining persistence phases — `market_spreads` table, new write path, new read-only route, ~150 lines, no migration); (b) **TD-3 Phase 3** (depends on Phase 2 patterns — `triangular_cycles` table, ~180 lines); (c) **TD-5 Phase 4** (highest risk — `daily_stats` table + one-shot backfill script + scheduler task + new endpoint, ~250 lines + script, medium-high risk). Recommended: ship (a) alone in iter 128 (establishes the persistence pattern), then (b) + (c) in iter 129+ once the pattern is proven.

---

Task ID: iter-126
Agent: main
Task: iter 126 — design-docs only for TD-3/4/5/9 + P10. User instruction was "design-doc first, no implementation in same iter" for the persistence gaps (TD-3/4/5/9) and "needs UX design-doc" for P10 Gold Map ROI. Both items were P3 high-value high-risk per iter 125's stopping-point brief. No production code touched in this iter; produced two architectural design-docs in a new `docs/design/` directory.

Work Log:
- Cloned repo. Read `STATUS.md` (iter 125 SHIPPED — DOC-1 closed, 111 warnings), `worklog.md` (iter 124 + iter 125), `AGENT_NAVIGATION.md` (post-trim 67KB / 233 lines), `PRODUCT_VISION.md` (full), `docs/MARKET_PLAYBOOK.md` (full — §C.8 P10 spec + P10 roadmap entry + B-section coverage table), `PoE2_Flipper_Canonical_Formulas.md` §7 (`market_spread` formula) + §8 (Bellman-Ford triangular) + §13 (gold-to-Chaos rate observation method + default 0.001 + bounds + 24h staleness).
- Read source files for TD-* context: `backend/data/historical.py` (590 lines — full HistoricalStore, `_CREATE_TABLES_SQL`, `_prune_old_league_data` chunked pattern, `_prune_old_records`, `_drop_obsolete_tables` dropping `gold_chaos_rates`), `backend/api/data_snapshot.py` (SnapshotManager `_refresh()` flow — confirmed `price_histories` already populated for every active currency, TD-9 needs only wiring), `backend/data/providers/poe2scout.py:get_daily_stats()` (line 735 — TD-5 provider method already exists, just never called by DataSnapshot), `backend/arbitrage/triangular.py` (TD-3 — `find_triangular_arbitrage`, `simulate_cycle_integers`, `find_min_profitable_start` already produce the `executable_estimate` field), `src/components/dashboard/flips-helpers.ts:deriveTrendSparklineData` (TD-9 — synthetic 6-point shape derived from `momentum × volatility`), `src/components/dashboard/dashboard-page.tsx:TAB_MAP` (14 entries — confirmed comment "15 in iter 109" is stale; actual count is 14).
- **Scope decision.** Two design-docs to produce: (1) unified persistence-layer design for TD-3/4/5/9 (one doc covering all four — they share the same root cause: computed metrics not persisted), (2) UX + ROI formula alignment design for P10. Both follow the user's "design-doc first, no implementation in same iter" mandate. No code changes, no schema migrations, no test additions.
- **Produced `docs/design/TD-3-4-5-9-persistence-gaps-design.md`** + **`docs/design/P10-gold-map-roi-design.md`**. (See iter 126 STATUS.md entry for the doc contents summary.)
- **Documentation updates:** `STATUS.md` header + design-docs section + DOC-2 closed-KI + TD backlog rows updated with design-doc references. `AGENT_NAVIGATION.md` header bump + new `docs/design/` row. `worklog.md` trimmed iter 124 (now in `git log`), shows iter 125 + iter 126.
- **Verification (docs-only change — expect zero impact on tsc/eslint/jest):** Confirmed no production code files touched. Did NOT run tsc/eslint/jest baselines because zero `.ts`/`.tsx`/`.py` files were modified — the iter 125 baselines (tsc green, 111 lint warnings, 622 jest green) are preserved by construction.

Stage Summary:
- **iter 126 SHIPPED — two architectural design-docs produced.** No production code touched. All baselines identical to iter 125 by construction. Tracked as **DOC-2** (closed).
- **Modified files (3):** `STATUS.md`, `AGENT_NAVIGATION.md`, `worklog.md`.
- **New files (2):** `docs/design/TD-3-4-5-9-persistence-gaps-design.md`, `docs/design/P10-gold-map-roi-design.md`.
- **Stopping point:** iter 126 = two design-docs SHIPPED, no code changes. Recommended iter 127: ship TD-9 Phase 1 + P10 Phase 1 MVP together (both low-risk, user-visible).
