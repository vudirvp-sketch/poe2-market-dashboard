# Worklog — PoE2 Market Dashboard

> Append-only shared multi-agent work log. Each section starts with `---`.
> Old task history is in `git log`. Trimmed to last 2 iterations.

---

Task ID: iter-133
Agent: main
Task: iter 133 — KI-26-audit. Per iter 132's stopping-point brief: KI-26-audit (P4) is the next priority — verify the 16 other `replace(tzinfo=timezone.utc)` call sites in `backend/` flagged as latent timezone bugs by KI-26. Goal: classify each site as SAFE (input is naive UTC from SQLite/POE2Scout) vs UNSAFE (input could be naive LOCAL from `datetime.now()` or API request body), then fix UNSAFE sites with `astimezone(timezone.utc)` (same fix pattern as KI-26). Backend-only, no UI impact. Per user instruction: "Если найден новый баг — сначала документируй в STATUS.md как Known Issue, потом фиксись" — document findings before fixing.

Work Log:
- Cloned repo. Read `STATUS.md` (iter 132 SHIPPED — P10 Phase 2, 698 jest green, 1432 pytest baseline), `worklog.md` (iter 131 + iter 132), iter 132 stopping-point brief (KI-26-audit is next priority).
- Grep'd `backend/` for `replace(tzinfo\s*=\s*(timezone\.utc|utc|pytz\.UTC|dt\.tz\.utc)` — found 17 hits across 9 files. One is a docstring (`triangular_cycles.py:147` — KI-26 fix rationale comment), leaving 16 actual call sites.
- For each call site, traced the input source by reading the surrounding function + callers (3–5 hops each):
  - `lifecycle.py:115, 117` — `current = now or datetime.now(timezone.utc)` (line 110): if caller passes `now` as naive local → UNSAFE. `reference = self._reference_date()` (line 111): `_patch_reset_date` is set via `reset_for_major_patch(patch_datetime)` from `routes_events.py:124` where `patch_ts = event_timestamp or datetime.now(timezone.utc)` — `event_timestamp` from API request body can be naive (pydantic accepts naive ISO). UNSAFE.
  - `arbitrage/triangular.py:146` — `snapshot_time` param of `_compute_confidence`. Production callers (`routes_arbitrage.py:871`, `triangular_cycles.py:217`) pass aware UTC (`datetime.now(timezone.utc)` or `snapshot.fetched_at` which defaults to aware). But the function signature accepts `datetime | None` and the `if snapshot_time.tzinfo is None` branch exists — same latent pattern as KI-26. UNSAFE.
  - `events.py:535` — `expires_at` from SQLite via `datetime.fromisoformat()` (line 158). Original write is `datetime.now(timezone.utc) + timedelta(...)` (line 218) → ISO with `+00:00` → round-trip preserves awareness. Naive case = legacy UTC data. SAFE.
  - `intraday_patterns.py:155, 211` — `time_val` from POE2Scout `price_logs` (UTC server). SAFE. Line 211 already uses defensive `astimezone if tzinfo else replace` form.
  - `circuit_patterns.py:224`, `speculation.py:133`, `weekly_patterns.py:185, 247`, `pricing.py:165`, `speculation_backtest.py:154, 161` — all read `time_val`/`ts` from POE2Scout `price_logs` (UTC source). SAFE.
  - `time_series.py:247, 407, 939` — `last_ts = timestamps[-1]` from caller. `ForecastEngine` is exposed via `get_forecast_engine()` but NOT called from any production route (only tests). Naive case = assume UTC (POE2Scout source). SAFE in practice.
- **Audit verdict: 3 UNSAFE (lifecycle ×2, triangular ×1), 14 SAFE.** Documented the full per-site classification table in `STATUS.md` § "KI-26-audit" BEFORE applying fixes (per user instruction).
- **Fixes applied (KI-27):**
  - `backend/economy/lifecycle.py:115, 117` — changed `replace(tzinfo=timezone.utc)` → `astimezone(timezone.utc)` for both `current` and `reference` in `days_since_reference`. Added 9-line block comment explaining KI-27 + when each input can be naive.
  - `backend/arbitrage/triangular.py:146` — changed `replace(tzinfo=timezone.utc)` → `astimezone(timezone.utc)` for `snapshot_time` in `_compute_confidence`. Added 6-line block comment.
- **Regression tests (4 new pytest):**
  - `tests/test_lifecycle.py::TestNaiveDatetimeTimezoneHandling` (2 tests) — `test_naive_current_datetime_uses_astimezone`: passes a naive `now`, asserts `days_since_reference` matches `floor((naive.astimezone(utc) - league_start) / 86400)`. `test_naive_patch_reset_date_uses_astimezone`: sets a naive `patch_reset_date`, asserts `days_since_reference` matches `floor((aware_now - naive.astimezone(utc)) / 86400)`. Both tests would FAIL with the old `replace(tzinfo=utc)` code in any non-UTC timezone (the resulting delta would differ from `astimezone` conversion by the timezone offset).
  - `tests/test_triangular.py::TestComputeConfidenceNaiveDatetime` (2 tests) — `test_naive_snapshot_time_uses_astimezone`: passes a naive `snapshot_time` (30min ago via `datetime.now() - timedelta(minutes=30)`), asserts `0 ≤ confidence ≤ 1` AND `expected_freshness ≥ 0.4` (catches the regression where buggy code drives freshness to 0 or >1). `test_none_snapshot_time_skips_freshness`: regression guard for the surrounding `None`-skip path.
- **Verification:** Full pytest suite in system default TZ (UTC) = 1436 passed (1432 baseline + 4 new), 0 failures, 0 regressions, 10.24s. Re-ran full suite under `TZ=Asia/Yekaterinburg` (UTC+5) = 1436 passed, 0 failures. Timezone-safe in both UTC and UTC+5.
- **Documentation updates:**
  - `STATUS.md` — updated "Last updated" header (iter 133); added KI-27 to "Known Issues — closed (recent)" with full audit summary; removed `KI-26-audit` from "Technical-debt backlog" (audit complete); added new "KI-26-audit — `replace(tzinfo=utc)` call-site classification (iter 133)" section with UNSAFE/SAFE tables + audit pattern note; added 2 new Quick Reference rows (KI-27 lifecycle + KI-27 triangular); updated KI-26 closed entry to reference audit completion via KI-27; compressed verbose P10/TD-5/KI-26 closed entries to 2-3 lines each; compressed react-hooks Recipes 1-7 from ~10 lines each to a single summary table (1 line per recipe) with canonical-example file references; expanded "Naive datetime handling" insight to reference KI-27 + audit pattern.
  - `AGENT_NAVIGATION.md` — updated header from iter 129 to iter 133.
  - `worklog.md` — prepended iter-133 entry, trimmed to last 2 iterations (iter 132 + iter 133 — iter 131 moved to `git log`).
  - `README_iter124.md` — DELETED (stale merge instructions from iter 124, no longer needed).

Stage Summary:
- **iter 133 SHIPPED — KI-26-audit complete + KI-27 closed.** All 17 `replace(tzinfo=timezone.utc)` call sites in `backend/` audited: 3 UNSAFE → fixed with `astimezone(timezone.utc)` (same fix pattern as KI-26); 14 SAFE (input is naive UTC from SQLite/POE2Scout). 4 new pytest regression tests. 1436 pytest green in both UTC and UTC+5, 0 regressions.
- **Modified files (4):** `backend/economy/lifecycle.py` (KI-27 fix in `days_since_reference` + comment), `backend/arbitrage/triangular.py` (KI-27 fix in `_compute_confidence` + comment), `tests/test_lifecycle.py` (+2 regression tests in new `TestNaiveDatetimeTimezoneHandling` class), `tests/test_triangular.py` (+2 regression tests in new `TestComputeConfidenceNaiveDatetime` class).
- **Doc updates (4):** `STATUS.md` (KI-27 entry + new audit-classification section + 2 Quick Reference rows + compressed verbose history + compressed Recipes table), `AGENT_NAVIGATION.md` (header iter 129 → iter 133), `worklog.md` (iter 133 prepended, iter 131 trimmed), `README_iter124.md` DELETED.
- **Test counts:** pytest 1436 green (1432 baseline + 4 new), 0 regressions. Timezone-safe in both UTC and UTC+5. Jest/tsc/lint: not run (no frontend files touched).
- **What was NOT done (intentionally deferred to iter 134+):**
  - **TD-3 pipeline_cache optimization** — eliminate doubled compute cost by populating `pipeline_cache` from `_refresh()` (currently `find_triangular_arbitrage` runs both in `_refresh` AND in the live `/triangular` route's first-request cache). Backend-only, no UI impact.
  - **TD-9 fallback removal** — `deriveTrendSparklineData` should be deleted iter 134+ if production logs confirm no fallback path is hit for 2 iters.
  - **Production verification P10 Phase 2:** (1) verify `/api/flipper/triangular/history` proxy returns non-empty `points` once `triangular_cycles` table has ≥ 2 rows; (2) verify the trend chart renders an SVG line on a real backend; (3) verify the Days selector refetches with the new `days` query param.
  - **P10 Phase 3 (gold rate SQLite promotion)** — optional, defer until feature gets adoption.
- **Stopping point:** iter 133 = KI-26-audit + KI-27 SHIPPED. All baselines preserved (1436 pytest green in both UTC and UTC+5, 0 regressions). Next iter (iter 134+) candidates in priority order: (a) **TD-3 pipeline_cache optimization** (eliminate doubled compute cost — backend-only, no UI impact); (b) **TD-9 fallback removal** (delete `deriveTrendSparklineData` once production logs confirm no fallback hits for 2 iters); (c) **Production verification P10 Phase 2** (verify the trend chart works end-to-end on a real backend).

---

Task ID: iter-132
Agent: main
Task: iter 132 — P10 Phase 2 (Gold Map ROI trend chart). Per iter 131's stopping-point brief: P10 Phase 2 is the next priority — now that TD-3 Phase 3 (iter 129) shipped `/api/v1/arbitrage/triangular/history`, the Gold Map ROI tab can render a trend chart of historical cycle profitability. Scope = new `gold-map-roi-trend-chart.tsx` SVG component + new `/api/flipper/triangular/history` proxy route + new TS types + wire trend chart into `gold-map-roi-tab.tsx` below the calculator + 13 new i18n keys × 4 locales + ~10 jest tests. Frontend-only, NO backend changes (reuses iter-129 read-only history route). Design doc: `docs/design/P10-gold-map-roi-design.md` §7 (UI layout) + §12 Phase 2.

Work Log:
- Cloned repo. Read `STATUS.md` (iter 131 SHIPPED — TD-5 Phase 4, 1432 pytest green), `worklog.md` (iter 128 + iter 129 + iter 131), `docs/design/P10-gold-map-roi-design.md` (full — §1 problem statement, §3.3 gold rate source options [B localStorage MVP chosen], §4.3 ROI formula reconciliation, §5.4 TAB_MAP placement at index 13, §7 UI layout with trend chart section, §12 Phase 2 = trend chart consumes `/api/v1/arbitrage/triangular/history`).
- Read source files for context: `gold-map-roi-tab.tsx`, `gold-map-roi-calculator.tsx`, `storage-value-history-chart.tsx` (SVG chart template), `routes_arbitrage.py:943-1031` (TD-3 history route), `response_models.py:1112-1145` (pydantic models), `types.ts:249-282` (existing TS types), `flipper/triangular/route.ts` (existing live proxy), `flipper/mirror-divine-arb/route.ts` (proxy template), `flipper-proxy.ts:425-430` (auto camelCase), `case-transform.ts:22-36`, `utils.ts:38-46` (`formatLocaleDate`), `storage-value-history-chart.test.tsx` (test template), `gold-map-roi-tab.test.tsx` (Phase 1 tab tests — must NOT regress).
- **P10 Phase 2 implementation — `src/lib/types.ts`:** Added 2 new TS interfaces after `TriangularResponse`. `TriangularCycleHistoryPoint` (8 fields, camelCase — `timestamp`, `cycleKey`, `cycleCurrencies`, `rawProfitPct`, `executableEstimate`, `executableProfit`, `confidence`, `snapshotAgeSec`; all numeric `number | null` to mirror backend `float | None` / `int | None` for legacy rows). `TriangularCyclesHistoryResponse` (7 fields — `league`, `cycleKey`, `days`, `points`, `availableCycleKeys`, `dataAvailable`, `fetchedAt`).
- **P10 Phase 2 implementation — `src/app/api/flipper/triangular/history/route.ts` (NEW):** Next.js proxy route. Calls `proxyWithFallback("/api/v1/arbitrage/triangular/history", ...)` with both `offlineFallback` and `insufficientDataFallback` returning `{ league: "", cycleKey, days, points: [], availableCycleKeys: [], dataAvailable: false, fetchedAt }` — frontend trend chart sees a graceful "no history yet" state instead of an error.
- **P10 Phase 2 implementation — `src/components/dashboard/gold-map-roi-trend-chart.tsx` (NEW, ~310 lines):** Dependency-free SVG chart following the `storage-value-history-chart.tsx` template. Props: `backendOnline: boolean`, `initialDays?: DaysOption` (default 7). Component structure: (1) `useState<DaysOption>` for the days selector (1/7/14/30/90). (2) `useQuery<TriangularCyclesHistoryResponse>` with `queryKey: ["gold-map-roi-trend", days]`, `staleTime: 60_000`, `retry: 1`. (3) `points` wrapped in `useMemo`. (4) `trendPoints = useMemo(() => pickBestPerTimestamp(points), [points])` — dedupes to one point per timestamp keeping the highest non-null `rawProfitPct`. (5) `geometry = useMemo(...)` — builds SVG path + Y-axis ticks (always includes 0) + X-axis ticks + zero-line Y. (6) Render branches: offline → loading → error → "no history yet" → full chart.
- **Pure helper `pickBestPerTimestamp` (exported for unit tests):** Map-based dedup keyed by epoch-ms. For each timestamp, keeps the entry with the highest non-null `rawProfitPct`; prefers non-null over null. Returns array sorted ascending by timestamp.
- **Chart signal decision — raw_profit_pct, NOT expected_div:** `expected_div` depends on user inputs (`goldAmount`, `mapCost`, `goldPerDiv`) that have NO historical persistence. The historical signal we DO have is `raw_profit_pct`, which is exactly the multiplier the live `computeRoi` helper consumes.
- **P10 Phase 2 implementation — `src/components/dashboard/gold-map-roi-tab.tsx`:** Added `import { GoldMapRoiTrendChart }` + `<GoldMapRoiTrendChart backendOnline={backendOnline} />` in the main render branch AFTER `<GoldMapRoiCalculator>` and BEFORE the fetched-at footer.
- **i18n keys — 13 new × 4 locales (1243 total per locale, parity preserved):** `goldMapTrendTitle`, `goldMapTrendSubtitle`, `goldMapTrendLoading`, `goldMapTrendError`, `goldMapTrendOffline`, `goldMapTrendEmpty`, `goldMapTrendLegendBestCycle`, `goldMapTrendLegendZero`, `goldMapTrendPointCount`, `goldMapTrendCycleKeysCount`, `goldMapTrendFetchedAt`, `goldMapTrendDaysLabel`, `goldMapTrendDaysValue`.
- **Test suite — `src/__tests__/gold-map-roi-trend-chart.test.tsx` (NEW, 13 tests):** 2 describe blocks. (1) `pickBestPerTimestamp` (5 tests). (2) `GoldMapRoiTrendChart` component (8 tests). Uses same `mockFetchApi` + `jest.requireActual` pattern as `gold-map-roi-tab.test.tsx`.
- **Verification:** `npx tsc --noEmit` → 0 errors. `npx jest` (full suite) → 698/698 pass, 0 regressions. `npx eslint` on all 4 new/modified files → 0 errors, 0 warnings.
- **Documentation updates:** `STATUS.md`, `docs/design/P10-gold-map-roi-design.md`, `worklog.md`.

Stage Summary:
- **iter 132 SHIPPED — P10 Phase 2 complete.** `GoldMapRoiTrendChart` SVG chart wired into `gold-map-roi-tab.tsx` below the calculator. NO backend changes — reuses existing iter-129 read-only route.
- **Test counts:** jest 698 green (685 baseline + 13 new), 0 regressions. `tsc --noEmit` clean. ESLint clean. i18n parity preserved (1243 keys × 4 locales). pytest NOT run — no backend files touched.
- **Stopping point:** iter 132 = P10 Phase 2 SHIPPED. Next iter (iter 133+) candidates in priority order: (a) **KI-26-audit** (low-risk verification of 16 timezone call sites); (b) **TD-3 pipeline_cache optimization** (eliminate doubled compute cost); (c) **TD-9 fallback removal**.
