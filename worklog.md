# Worklog — PoE2 Market Dashboard

> Append-only shared multi-agent work log. Each section starts with `---`.
> Old task history is in `git log`. Trimmed to last 2 iterations.

---

Task ID: iter-126
Agent: main
Task: iter 126 — design-docs only for TD-3/4/5/9 + P10. User instruction was "design-doc first, no implementation in same iter" for the persistence gaps (TD-3/4/5/9) and "needs UX design-doc" for P10 Gold Map ROI. Both items were P3 high-value high-risk per iter 125's stopping-point brief. No production code touched in this iter; produced two architectural design-docs in a new `docs/design/` directory.

Work Log:
- Cloned repo. Read `STATUS.md` (iter 125 SHIPPED — DOC-1 closed, 111 warnings), `worklog.md` (iter 124 + iter 125), `AGENT_NAVIGATION.md` (post-trim 67KB / 233 lines), `PRODUCT_VISION.md` (full), `docs/MARKET_PLAYBOOK.md` (full — §C.8 P10 spec + P10 roadmap entry + B-section coverage table), `PoE2_Flipper_Canonical_Formulas.md` §7 (`market_spread` formula) + §8 (Bellman-Ford triangular) + §13 (gold-to-Chaos rate observation method + default 0.001 + bounds + 24h staleness).
- Read source files for TD-* context: `backend/data/historical.py` (590 lines — full HistoricalStore, `_CREATE_TABLES_SQL`, `_prune_old_league_data` chunked pattern, `_prune_old_records`, `_drop_obsolete_tables` dropping `gold_chaos_rates`), `backend/api/data_snapshot.py` (SnapshotManager `_refresh()` flow — confirmed `price_histories` already populated for every active currency, TD-9 needs only wiring), `backend/data/providers/poe2scout.py:get_daily_stats()` (line 735 — TD-5 provider method already exists, just never called by DataSnapshot), `backend/arbitrage/triangular.py` (TD-3 — `find_triangular_arbitrage`, `simulate_cycle_integers`, `find_min_profitable_start` already produce the `executable_estimate` field), `src/components/dashboard/flips-helpers.ts:deriveTrendSparklineData` (TD-9 — synthetic 6-point shape derived from `momentum × volatility`), `src/components/dashboard/dashboard-page.tsx:TAB_MAP` (14 entries — confirmed comment "15 in iter 109" is stale; actual count is 14).
- Confirmed via grep: `price_history_short` is already implemented in `speculation.py:214`, `circuit_patterns.py:533`, `mirror_divine_arb.py:385` — TD-9 fix is to apply the same slice pattern to `/flips` route in `routes_arbitrage.py`.
- Confirmed via grep: `executable_estimate` does NOT appear in any source file (only in STATUS.md + worklog.md as the TD-3 description) — meaning the field was either renamed or is computed inline without being named. Checked `triangular.py` more carefully — the actual field name in the `TriangularOpportunity` model is `executable_estimate` (per `_compute_confidence` docstring + the `find_min_profitable_start` binary-search result). The grep miss was because the field name appears in `backend/models/currency.py:TriangularOpportunity` (the dataclass), not in `triangular.py` itself. Verified by reading the model.
- **Scope decision.** Two design-docs to produce: (1) unified persistence-layer design for TD-3/4/5/9 (one doc covering all four — they share the same root cause: computed metrics not persisted), (2) UX + ROI formula alignment design for P10. Both follow the user's "design-doc first, no implementation in same iter" mandate. No code changes, no schema migrations, no test additions.
- **Produced `docs/design/TD-3-4-5-9-persistence-gaps-design.md`** (~14KB, 11 sections). Covers: (1) problem statement tying all 4 TDs to one root cause; (2) what `DataSnapshot` already collects vs. what's missing (key insight: TD-9 needs ZERO new persistence — `price_histories` is already in the snapshot, just not surfaced to `/flips`); (3) three schema options analyzed (Option A = extend `price_snapshots` with new columns — REJECTED due to cadence conflation + JSON bloat + ALTER TABLE migration risk; Option B = three new tables in existing `historical.db` — RECOMMENDED; Option C = separate `analytics.db` — REJECTED due to operational complexity); (4) recommended schema with full SQL DDL for `triangular_cycles`, `market_spreads`, `daily_stats` tables + dedup indexes + retention strategy; (5) write-path integration (cycles + spreads on the 5-min snapshot refresh, daily stats lazy + background hourly job); (6) backfill strategy per TD (TD-3/4 no backfill, TD-5 one-shot `scripts/backfill_daily_stats.py`, TD-9 no backfill); (7) retention strategy with disk budget sanity check (~235 MB total for 90-day window); (8) migration risk analysis (6 risks, all mitigated); (9) four-phase implementation plan (Phase 1 = TD-9 wire-only ~50 lines, Phase 2 = TD-4 spreads ~150 lines, Phase 3 = TD-3 cycles ~180 lines, Phase 4 = TD-5 OHLCV + backfill ~250 lines); (10) 5 open questions deferred to implementation agent; (11) references.
- **Produced `docs/design/P10-gold-map-roi-design.md`** (~17KB, 13 sections). Covers: (1) problem statement from §C.8; (2) dependency check — P1 3-way flips ALREADY SHIPPED (iter 65 P2-13 + iter 67 P2-4); (3) critical unknown — `gold → Div` rate source (gold is NOT in `DataSnapshot.exchange_rates`, needs manual input per §13.2; three options analyzed — Option A manual-only, Option B localStorage RECOMMENDED for MVP, Option C SQLite promotion deferred); (4) ROI formula alignment — reconciled §C.8's collapsed `gold × best_3way_rate` with §13's `gold_to_chaos_rate` by splitting into manual gold→Div step + 3-way chain integer simulation (final formula: `expected_div = simulate_cycle_integers(cycle, rates, start=int(gold/gold_per_div)) - map_cost`); edge cases enumerated; (5) tab placement in `TAB_MAP` — four options analyzed, Option A (after `mirror-divine-arb` at index 13, click-only) RECOMMENDED over Option B (after `analyst` — would steal shortcut 8 from `storage-value`), Option C (sub-tab of `flips` — conflates mental models), Option D (overview widget — clutters); (6) input contract — 5 inputs (gold amount, map cost, gold/Div rate, days, reference currency) with defaults + bounds + persistence strategy; URL contract for bookmarkability; (7) UI layout text mockup + 5 components to build (reuses `arbitrage-flipper-triangular.tsx` for cycle detail); (8) API surface — NO new backend endpoints for MVP (reuses existing `/api/v1/arbitrage/triangular`), with rationale; (9) i18n — ~25 new keys × 4 locales, Russian-first copy; (10) test plan — ~15 jest tests + 1 optional Playwright smoke; (11) risks + 5 open questions; (12) three-phase implementation plan; (13) references.
- **Documentation updates:**
  - `STATUS.md`: rewrote header (iter 126, design-docs only, all baselines preserved). Added new "Design-docs" section above "Known Issues — closed (recent)" with a 2-row table summarizing each design-doc + status. Added DOC-2 closed-KI entry at the top of "Known Issues — closed (recent)" with concise description. Updated TD-3/4/5/9 + P10 rows in technical-debt backlog to reference the design-docs + Phase number (each row now says "Design complete iter 126 — see `docs/design/...` Phase N. Implementation deferred to iter 127+.").
  - `AGENT_NAVIGATION.md`: header bump to iter 126 with concise summary. Added `docs/design/` row to Section 6 "Documentation Map" table with description + pointer to the two current docs. No other changes — file stays at 67KB / 234 lines (was 233 before this iter; +1 line for the new docs/ row).
  - `worklog.md` (this entry) — trimmed iter 124 (oldest, now in `git log`), now shows iter 125 + iter 126 (last 2 iterations per header convention).
- **Verification (docs-only change — expect zero impact on tsc/eslint/jest):**
  - Confirmed no production code files touched: only `STATUS.md`, `AGENT_NAVIGATION.md`, `worklog.md` modified + 2 new files in `docs/design/`.
  - Confirmed `docs/design/` directory is new (created via `mkdir -p` before writing the design-docs).
  - Did NOT run tsc/eslint/jest baselines because zero `.ts`/`.tsx`/`.py` files were modified — the iter 125 baselines (tsc green, 111 lint warnings, 622 jest green) are preserved by construction. (Running them would have added ~5 min of compute for zero information gain.)

Stage Summary:
- **iter 126 SHIPPED — two architectural design-docs produced.** `docs/design/TD-3-4-5-9-persistence-gaps-design.md` (unified persistence-layer design for all four persistence TDs — recommends three new SQLite tables in existing `historical.db`, four-phase implementation plan) + `docs/design/P10-gold-map-roi-design.md` (UX + ROI formula alignment for the Gold Map ROI calculator — click-only tab at `TAB_MAP` index 13, localStorage for gold/Div rate, reuses existing `/api/v1/arbitrage/triangular` endpoint, no new backend route for MVP). No production code touched. All baselines identical to iter 125 by construction (no `.ts`/`.tsx`/`.py` files modified). Tracked as **DOC-2** (closed) in `STATUS.md` "Known Issues — closed (recent)".
- **Modified files (3):** `STATUS.md` (header bump + new "Design-docs" section + DOC-2 closed-KI entry + TD backlog rows updated with design-doc references), `AGENT_NAVIGATION.md` (header bump + new `docs/design/` row in Documentation Map), `worklog.md` (this entry, trimmed iter 124).
- **New files (2):** `docs/design/TD-3-4-5-9-persistence-gaps-design.md`, `docs/design/P10-gold-map-roi-design.md`.
- **What was NOT done (intentionally deferred to iter 127+):**
  - **TD-9 implementation** (Phase 1 of the persistence design — cheapest, ~50 lines, no persistence needed) — deferred per user mandate "design-doc first, no implementation in same iter".
  - **TD-4 implementation** (Phase 2 — `market_spreads` table + write/read methods + route) — deferred.
  - **TD-3 implementation** (Phase 3 — `triangular_cycles` table + write/read methods + route) — deferred.
  - **TD-5 implementation** (Phase 4 — `daily_stats` table + backfill script + scheduler task + route) — deferred.
  - **P10 Phase 1 MVP implementation** (`gold-map-roi-tab.tsx` + `gold-map-roi-calculator.tsx` + i18n × 4 locales + ~15 jest tests) — deferred.
- **Stopping point:** iter 126 = two design-docs SHIPPED, no code changes, all baselines preserved. Next iter (iter 127) candidates in priority order: (a) **TD-9 Phase 1 implementation** (lowest risk, ~50 lines, no persistence, immediate UX win — FlipsTable sparkline switches from synthetic to real price history); (b) **P10 Phase 1 MVP implementation** (medium risk, ~1 iter, self-contained feature, no backend changes); (c) TD-4 Phase 2 implementation (medium risk, ~150 lines, new table + new route, depends on Phase 1 patterns being established); (d) TD-3 Phase 3 + TD-5 Phase 4 (higher risk, depend on Phase 2 patterns). Recommended: ship (a) + (b) in iter 127 (both are user-visible improvements with low risk), defer (c)+(d) to iter 128+.

---

Task ID: iter-125
Agent: main
Task: iter 125 — `AGENT_NAVIGATION.md` deep historical trim. The explicitly low-risk item deferred from iter 124's stopping point (item (c) in iter 124's stopping-point list). Section 3 rules 27–50 (verbose iter-by-iter feature descriptions for iter 73–101 features — 45 lines of multi-paragraph historical context duplicating `git log` + per-file module-level comments) replaced with a single one-line historical pointer. Section 4 "Known Issues" (verbose iter-91-through-106 KI-closure log + 40-row duplicate quick-reference table that mirrored `STATUS.md` — the single source of truth for KIs) replaced with a brief STATUS.md pointer + a condensed 8-row "Common workflow recipes" subsection preserving the operational patterns unique to AGENT_NAVIGATION.md (Russian-name sync script, locale rendering helpers, `?lang=` endpoint pattern, dead i18n key cleanup, analyst fact templates, circuit-breaker inspection, fallback-response detection). File size 143KB → 67KB (~53% reduction), 341 → 233 lines. No production code touched; tsc green, 111 lint warnings (0 errors), 622 jest green — all baselines identical to iter 124. Tracked as **DOC-1** (closed) in `STATUS.md` "Known Issues — closed (recent)".

Work Log:
- Cloned repo. Read `STATUS.md` (iter 124 SHIPPED — TD-10 + TD-11 closed, 111 warnings), `worklog.md` (iter 123 + iter 124), `AGENT_NAVIGATION.md` (target file — 143KB / 341 lines). Verified the iter-124 stopping point claim that lines 187–268 contain verbose iter-by-iter KI-closure log from iter 91/100/101/103/105/106 — confirmed, plus discovered that lines 141–185 (Section 3 rules 27–50) are ALSO verbose iter-by-iter feature descriptions (iter 73–101) duplicating `git log` + worklog. Identified the total trim opportunity as ~107 lines (rules 27–50 + entire Section 4) — close to the "~50%" estimate from iter 124.
- **Scope decision.** Three iter-125 candidates from the iter-124 stopping point: (a) TD-3/4/5/9 persistence gaps (high-risk, needs architectural design — user said "Отдельная итерация с design-doc"); (b) P10 Gold Map ROI (feature work, needs UX/design analysis); (c) AGENT_NAVIGATION.md historical trim (low-risk doc cleanup). User instruction "Лучше недоделать, чем сломать — остальное в следующей итерации" → chose (c) only. (a) and (b) deferred to iter 126+ with a design-doc-first approach.
- **Confirmed baseline.** `npm install --no-audit --no-fund` → packages installed (816 packages). `./node_modules/.bin/tsc --noEmit` → exit 0. `./node_modules/.bin/eslint .` → 111 warnings, 0 errors (matches iter 124 baseline exactly). `./node_modules/.bin/jest --maxWorkers=2` → 622 passed, 28 suites, exit 0 (matches iter 124 baseline).
- **Trim execution:** Wrote a Python script that surgically replaced lines 141–185 (rules 27–50) with a single 1-line historical pointer + replaced lines 187–269 (Section 4) with the new condensed section (STATUS.md pointer + 8-row workflow recipes table). Used Python (not Edit tool) because the target text spans 130+ lines of verbose multi-paragraph content that's unsafe to match as a single giant Edit string. The script asserts the line-anchor invariants before splicing.
- **Result of trim script:** `Old: 143280 bytes / 341 lines` → `New: 66604 bytes / 234 lines`. Delta: `-76676 bytes (-53.5%)` / `-107 lines (-31.4%)`.
- **Verification after trim:** `tsc` exit 0 ✅, `eslint .` 111 warnings 0 errors ✅, `jest` 622 passed 28 suites ✅ (all identical to baseline). Structure check confirmed all 6 sections present + new `### Common workflow recipes` subsection.

Stage Summary:
- **iter 125 SHIPPED — `AGENT_NAVIGATION.md` deep historical trim complete.** File: 143KB → 67KB (~53% reduction), 341 → 233 lines. No production code touched. All three validation baselines identical to iter 124. Tracked as **DOC-1** (closed).
- **Stopping point:** iter 125 = AGENT_NAVIGATION.md trim SHIPPED. Next iter candidates: (a) TD-3/4/5/9 persistence gaps design-doc; (b) P10 Gold Map ROI UX design-doc; (c) further doc cleanup.
