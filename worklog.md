# Worklog — PoE2 Market Dashboard

> Append-only shared multi-agent work log. Each section starts with `---`.
> Old task history is in `git log`. Trimmed to last 2 iterations.

---

Task ID: iter-125
Agent: main
Task: iter 125 — `AGENT_NAVIGATION.md` deep historical trim. The explicitly low-risk item deferred from iter 124's stopping point (item (c) in iter 124's stopping-point list). Section 3 rules 27–50 (verbose iter-by-iter feature descriptions for iter 73–101 features — 45 lines of multi-paragraph historical context duplicating `git log` + per-file module-level comments) replaced with a single one-line historical pointer. Section 4 "Known Issues" (verbose iter-91-through-106 KI-closure log + 40-row duplicate quick-reference table that mirrored `STATUS.md` — the single source of truth for KIs) replaced with a brief STATUS.md pointer + a condensed 8-row "Common workflow recipes" subsection preserving the operational patterns unique to AGENT_NAVIGATION.md (Russian-name sync script, locale rendering helpers, `?lang=` endpoint pattern, dead i18n key cleanup, analyst fact templates, circuit-breaker inspection, fallback-response detection). File size 143KB → 67KB (~53% reduction), 341 → 233 lines. No production code touched; tsc green, 111 lint warnings (0 errors), 622 jest green — all baselines identical to iter 124. Tracked as **DOC-1** (closed) in `STATUS.md` "Known Issues — closed (recent)".

Work Log:
- Cloned repo. Read `STATUS.md` (iter 124 SHIPPED — TD-10 + TD-11 closed, 111 warnings), `worklog.md` (iter 123 + iter 124), `AGENT_NAVIGATION.md` (target file — 143KB / 341 lines). Verified the iter-124 stopping point claim that lines 187–268 contain verbose iter-by-iter KI-closure log from iter 91/100/101/103/105/106 — confirmed, plus discovered that lines 141–185 (Section 3 rules 27–50) are ALSO verbose iter-by-iter feature descriptions (iter 73–101) duplicating `git log` + worklog. Identified the total trim opportunity as ~107 lines (rules 27–50 + entire Section 4) — close to the "~50%" estimate from iter 124.
- **Scope decision.** Three iter-125 candidates from the iter-124 stopping point: (a) TD-3/4/5/9 persistence gaps (high-risk, needs architectural design — user said "Отдельная итерация с design-doc"); (b) P10 Gold Map ROI (feature work, needs UX/design analysis); (c) AGENT_NAVIGATION.md historical trim (low-risk doc cleanup). User instruction "Лучше недоделать, чем сломать — остальное в следующей итерации" → chose (c) only. (a) and (b) deferred to iter 126+ with a design-doc-first approach.
- **Confirmed baseline.** `npm install --no-audit --no-fund` → packages installed (816 packages). `./node_modules/.bin/tsc --noEmit` → exit 0. `./node_modules/.bin/eslint .` → 111 warnings, 0 errors (matches iter 124 baseline exactly). `./node_modules/.bin/jest --maxWorkers=2` → 622 passed, 28 suites, exit 0 (matches iter 124 baseline).
- **Identification of what to KEEP vs TRIM in AGENT_NAVIGATION.md:**
  - KEEP: Header, Section 1 "Where Things Are" (80-row table — active file/directory reference), Section 2 "Build & Run Commands" (lean, current), Section 3 rules 1–26 (genuine architectural invariants — snake_case convention, ProcessPoolExecutor picklable-args rule, SSE payload contract, etc.), Section 5 "API Endpoints" (active REST endpoint reference), Section 6 "Documentation Map" (lean file-pointer table).
  - TRIM: Section 3 rules 27–50 (lines 141–185 — verbose iter-by-iter feature descriptions, each 5+ lines, duplicating `git log` per-feature commits + per-file module-level comments). Section 4 "Known Issues" entirely (lines 187–269 — verbose iter-91-through-106 KI-closure log duplicating `STATUS.md` "Known Issues — closed (recent)" + 40-row quick-reference table duplicating `STATUS.md` "Quick Reference — Frequent Problems" table).
  - PRESERVE from Section 4: The handful of "operational pattern" rows that are NOT bug-fix recipes but workflow guidance (Russian-name sync script, locale rendering helpers, `?lang=` endpoint pattern, dead i18n key cleanup, analyst fact templates, circuit-breaker inspection, fallback-response detection) — these don't belong in `STATUS.md` (which is for known-bug recovery) but DO belong in a navigation guide. Moved into a new "Common workflow recipes" subsection (8 rows) inside the rewritten Section 4.
- **Trim execution:** Wrote a Python script `/home/z/my-project/scripts/trim_agent_nav.py` that surgically replaces lines 141–185 (rules 27–50) with a single 1-line historical pointer + replaces lines 187–269 (Section 4) with the new condensed section (STATUS.md pointer + 8-row workflow recipes table). Used Python (not Edit tool) because the target text spans 130+ lines of verbose multi-paragraph content that's unsafe to match as a single giant Edit string. The script asserts the line-anchor invariants before splicing (line 141 starts with "27. **Storage Value tab**", line 187 starts with "## 4. Known Issues", line 270 starts with "## 5. API Endpoints") — all three assertions passed, confirming the splice points were exactly where expected.
- **Result of trim script:** `Old: 143280 bytes / 341 lines` → `New: 66604 bytes / 234 lines`. Delta: `-76676 bytes (-53.5%)` / `-107 lines (-31.4%)`. After the script run, fixed a minor cosmetic double-blank-line artifact between rule 27 and the new "## 4." heading via a tiny Edit.
- **Header line refined.** After the trim, updated the iter-125 header line to reflect the actual achieved numbers (53%, not ~47% as initially estimated) + added "8 rows of operational patterns unique to AGENT_NAVIGATION.md" to make the preservation contract explicit.
- **Verification after trim (docs-only change — expect zero impact on tsc/eslint/jest):**
  - `./node_modules/.bin/tsc --noEmit` → **exit 0** ✅ (identical to baseline)
  - `./node_modules/.bin/eslint .` → **111 warnings, 0 errors, exit 0** ✅ (identical to baseline)
  - `./node_modules/.bin/jest --maxWorkers=2` → **622 passed, 0 failed, 28 suites, exit 0** ✅ (identical to baseline)
  - Structure check: `grep '^## ' AGENT_NAVIGATION.md` → all 6 sections present (1. Where Things Are, 2. Build & Run, 3. Critical Rules, 4. Known Issues, 5. API Endpoints, 6. Documentation Map) + new `### Common workflow recipes` subsection under Section 4.
- **Documentation updates:**
  - `AGENT_NAVIGATION.md` — the trim itself (described above). Header line bumped to iter 125 with concise summary.
  - `STATUS.md`: rewrote header (iter 125, DOC-1 closed, 143KB → 67KB). Added DOC-1 entry at the top of "Known Issues — closed (recent)" with a concise description of what was trimmed + what was preserved. Kept TD-3/4/5/9 + P10 in the technical-debt backlog unchanged (none of those were addressed this iter — they remain the iter 126+ priorities).
  - `worklog.md` (this entry) — trimmed iter 123 (oldest, now in `git log`), now shows iter 124 + iter 125 (last 2 iterations per header convention).

Stage Summary:
- **iter 125 SHIPPED — `AGENT_NAVIGATION.md` deep historical trim complete.** File: 143KB → 67KB (~53% reduction), 341 → 233 lines. No production code touched. All three validation baselines identical to iter 124: `tsc` green, 111 lint warnings (0 errors), 622 jest tests passing (28 suites). Tracked as **DOC-1** (closed) in `STATUS.md`.
- **Modified files (3):** `AGENT_NAVIGATION.md` (deep trim — Section 3 rules 27–50 + Section 4 entirely replaced with brief pointers + condensed workflow-recipes subsection), `STATUS.md` (header bump + DOC-1 closed-KI entry), `worklog.md` (this entry).
- **What was NOT done (intentionally deferred to iter 126+):**
  - **TD-3/4/5/9 persistence gaps** (P3, high-value, high-risk) — needs architectural design-doc BEFORE any implementation. The 4 gaps: triangular arbitrage backtest persistence (`executable_estimate` cannot be backtested), `market_spread` not persisted in HistoricalStore, `DailyStatsHistory` POE2Scout OHLCV endpoint unused, FlipsTable sparkline uses derived `momentum × volatility` instead of real `priceHistoryShort`. Recommended approach for iter 126: produce a design-doc markdown analyzing (a) data sources already collected by DataSnapshot vs. what would need new collection, (b) persistence-layer schema options (extend existing SQLite `historical.py` vs. new table vs. new namespace), (c) backfill strategy, (d) migration risk. Do NOT implement in the same iter as the design.
  - **P10 Gold Map ROI** (§C.8, P3, feature work) — depends on P1 3-way flips (already shipped). Needs UX/design analysis: where does the Gold Map tab sit in `TAB_MAP` (currently 14 tabs, only 10 keyboard-shortcut slots — likely click-only like Intraday/Weekly/Liquid-Chain/Watchlist), what's the input contract (league + days + reference currency), what's the ROI computation formula (canonical formula lives in `PoE2_Flipper_Canonical_Formulas.md` §C.8 — verify alignment). Recommended approach for iter 127: UX design-doc first, then implementation iter.
  - **Further doc cleanup opportunities** identified during this iter but not actioned (conservative scope): (a) `PoE2_Flipper_Canonical_Formulas.md` is 57KB — may have similar verbose historical content; (b) `STATUS.md` "Key technical insights" recipes section (Recipe 1–7) is dense but all 7 recipes are still actively useful for the 4 React Compiler rules — leaving as-is until the rules are removed from the eslint config.
- **Stopping point:** iter 125 = `AGENT_NAVIGATION.md` deep historical trim SHIPPED (143KB → 67KB, no code changes, all baselines preserved). Next iter candidates in approximate priority order: (a) TD-3/4/5/9 persistence gaps design-doc (high-value, no implementation risk — write design only, defer implementation to a separate iter); (b) P10 Gold Map ROI UX design-doc (depends on §C.8 formula review); (c) further doc cleanup if (a)/(b) are still in design phase.

---

Task ID: iter-124
Agent: main
Task: iter 124 — TD-10 + TD-11 closed. Two low-risk high-value cleanups from the iter 123 stopping point. TD-10: 3 pre-existing lint warnings in `dashboard-page.tsx` (unused `ReferenceCurrency` import, unused `sseStatus` destructuring, `keyboardActions` useMemo exhaustive-deps). TD-11: repo cleanup — removed 26 obsolete iter-100–118 files (`MERGE_INSTRUCTIONS_iter*.md` ×8, `git_commands_iter*.txt` ×12, `DELETIONS.{sh,txt}`, `DELETE_obsolete_files.sh`, `README.txt`, `scripts_flipper-backend-bridge.ts.DELETED`, `scripts/DELETE_flipper-backend-bridge.ts`, stale tracked `flipper-bridge.log`). Lint 114 → 111, 0 errors. 622 jest green, tsc green.

Work Log:
- Cloned repo. Read STATUS.md (iter 123 SHIPPED — KI-24 FULLY RESOLVED, 114 warnings), worklog.md (iter 122 + iter 123), `src/components/dashboard/dashboard-page.tsx` (target for TD-10 — confirmed all 3 warnings via `npx eslint`). Confirmed the 26 obsolete files exist via `ls`. Confirmed `flipper-bridge.log` is tracked in git but listed in `.gitignore` (stale accidentally-committed runtime log, 692KB). Confirmed `AGENT_NAVIGATION.md` line 185 references `MERGE_INSTRUCTIONS_iter101.md` (will become a dead reference — fix in AGENT_NAVIGATION update).
- **Confirmed baseline.** `npm install` → packages installed. `npx tsc --noEmit` → exit 0. `npx eslint src/components/dashboard/dashboard-page.tsx` → 3 warnings (unused `ReferenceCurrency` line 163, unused `sseStatus` line 312, exhaustive-deps on `keyboardActions` useMemo line 732 — missing `TAB_MAP`, `openDetail`). `npx eslint .` → 114 warnings, 0 errors (matches iter 123 baseline). `npx jest --maxWorkers=2` → 622 passed, 28 suites, exit 0.
- **TD-10 Site 1 (unused `ReferenceCurrency` import):** The type was imported at line 163 but never used — `referenceCurrency` is a `useState("")` string, not typed as `ReferenceCurrency`. Removed the type from the `import type { ... }` block.
- **TD-10 Site 2 (unused `sseStatus`):** `const { status: sseStatus } = usePriceStream({...})` destructured `status` into `sseStatus` but never read it. The hook is called for its side effects (SSE subscription + React Query cache invalidation). Removed the destructuring — just call `usePriceStream({...})`.
- **TD-10 Site 3 (exhaustive-deps on `keyboardActions` useMemo):** Deps array `[tab, uiState.exchange.viewMode, setExchangeViewMode, navigableList, highlightedRowIndex, openPairDetail, setTab]` was missing `TAB_MAP` (used in `onSwitchTab`) and `openDetail` (used in `onEnter`). Two-part fix: (a) **Moved `TAB_MAP` to module level** (top of file, outside the component). It's a constant array — defining it inside the component created a new array identity every render, polluting every downstream memo. The 18-line comment block documenting the tab-order history moved with it. (b) **Added `openDetail` to the deps array.** This triggered a NEW warning: `setTab` (a plain wrapper function) changes identity every render. Fixed by wrapping `setTab` in `useCallback` with `[setActiveTab]` deps (`setTabLocal` is a stable useState setter, `setActiveTab` is a stable Zustand action). Result: all 3 warnings GONE, no new warnings.
- **TD-11 (repo cleanup):** Deleted 26 files in the cloned repo: 8 `MERGE_INSTRUCTIONS_iter*.md` (iter 101/102/103/105/106/107/108/109), 12 `git_commands_iter*.txt` (iter 101/102/103/105/106/107/108/109/110/111/117/118), `DELETIONS.sh`, `DELETIONS.txt`, `DELETE_obsolete_files.sh`, `README.txt`, `scripts_flipper-backend-bridge.ts.DELETED`, `scripts/DELETE_flipper-backend-bridge.ts`, `flipper-bridge.log` (stale tracked runtime log — already in `.gitignore`). Verified no source code references any of these files (the only references were in docs: `STATUS.md` TD-11 row + KI-19 quick-reference row, `AGENT_NAVIGATION.md` line 185 — all updated/cleaned). The `tsconfig.json` `exclude: ["**/DELETE_*"]` defense-in-depth remains.
- **Verification after TD-10:**
  - `npx tsc --noEmit` → **exit 0** ✅
  - `npx eslint src/components/dashboard/dashboard-page.tsx` → **0 warnings, 0 errors** ✅ (was 3 — all 3 TD-10 sites GONE)
  - `npx eslint .` → **0 errors, 111 warnings, exit 0** ✅ (was 114 → 111 = 3 warnings removed)
  - `npx jest --maxWorkers=2` → **622 passed, 0 failed, 28 suites, exit 0** ✅ (same count as iter 123 — no test regressions)
- **Verification after TD-11:** Re-ran tsc + eslint + jest — all green (111 warnings, 622 jest passed). Deletions are pure file-removal — no code path references any deleted file.
- **Documentation updates:**
  - `STATUS.md`: rewrote header (iter 124, TD-10 + TD-11 closed, lint 114 → 111). Added TD-10 + TD-11 to "Known Issues — closed (recent)" with concise fix summaries. Removed TD-10 + TD-11 from technical-debt backlog table (now 5 rows: TD-3/4/5/9 + P10). Updated KI-19 quick-reference row (DELETE_obsolete_files.sh removed, but `tsconfig.json` exclude remains as defense-in-depth). Updated `flipper-bridge.log` quick-reference row (file removed from repo tracking iter 124, `.gitignore` entry kept). Added new "exhaustive-deps stable-identity recipe" subsection documenting the iter-124 module-level-constant + useCallback-wrapper pattern.
  - `worklog.md` (this entry) — trimmed iter 122 (oldest, now in git log), now shows iter 123 + iter 124 (last 2 iterations per header convention).
  - `AGENT_NAVIGATION.md` — header bump to iter 124. Removed the dead `See MERGE_INSTRUCTIONS_iter101.md` reference at line 185 (file deleted in TD-11). Added a note in the iter 101 entry that the merge instructions file is now in git history only.

Stage Summary:
- **iter 124 SHIPPED — TD-10 + TD-11 closed.** Lint warnings: 114 → 111 (0 errors). `tsc` green. 622 jest green. Repo is 26 files lighter (including a 692KB stale runtime log).
- **Modified files (3):** `src/components/dashboard/dashboard-page.tsx` (TD-10 lint fixes — `TAB_MAP` moved to module level, `setTab` wrapped in `useCallback`, unused import + destructuring removed), `STATUS.md`, `worklog.md` (this entry). `AGENT_NAVIGATION.md` (header bump + dead reference cleanup).
- **Deleted files (26):** `MERGE_INSTRUCTIONS_iter{101,102,103,105,106,107,108,109}.md` ×8, `git_commands_iter{101,102,103,105,106,107,108,109,110,111,117,118}.txt` ×12, `DELETIONS.sh`, `DELETIONS.txt`, `DELETE_obsolete_files.sh`, `README.txt`, `scripts_flipper-backend-bridge.ts.DELETED`, `scripts/DELETE_flipper-backend-bridge.ts`, `flipper-bridge.log`.
- **Stopping point:** iter 124 = TD-10 + TD-11 SHIPPED (114 → 111 lint, 26 obsolete files gone). Next iter candidates in approximate priority order: (a) TD-3/4/5/9 persistence gaps (need design — highest value but highest risk); (b) P10 Gold Map ROI feature work (needs UX/design); (c) AGENT_NAVIGATION.md deep historical trim (low-risk doc cleanup).
