# Worklog — PoE2 Market Dashboard

> Append-only shared multi-agent work log. Each section starts with `---`.
> Old task history is in `git log`. Trimmed to last 2 iterations.

---

Task ID: iter-110
Agent: main
Task: iter 110 — P9 Phase-aware Investment Advisor: live-price binding for phase hints. Extend `phase_hints.py` with optional `snapshot` param + enrichment logic (current_price / change_pct_week / change_pct_month / momentum / phase-aware recommendation). 3 hints tracked (exalted/divine).

Work Log:
- Cloned repo. Read STATUS.md (KI-19 open, KI-13 closed, TD-3/4/5/9 backlog), worklog.md (iter 108 + iter 109 entries), docs/MARKET_PLAYBOOK.md §C.7 (P9 plan: "Расширить phase_hints.py: динамические подсказки на основе текущего дня лиги + live цен").
- **Studied existing code.** Read `phase_hints.py` (479 lines — pure function `get_phase_hints(phase, days_since_reference, ...)` with hardcoded `_PHASE_HINTS` table, 4 hints × 3 phases = 12 hints, parallel `_PHASE_HINTS_RU` Russian table, `_PHASE_META` for phase labels). Read `routes_phase_hints.py` (thin wrapper — PhaseDetector only, NO snapshot dependency). Read `phase-hints-widget.tsx` (336 lines — `HintRow` renders title/detail/action). Read `mirror_divine_arb.py` (iter 108 — fresh pattern for snapshot price-history extraction with `_find_nearest_price` 24h tolerance). Read `data_snapshot.py` (`get_price_history(api_id)` + `get_current_price(api_id)` methods). Read `test_phase_hints.py` (427 lines, 61 tests — REQUIRED_HINT_KEYS uses issubset, so adding new keys is safe).
- **Found KI-20 bug.** While designing field names, tested `case-transform.ts` regex `/_([a-z])/g` empirically: `delta_7d_pct` → `delta_7dPct` (NOT `delta7dPct`), `rolling_7d` → `rolling_7d` (unchanged). The regex skips `_<digit>` patterns — latent bug in content-pulse widget (accesses `delta7dPct` but proxy delivers `delta_7dPct`). Documented as KI-20 in STATUS.md (open, fix deferred — risky). Iter 110 new code AVOIDS the bug by using clean field names: `change_pct_week` / `change_pct_month` (NOT `change_pct_7d` / `change_pct_30d`).
- **Design decision.** Additive, backward-compatible extension: `get_phase_hints()` gains optional `snapshot: DataSnapshot | None = None` param. When None → static-only hints (backward-compat, all 61 existing tests stay green). When provided → each hint with non-empty `tracked_currency` is enriched with 5 live-price fields.
- **Tracked currencies.** Only 3 of 12 hints declare `tracked_currency` — major reference currencies we are confident are in the snapshot: `early-quick-flips` → "exalted", `mid-triangular-arb` → "divine", `late-portfolio-hold` → "divine". Hints about uniques (Temporalis) or category-items (vault keys, breach catalysts) left untracked — their api_ids are not reliably in the currency snapshot.
- **Recommendation matrix (phase × momentum):** EARLY+UP→HOLD, EARLY+DOWN→BUY_OPPORTUNITY, EARLY+FLAT→WATCH; MID+UP→HOLD, MID+DOWN→WATCH, MID+FLAT→NEUTRAL; LATE+UP→SELL_INTO_STRENGTH, LATE+DOWN→SELL_NOW, LATE+FLAT→NEUTRAL. Rationale: EARLY dip = buy opportunity, LATE spike = sell into strength.
- **Files modified (backend):**
  - `backend/economy/phase_hints.py` — added `tracked_currency` field to all 12 hints in BOTH `_PHASE_HINTS` (EN) and `_PHASE_HINTS_RU` (RU) tables. Added 6 tunable constants (MOMENTUM_UP/DOWN_THRESHOLD_PCT, CHANGE_WEEK/MONTH_DAYS, NEAREST_PRICE_TOLERANCE_HOURS). Added 6 recommendation enum constants. Added 5 helper functions: `_find_price_near()` / `_compute_change_pct()` / `_momentum_from_change()` / `_recommendation_from_phase_momentum()` / `_enrich_hint_with_live_price()`. Extended `get_phase_hints()` with optional `snapshot` param + deep-copy of hints (prevents mutation of static table). Added `list_tracked_hints()` helper for tests. Updated module docstring with iter-110 design notes.
  - `backend/api/routes_phase_hints.py` — added best-effort snapshot fetch (get_snapshot_manager → last_snapshot check → await get_snapshot()). Wrapped in try/except — snapshot failure never breaks hints (graceful fallback to static-only). Preserves "immune to KI-11" property.
  - `backend/api/response_models.py` — extended `PhaseHintData` pydantic model with 6 optional fields (tracked_currency, current_price, change_pct_week, change_pct_month, momentum, recommendation). All default=None.
- **Files modified (frontend):**
  - `src/lib/types.ts` — extended `PhaseHint` interface with 6 new fields (trackedCurrency: string, currentPrice/changePctWeek/changePctMonth: number | null, momentum: "UP"|"DOWN"|"FLAT"|null, recommendation: union type | null).
  - `src/components/dashboard/phase-hints-widget.tsx` — added TrendingUp/TrendingDown/Minus icon imports. Added 6 helper functions: `momentumBadgeClass()` / `momentumLabelKey()` / `recommendationBadgeClass()` / `recommendationLabelKey()` / `fmtSignedPct()` / `fmtPrice()`. Extended `HintRow` with live-price section (renders when trackedCurrency + currentPrice available): current price + 7d change + 30d change + momentum badge + recommendation badge. Color coding: emerald for UP/BUY, red for DOWN/SELL, amber for WATCH, violet for HOLD, muted for NEUTRAL/FLAT.
  - `src/lib/i18n/locales/{en,ru,ko,zh}.ts` — added 12 new keys × 4 locales: phaseHintsLivePriceLabel, phaseHintsChangeWeekLabel, phaseHintsChangeMonthLabel, phaseHintsMomentumUp/Down/Flat, phaseHintsRecBuyOpportunity/Hold/Watch/SellIntoStrength/SellNow/Neutral. Parity: 1191 keys × 4 locales (verified programmatically).
  - `src/__tests__/phase-hints-widget.test.tsx` — updated `makeHint()` helper to include new null/empty default fields. Added 13 new jest tests in `describe("iter 110 — live-price section")` block: tracked renders live-price section, untracked does NOT, current price renders, 7d change with + sign, 30d change, momentum badge UP, recommendation badge HOLD, negative 7d change with - sign + red color, BUY_OPPORTUNITY for EARLY+DOWN, SELL_INTO_STRENGTH for LATE+UP, FLAT momentum, no live-price section when currentPrice is null.
- **Files modified (tests):**
  - `tests/test_phase_hints.py` — added AsyncMock import. Added 7 new test classes (48 tests total): TestListTrackedHints × 5, TestFindPriceNear × 6, TestComputeChangePct × 7, TestMomentumFromChange × 6, TestRecommendationFromPhaseMomentum × 10 (9 parametrized matrix + 1 None), TestGetPhaseHintsWithSnapshot × 10 (no-snapshot backward-compat, enrichment with data, falling price in EARLY, rising price in LATE, untracked hint gets None, snapshot without data for tracked currency, short history no weekly change, Russian table with snapshot, flat momentum in MID, does NOT mutate static table), TestRouteHandlerWithSnapshot × 4 (enriches when snapshot available, falls back when unavailable, falls back when snapshot raises, pydantic validation with enrichment).
- **Files modified (docs):** STATUS.md (KI-20 documented, header updated), docs/MARKET_PLAYBOOK.md (header, §B P9 row, §C.7 → DONE with full implementation details, §D.3 stopping point, §E references, summary counts), AGENT_NAVIGATION.md (header, phase_hints.py row, routes_phase_hints.py row), worklog.md.
- **Verification:** (1) Ran `/home/z/my-project/scripts/check_iter110_i18n.py` — PASS: 12 new keys × 4 locales all present, 1191 keys × 4 parity. (2) Ran `python3 -m pytest tests/test_phase_hints.py -q` → 109 passed in 4.31s (61 original + 48 new). (3) Ran `python3 -m pytest -q --ignore=tests/test_scheduler.py` → 1266 passed in 13.27s (no regression — was 1218 in iter 109, +48 new). (4) Ran `/home/z/my-project/scripts/check_iter110_syntax.py` — bracket balance OK for all files except ko.ts/zh.ts (pre-existing CJK false-positive in verification script, confirmed by git stash — NOT a real syntax error). (5) tsc/jest NOT run — Known Issue with OOM-killer at `npm install` (4GB RAM, no swap, requires 8GB+ since iter 99).
- **Docs updated:** STATUS.md, worklog.md, docs/MARKET_PLAYBOOK.md, AGENT_NAVIGATION.md.

Stage Summary:
- **iter 110 SHIPPED — P9 Phase-aware Investment Advisor DONE.** Live-price binding for phase hints: 3 of 12 hints tracked (exalted/divine) with current_price / change_pct_week / change_pct_month / momentum / phase-aware recommendation.
- Modified files (10): `backend/economy/phase_hints.py` (core enrichment logic + helpers), `backend/api/routes_phase_hints.py` (snapshot fetch), `backend/api/response_models.py` (pydantic model), `src/lib/types.ts` (TS types), `src/components/dashboard/phase-hints-widget.tsx` (UI live-price section), `src/lib/i18n/locales/{en,ru,ko,zh}.ts` (12 keys × 4 = 48 new strings), `tests/test_phase_hints.py` (48 new pytest), `src/__tests__/phase-hints-widget.test.tsx` (13 new jest), `STATUS.md`, `docs/MARKET_PLAYBOOK.md`, `AGENT_NAVIGATION.md`, `worklog.md`.
- Verified: 1266 pytest green (48 new + 1218 regression, no regression). i18n parity verified programmatically (1191 keys × 4 locales). KI-20 documented (case-transform `_<digit>` bug, fix deferred).
- **Key design decision for future agents:** `get_phase_hints()` now takes optional `snapshot` param — when None, behavior is unchanged (backward-compat). Enrichment is ADDITIVE: tracked hints get 5 new fields, untracked hints get those fields set to None. Route handler does best-effort snapshot fetch with graceful fallback (preserves "immune to KI-11"). Field names use `_week`/`_month` (NOT `_7d`/`_30d`) to avoid KI-20 case-transform bug.
- **Stopping point:** iter 110 = P9 live-price binding DONE. Next iter (iter 111) candidates: (a) TD-3/4/5/9 persistence gaps (require careful persistence-layer design); (b) P10 Gold Map ROI (§C.8) — calculator depending on P1 (3-way flips, ready); (c) KI-20 fix — `case-transform.ts` regex `/_([a-z])/g` → `/_([a-z0-9])/g` (risky, needs full jest + UI regression); (d) tsc/jest regression — requires 8GB+ RAM environment (Known Issue since iter 99); (e) extend tracked coverage (vault keys, breach catalysts, uniques) — needs api_id verification in snapshot.

---

Task ID: iter-109
Agent: main
Task: iter 109 — P7 Mirror/Divine Arb Detector UI tab (single-object card render) + wiring + i18n × 4 locales. Backend was iter 108.

Work Log:
- Cloned repo. Read STATUS.md (KI-19 open, KI-13 verified closed iter 108, TD-3/4/5/9 backlog), worklog.md (iter 107 + iter 108 entries), MERGE_INSTRUCTIONS_iter108.md (P7 design note for iter 109: "P7 returns a SINGLE-OBJECT response, not a per-currency list. Mirror:Divine is ONE market. The UI tab should render ONE record (current rate / z-score / signal / action / sparkline), NOT a table of rows. Follow the speculation-tab.tsx pattern but with single-object shape").
- **Studied existing patterns.** Read `circuit-patterns-tab.tsx` (668 lines — the cleanest per-currency list pattern to mirror), `dashboard-page.tsx` (TAB_MAP at line 575, lazy-load blocks at lines 75-112, TabsContent blocks at lines 935-1013), `dashboard-toolbar.tsx` (TabsTrigger list, lucide-react icon imports), `shortcuts-dialog.tsx` (10-key shortcut map + tab-mapping reference), `src/lib/i18n/locales/en.ts` (1343 lines, ends with `} as const;` + `export type TranslationKeys`), `src/lib/i18n/locales/{ru,ko,zh}.ts` (all `Record<TranslationKeys, string>` typed, end with `export default <name>;`).
- **Verified P7 backend shapes.** `MirrorDivineArbResponse` in `src/lib/types.ts` (camelCase): `currentRate` / `meanRate` / `stdRate` / `minRate` / `maxRate` / `zScore` / `deviationPct` / `profitPotentialPerMirrorDiv` / `signal` / `isActionable` / `recommendedAction` / `sampleSize` / `priceHistoryShort: { date, rate }[]` / `dataAvailable` / `fetchedAt` / `days` / `mirrorCurrency` / `divineCurrency` / `league`. Backend pydantic model uses snake_case (`current_rate`, etc.) — `flipper-proxy.ts` `transformKeys()` handles snake→camel. Proxy at `src/app/api/flipper/mirror-divine-arb/route.ts` returns empty fallback with `dataAvailable: false` when backend offline.
- **UI component design decision.** Per the iter 108 design note, P7 UI is a SINGLE-OBJECT CARD, not a per-currency list. Layout (top→bottom): (1) header row (icon + title + subtitle + days selector + refresh); (2) signal+action badges (2-column grid); (3) hero metrics block (3-column grid: current rate / z-score / deviation %); (4) profit + actionable row; (5) 5-cell stats grid (mean/std/min/max/sample size); (6) rate sparkline (360×48 SVG, colored by signal); (7) footer (fetched-at + sample size + window). Days selector 7/14/30/90 (default 30). 5 graceful-degradation states: backendOffline / loading / error / no-data / insufficient-sample.
- **TAB_MAP decision.** Inserted `mirror-divine-arb` at index 12 (after `weekly-patterns` at 11, before `liquid-chain` at 13). Extends the analytics-cluster (storage-value → speculation → circuits → intraday → weekly → mirror-divine-arb). Tab count grew from 14 to 15. No keyboard-shortcut slot consumed — already capped at 10 (1-9 + 0), so this tab is click-only (consistent with intraday/weekly/liquid-chain/watchlist).
- **Icon choice.** Picked `ArrowUpDown` from lucide-react for the new tab — semantically clear (rate up/down comparison), distinct from `ArrowLeftRight` (Exchange tab) and `Activity` (Circuit Patterns tab).
- **Files created:**
  - `src/components/dashboard/mirror-divine-arb-tab.tsx` (~525 lines) — single-object card render with all 5 graceful-degradation branches + main render. Reuses `fetchApi` / `fmt` / `MirrorDivineArbResponse` types from `src/lib/types.ts`. Uses `useQuery` (30s staleTime, retry 1) bound to `/api/flipper/mirror-divine-arb?days={days}`. Local `Sparkline` component (dependency-free SVG, 360×48) mirrors `circuit-patterns-tab.tsx` Sparkline but uses `rate` field instead of `price`. `signalBadgeClass` / `actionBadgeClass` / `signalIcon` / `actionIcon` / `signalLabelKey` / `actionLabelKey` helpers color-code badges (red for SELL_MIRROR, emerald for SELL_DIVINE, fuchsia for EXECUTE_ARB, amber for WATCH, muted for NEUTRAL/HOLD).
- **Files modified:**
  - `src/components/dashboard/dashboard-page.tsx` — added `MirrorDivineArbTab` lazy-load block (after `WeeklyPatternsTab`), added `"mirror-divine-arb"` to `TAB_MAP` (idx 12), added `<TabsContent value="mirror-divine-arb">` block (after weekly-patterns, before liquid-chain) with `<ErrorBoundary fallbackTitle={t("fallbackMirrorDivineArb")}>`.
  - `src/components/dashboard/dashboard-toolbar.tsx` — added `ArrowUpDown` to lucide-react imports, added `<TabsTrigger value="mirror-divine-arb">` (after weekly-patterns, before liquid-chain).
  - `src/components/dashboard/shortcuts-dialog.tsx` — updated iter-99 comment to mention iter 109 + mirror-divine-arb (click-only, idx 12). No new shortcut mapping (10 slots already full).
  - `src/lib/i18n/locales/en.ts` — added 42 new keys (mirrorDivine* + tabMirrorDivineArb + fallbackMirrorDivineArb). en ends with `} as const;` + `export type TranslationKeys = keyof typeof en;` — verified preserved.
  - `src/lib/i18n/locales/ru.ts` — added same 42 keys, RU translations. Ends with `export default ru;`.
  - `src/lib/i18n/locales/ko.ts` — added same 42 keys, KO translations. Ends with `export default ko;`.
  - `src/lib/i18n/locales/zh.ts` — added same 42 keys, ZH translations. Ends with `export default zh;`.
  - `STATUS.md` — updated header (iter 109), added Quick-Reference row for "Mirror/Divine Arb tab shows 'no price history yet'" (by-design behavior).
  - `docs/MARKET_PLAYBOOK.md` — updated header (iter 109), §B P7 row (now end-to-end done), summary counts (P7 moved from "P7-backend" to "P7"), §C.6 (renamed to "iter 108 + iter 109 — End-to-end DONE", added UI description), §D.2 row 5 (✅ Done), §D.3 (replaced iter 108 stop-point with iter 109), §E (iter 109 reference).
  - `AGENT_NAVIGATION.md` — updated header (iter 109), added new row in module table for `mirror-divine-arb-tab.tsx` (after `weekly-patterns-tab.tsx`), updated `weekly-patterns-tab.tsx` row to reference idx 11 → mirror-divine-arb at 12 (and Calendar icon between Clock and ArrowUpDown).
- **Verification:** (1) Ran `/home/z/my-project/scripts/check_iter109.py` — PASS: 42 i18n keys × 4 locales all present (en/ru/ko/zh each have 1185 keys total — parity), all 4 modified TS/TSX files have balanced brackets (after stripping strings/comments), TAB_MAP / lazy-import / TabsContent / TabsTrigger / shortcuts-comment all wired correctly, all 4 locale files end correctly. (2) Ran `python3 -m pytest tests/test_mirror_divine_arb.py -q` → 70 passed in 5.70s. (3) Ran `python3 -m pytest -q --ignore=tests/test_scheduler.py` → 1218 passed in 12.52s (no regression). (4) tsc/jest regression NOT run — Known Issue with OOM-killer at `npm install` (4GB RAM, no swap, requires 8GB+ since iter 99).
- **Docs updated:** STATUS.md, worklog.md, docs/MARKET_PLAYBOOK.md, AGENT_NAVIGATION.md.

Stage Summary:
- **iter 109 SHIPPED — P7 Mirror/Divine Arb Detector UI tab DONE end-to-end.** Backend (iter 108) + UI (iter 109) + i18n × 4 locales all complete.
- New files (1): `src/components/dashboard/mirror-divine-arb-tab.tsx` (~525 lines, single-object card render with 5 graceful-degradation branches).
- Modified files (8): `dashboard-page.tsx` (TAB_MAP + lazy import + TabsContent), `dashboard-toolbar.tsx` (new TabsTrigger + ArrowUpDown icon), `shortcuts-dialog.tsx` (comment update), `src/lib/i18n/locales/{en,ru,ko,zh}.ts` (42 keys × 4 locales = 168 new strings), `STATUS.md`, `docs/MARKET_PLAYBOOK.md`, `AGENT_NAVIGATION.md`, `worklog.md`.
- Verified: 1218 pytest green (no regression — backend untouched). 42 i18n keys × 4 locales parity verified programmatically. Bracket balance verified in all 4 modified TS/TSX files. TAB_MAP / TabsContent / TabsTrigger / shortcuts-dialog wiring all verified.
- **Key design decision for future agents:** P7 UI is a SINGLE-OBJECT CARD, NOT a per-currency list. Mirror:Divine is ONE market. The card has 7 sections (header / signal+action badges / hero metrics / profit+actionable / stats grid / sparkline / footer) and 5 graceful-degradation branches (offline / loading / error / no-data / insufficient-sample). Days selector 7/14/30/90 (default 30). Tab is at TAB_MAP idx 12 (click-only — no keyboard shortcut, outside the 1-9+0 range). Icon: ArrowUpDown.
- **Stopping point:** iter 109 = P7 end-to-end DONE. Next iter (iter 110) candidates: (a) TD-3/4/5/9 persistence gaps (require careful persistence-layer design — deferred since iter 108); (b) P9 Phase-aware investment advisor (§C.7) — extend `phase_hints.py` with live-price binding; (c) P10 Gold Map ROI (§C.8) — calculator depending on P1 (3-way flips); (d) tsc/jest regression — requires 8GB+ RAM environment (Known Issue since iter 99).

