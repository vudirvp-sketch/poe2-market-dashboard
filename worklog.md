# Worklog — PoE2 Market Dashboard

> Append-only shared multi-agent work log. Each section starts with `---`.
> Old task history is in `git log`. Trimmed to last 2 iterations.

---

Task ID: iter-116
Agent: main
Task: iter 116 — close KI-20 + KI-23, advance KI-24 by 2 sites. KI-20: `case-transform.ts` regex `/_([a-z])/g` → `/_([a-z0-9])/g` so `delta_7d_pct` correctly becomes `delta7dPct` (was `delta_7dPct` with leftover `_`); added 29 jest tests. KI-23: extracted `<CategoryGroupTable>` child from `unique-table.tsx` so `useReactTable` is called at top level (was inside `.map()` callback — latent rules-of-hooks violation). KI-24: refactored `use-reduced-motion.ts` to `useSyncExternalStore` (1 `set-state-in-effect` site) + suppressed `speculation-tab.tsx:316` `useMemo` warning with documented rationale (1 `preserve-manual-memoization` site). Lint 125 → 123, 0 errors. 618 jest green (was 582), tsc green. KI-24 backlog now 8 sites across 1 React Compiler rule.

Work Log:
- Cloned repo. Read STATUS.md (KI-23 open, KI-24 open with 10 sites / 2 rules after iter 115, KI-20 open), worklog.md (iter 114 + iter 115), package.json, `case-transform.ts`, `use-reduced-motion.ts`, `speculation-tab.tsx` (lines 280-345), `unique-table.tsx` (full 464-line file), `eslint.config.mjs`, `next.config.ts`.
- **Selected iter 116 scope = 4 small fixes (KI-20 + KI-23 + 2 KI-24 sites).** Per "Лучше недоделать, чем сломать": chose 4 contained, mechanical fixes with low regression risk. Deferred TD-3/4/5/9 persistence gaps (need persistence-layer design) and P10 Gold Map ROI (feature work, needs product design) to future iters. Other 8 KI-24 `set-state-in-effect` sites need case-by-case evaluation — deferred to incremental future iters.
- **Confirmed baseline.** `npm install` → 816 packages in 16s. `npx tsc --noEmit` → exit 0. `npx eslint .` → 125 warnings, 0 errors (matches iter-115 baseline). Per-file lint on targets: `case-transform.ts` 0 warnings (regex change is semantics, not lint), `use-reduced-motion.ts` 1 `set-state-in-effect` warning (line 15), `unique-table.tsx` 5 warnings (1 was the suppressed `rules-of-hooks` at line 305 + 1 `incompatible-library` at line 306), `speculation-tab.tsx` 1 `preserve-manual-memoization` warning at line 316. `npx jest --maxWorkers=1` → 582 passed, 25 suites, exit 0.
- **Confirmed React Compiler status.** Searched `next.config.ts` for `reactCompiler` — NOT enabled. `babel-plugin-react-compiler` appears only as a transitive dep in `package-lock.json`. This means removing `useMemo` from `speculation-tab.tsx:316` would be a PERFORMANCE regression (Map rebuilt every render) — critical context for the KI-24 `preserve-manual-memoization` evaluation.
- **KI-20 fix:** Changed `/_([a-z])/g` → `/_([a-z0-9])/g` in `src/lib/case-transform.ts:23`. Added KI-20 note to the file's JSDoc. Created `src/__tests__/case-transform.test.ts` with 29 tests covering: baseline idempotency (already-camelCase), simple snake_case, multi-segment, `_<uppercase>` not matched (by design), KI-20 regression (`delta_7d_pct` → `delta7dPct`, `rolling_7d` → `rolling7d`, `volume_24h` → `volume24h`), pure-digit segments, mixed digit+letter, consecutive underscores, trailing underscore, empty string, leading-underscore-digit, transformKeys recursion (nested objects, arrays, mixed-case), value preservation. Two initial test expectations were wrong (assumed `_<uppercase>` was matched; assumed leading-`_1foo` was preserved) — fixed after first run with explanatory comments.
- **KI-24 `use-reduced-motion.ts` fix:** Rewrote `src/hooks/use-reduced-motion.ts` to use `useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot)`. Module-level constants `REDUCED_MOTION_QUERY`, three pure functions (`subscribe` guarded for SSR with `typeof window === "undefined"` check; `getSnapshot` reads `window.matchMedia(...).matches` synchronously; `getServerSnapshot` returns `false`). Hook body is a single `return useSyncExternalStore(...)`. Eliminates the `set-state-in-effect` warning entirely (no `useEffect` + `setState` pattern). `"use client"` directive preserved — Next.js still needs the boundary even though `useSyncExternalStore` itself is SSR-safe. Created `src/__tests__/use-reduced-motion.test.ts` with 7 tests: returns false/true based on matchMedia, updates on change (both directions), unsubscribes on unmount (no listener leak), multiple consumers share subscription, no setState-during-render warning. Initial test used `new MediaQueryListEvent(...)` which jsdom doesn't implement — fixed by synthesizing a minimal `{media, matches}` object cast to `MediaQueryListEvent`.
- **KI-23 fix:** Extracted `<CategoryGroupTable>` child component from `unique-table.tsx`. The original code called `useReactTable({...})` INSIDE `categoryGroups.map((group) => {...})` (line 306) — a `react-hooks/rules-of-hooks` violation suppressed by an inline `eslint-disable` comment. New structure: `UniqueTable` renders `{categoryGroups.map(group => <CategoryGroupTable key={group.name} ... />))}`. The `CategoryGroupTable` child component (defined after `UniqueTable`, following the existing `SortHeader` convention) calls `useReactTable` at its top level, receiving 13 props: `group`, `columns`, `sorting`, `setSorting`, `isCollapsed`, `onToggleCollapse`, `t`, `highlightedItemId`, `onItemClick`, `onRowMouseEnter`, `rowHeight`, `fontSize`, `cellPadding`. The parent computes `isCollapsed={collapsedCategories.has(group.name)}` and binds `onToggleCollapse={() => toggleCategoryCollapse(group.name)}` so the child stays generic. Render output is byte-identical to the pre-refactor inline `.map()` body — only the structure changed. Removed the inline `eslint-disable react-hooks/rules-of-hooks` comment. Imported `TranslationKeys` type from `@/lib/i18n` for the `t` prop signature.
- **KI-23 verification:** `npx tsc --noEmit` exit 0 (prop types match — `SortingState`, `ColumnDef<PoeItem>[]`, `React.Dispatch<React.SetStateAction<SortingState>>` all check out). `npx eslint src/components/dashboard/unique-table.tsx` → 5 warnings (was 5), but the `react-hooks/rules-of-hooks` violation is GONE (the eslint-disable comment is no longer needed). The `react-hooks/incompatible-library` warning still fires on the new `useReactTable` call at line 376 (was at line 306) — this is a separate React Compiler concern about TanStack Table's API, NOT a hooks violation, and is documented in STATUS.md.
- **KI-24 `speculation-tab.tsx:316` evaluation:** Analyzed the `flipsByApiId` `useMemo` at line 332 (post-refactor line; was line 316 in iter-115 STATUS.md — the STATUS.md line reference was slightly off because the iter-115 eslint-disable comment shifted line numbers). The `useMemo` builds a `Map<string, FlipOpportunity>` keyed by `fromId` from `flipsData?.opportunities`. Deps array is `[flipsData?.opportunities]` (narrow) — leverages TanStack Query's structural sharing so the Map only rebuilds when the `opportunities` array ref actually changes. The React Compiler infers `flipsData` (broader) as the dep, causing the `preserve-manual-memoization` warning. Evaluation findings: (a) React Compiler NOT enabled in `next.config.ts` → removing `useMemo` would rebuild Map every render (performance regression); (b) `flipsByApiId` is only consumed during render at line ~508 (`flip={flipsByApiId.get(sig.apiId)}`) — no downstream `useEffect`/`useMemo` deps read its reference, so removal would be correctness-safe but slow. Decision: KEEP the `useMemo`, suppress with inline `eslint-disable-next-line react-hooks/preserve-manual-memoization` + a 15-line comment block explaining the rationale and when to revisit (after enabling `reactCompiler: true` in `next.config.ts`).
- **Final verification:**
  - `npx tsc --noEmit` → **exit 0** ✅
  - `npx eslint .` → **0 errors, 123 warnings, exit 0** ✅ (was 125 → 123 = 2 warnings removed: 1 `set-state-in-effect` in `use-reduced-motion.ts`, 1 `preserve-manual-memoization` in `speculation-tab.tsx`).
  - React Compiler rule warnings: **8** (was 10) ✅ — `set-state-in-effect` 9 → 8, `preserve-manual-memoization` 1 → 0.
  - `npx jest --maxWorkers=1` → **618 passed, 0 failed, 27 suites, exit 0** ✅ (was 582 + 29 case-transform + 7 use-reduced-motion = 618; 25 + 2 = 27 suites).
  - `pytest` not run (no backend changes) — 1279 passed expected per iter 113.
  - `next build` not run (4GB RAM env constraint, see Quick Reference OOM note) — iter-115 baseline was green; the changes are (a) 1-char regex change in a pure function with full test coverage, (b) hook rewrite using a canonical React primitive (`useSyncExternalStore`) with full test coverage, (c) pure structural refactor (extract child component) with `tsc` + `eslint` + `jest` green, (d) comment-only addition to `speculation-tab.tsx`. None of these are plausible build-regression failure modes.
- **Documentation updates:**
  - `STATUS.md`: bumped "Last updated" header (iter 116). Moved KI-23 from "Known Issues — open" to "Known Issues — closed (recent)" with closure note. Moved KI-20 from "Known Issues — open" to "Known Issues — closed (recent)" with closure note. Updated KI-24 section — table now shows 1 rule / 8 sites (was 2 rules / 10 sites), `use-reduced-motion.ts` and `speculation-tab.tsx` removed from sites list. Added 3 new bullets to the "Closed sub-rules (history)" list documenting iter-116 fixes. Updated backlog row: "8 React Compiler rule sites remaining (was 25 — `static-components` fully resolved iter 113, `refs` fully resolved iter 114, `set-state-in-effect` 2 of 10 resolved iter 115+116, `preserve-manual-memoization` 1 of 1 suppressed with rationale iter 116)". Added 2 new "Key technical insights" paragraphs: "`react-hooks/set-state-in-effect` fix via `useSyncExternalStore` (iter 116)" and "`react-hooks/preserve-manual-memoization` evaluation recipe (iter 116)" and "`react-hooks/rules-of-hooks` extraction recipe (iter 116, KI-23)". Trimmed old closed-issue entries (kept iter 102-111, removed nothing — the closed list was already lean). Added P10 to the technical-debt backlog table.
  - `worklog.md` (this entry) — trimmed iter 114 (oldest, in git log), now shows iter 115 + iter 116 (last 2 iterations per header convention).
  - `AGENT_NAVIGATION.md` — header bump only.

Stage Summary:
- **iter 116 SHIPPED — 4 fixes: KI-20 closed, KI-23 closed, KI-24 advanced by 2 sites.** Lint warnings: 125 → 123 (0 errors). `tsc` green. 618 jest green (was 582). 1279 pytest expected green (no backend changes).
- **Modified files (7):** `src/lib/case-transform.ts`, `src/hooks/use-reduced-motion.ts`, `src/components/dashboard/unique-table.tsx`, `src/components/dashboard/speculation-tab.tsx`. Plus 2 new test files: `src/__tests__/case-transform.test.ts`, `src/__tests__/use-reduced-motion.test.ts`. Plus docs: `STATUS.md`, `worklog.md` (this entry), `AGENT_NAVIGATION.md` (header bump).
- **What was NOT done (intentionally deferred):**
  - KI-24 remaining 8 `set-state-in-effect` sites: `dashboard-page.tsx` (3), `fuzzy-search.tsx` (1), `header.tsx` (1), `offline-banner.tsx` (1), `use-realms-and-leagues.ts` (1), `i18n/index.tsx` (1). Each needs case-by-case evaluation — `use-realms-and-leagues.ts` and `i18n/index.tsx` are the next-smallest candidates (both 1 site, both are "hydrate from localStorage" patterns).
  - TD-3/4/5/9 persistence gaps (need persistence-layer design).
  - P10 Gold Map ROI (§C.8) — feature work, depends on P1 3-way flips (already done).
- **Stopping point:** iter 116 = KI-20 closed + KI-23 closed + KI-24 advanced by 2 sites (10 → 8). KI-24 backlog now 8 sites / 1 rule. Next iter (iter 117) candidates: (a) KI-24 `set-state-in-effect` incremental — `use-realms-and-leagues.ts` (1 site, hook, similar to use-reduced-motion — may also be a `useSyncExternalStore` candidate or a `useCallback`+signal-ref candidate); (b) KI-24 `set-state-in-effect` incremental — `i18n/index.tsx` (1 site, "hydrate from localStorage" pattern, may move to `useSyncExternalStore`); (c) KI-24 `set-state-in-effect` incremental — `header.tsx` or `offline-banner.tsx` (1 site each); (d) KI-24 `set-state-in-effect` — `dashboard-page.tsx` (3 sites, larger refactor, needs careful analysis); (e) KI-24 `set-state-in-effect` — `fuzzy-search.tsx` (1 site); (f) TD-3/4/5/9 persistence gaps; (g) P10 Gold Map ROI (§C.8).

---

Task ID: iter-115
Agent: main
Task: iter 115 — incremental KI-24 fix: eliminate the 1 `react-hooks/set-state-in-effect` warning in `use-price-stream.ts` (the `backendOnline` transitions effect). Strategy: (a) derive `status`/`lastError` from `backendOnline` in the return statement; (b) move `setReconnectCount(0)` + `setLastError(null)` out of the effect into `connect()` via a `freshSessionRef` latest-ref pattern. Lint 126 → 125, 0 errors. No backend/test changes.

Work Log:
- Cloned repo. Read STATUS.md (KI-23 open, KI-24 open with 11 sites / 2 rules after iter 114, KI-20 open), worklog.md (iter 113 + iter 114), package.json, `use-price-stream.ts` (full 399-line file).
- **Selected iter 115 scope = KI-24 `set-state-in-effect` — `use-price-stream.ts`.** Per "Лучше недоделать, чем сломать": chose the smallest-scope candidate from the iter-114 stopping-point list. Same file already in context from iter 114. Single warning site. Other candidates (KI-23 ~120-line refactor, KI-20 regex change with full regression, KI-24 `preserve-manual-memoization` in a different file, TD-3/4/5/9 persistence, P10 Gold Map ROI feature) all carry higher risk or need their own dedicated iter.
- **Confirmed baseline.** `npm install` → packages installed silently. `npx eslint .` → 126 warnings, 0 errors (matches iter-114 baseline). Per-file lint on the target: `use-price-stream.ts` had 1 warning — `react-hooks/set-state-in-effect` at line 386 (`setStatus("disconnected")` inside the `backendOnline` transitions effect). `npx tsc --noEmit` → exit 0. `npx jest --maxWorkers=1` → 582 passed, 25 suites, exit 0.
- **Read the full target file & analyzed the transitions effect.** The `backendOnline` transitions effect (lines 375-395 pre-fix) had 3 setState calls:
  - Offline branch: `setStatus("disconnected")` (line 386, flagged) + `setLastError(null)` (line 387) — clear stale status/error so UI shows "disconnected" / no-error while backend is offline.
  - Online branch: `setReconnectCount(0)` (line 390) — reset the reconnect counter for the new session, so the user doesn't see stale counts from the previous session.
  - The rule fires on the FIRST setState in an effect (line 386). Removing only that line would cause the rule to fire on line 387, then line 390. To fully eliminate the warning, ALL setState calls must be removed from the effect.
- **Safety analysis (semantics preservation):**
  - `status` and `lastError` are FULLY DETERMINED by `backendOnline` when offline — when `backendOnline === false`, the UI must show "disconnected" with no error regardless of internal state. This is a pure derivation: `backendOnline === false ? "disconnected" : status`. The internal state is no longer mutated by the effect, but the rendered output is identical. This is the React-recommended pattern (https://react.dev/learn/you-might-not-need-an-effect).
  - `setReconnectCount(0)` (online branch) is NOT fully derivable — `reconnectCount` is genuine state that accumulates over time (incremented in `es.onerror`'s reconnect callback). It needs to be RESET once per backend-online transition. Strategy: move the reset into `connect()` (a `useCallback`, not an effect — the rule doesn't trace setState through useCallback boundaries, confirmed by the existing `setStatus("connecting")` at line 226 not firing the rule) via a `freshSessionRef` "signal ref" pattern.
- **Applied fix:**
  1. Added `const freshSessionRef = useRef(false);` near the other refs (after `thresholdRef`), with explanatory comment block citing the rule, the rationale, and the pattern.
  2. At the top of `connect()` (before any early-return guards), added a `freshSessionRef` consumption block: `if (freshSessionRef.current) { setReconnectCount(0); setLastError(null); reconnectCountRef.current = 0; freshSessionRef.current = false; }`. Placed BEFORE guards so the reset fires even if `connect` is skipped this tick.
  3. In the `backendOnline` transitions effect: offline branch reduced to `cleanup()` only (removed `setStatus("disconnected")` + `setLastError(null)` — now derived in return); online branch reduced to `freshSessionRef.current = true; everConnectedRef.current = false; connectRef.current();` (removed `setReconnectCount(0)` + `reconnectCountRef.current = 0` — now handled by `connect()` consuming `freshSessionRef`).
  4. Replaced `return { status, lastError, reconnectCount };` with derivation: `const effectiveStatus = backendOnline === false ? "disconnected" : status;` + `const effectiveLastError = backendOnline === false ? null : lastError;` + `return { status: effectiveStatus, lastError: effectiveLastError, reconnectCount };`.
- **Verification:**
  - `npx eslint src/hooks/use-price-stream.ts` → **0 warnings, 0 errors** ✅ (was 1 warning).
  - `npx eslint .` → **0 errors, 125 warnings, exit 0** ✅ (was 126 → 125 = 1 warning removed).
  - `npx tsc --noEmit` → **exit 0** ✅.
  - `npx jest --maxWorkers=1` → **582 passed, 0 failed, 25 suites, exit 0** ✅.

Stage Summary:
- **iter 115 SHIPPED — KI-24 `set-state-in-effect` 1 of 10 sites resolved.** Lint warnings: 126 → 125 (0 errors). `tsc` green. 582 jest green. 1279 pytest expected green (no backend changes).
- **Modified files (3):** `src/hooks/use-price-stream.ts`, `STATUS.md`. Plus `worklog.md` (this entry) + `AGENT_NAVIGATION.md` (header bump).
- **Stopping point:** iter 115 = KI-24 `set-state-in-effect` 1 of 10 sites resolved (use-price-stream.ts). KI-24 backlog now 10 sites / 2 rules.
