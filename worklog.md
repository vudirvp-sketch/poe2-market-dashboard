# Worklog — PoE2 Market Dashboard

> Append-only shared multi-agent work log. Each section starts with `---`.
> Old task history is in `git log`. Trimmed to last 2 iterations.

---

Task ID: iter-132
Agent: main
Task: iter 132 — P10 Phase 2 (Gold Map ROI trend chart). Per iter 131's stopping-point brief: P10 Phase 2 is the next priority — now that TD-3 Phase 3 (iter 129) shipped `/api/v1/arbitrage/triangular/history`, the Gold Map ROI tab can render a trend chart of historical cycle profitability. Scope = new `gold-map-roi-trend-chart.tsx` SVG component + new `/api/flipper/triangular/history` proxy route + new TS types + wire trend chart into `gold-map-roi-tab.tsx` below the calculator + 13 new i18n keys × 4 locales + ~10 jest tests. Frontend-only, NO backend changes (reuses iter-129 read-only history route). Design doc: `docs/design/P10-gold-map-roi-design.md` §7 (UI layout) + §12 Phase 2.

Work Log:
- Cloned repo. Read `STATUS.md` (iter 131 SHIPPED — TD-5 Phase 4, 1432 pytest green), `worklog.md` (iter 128 + iter 129 + iter 131), `docs/design/P10-gold-map-roi-design.md` (full — §1 problem statement, §3.3 gold rate source options [B localStorage MVP chosen], §4.3 ROI formula reconciliation, §5.4 TAB_MAP placement at index 13, §7 UI layout with trend chart section [Phase 1 greyed-out placeholder was NOT actually shipped — Phase 1 shipped without it], §11.2 Q1 "ship disabled vs hidden" [Phase 1 actually shipped NEITHER — clean cut], §12 Phase 2 = trend chart consumes `/api/v1/arbitrage/triangular/history`).
- Read source files for context: `src/components/dashboard/gold-map-roi-tab.tsx` (Phase 1 tab — wraps calculator, has 3 early-return branches for offline/error/loading then main render with header + optional "no cycles" notice + GoldMapRoiCalculator + fetched-at footer), `src/components/dashboard/gold-map-roi-calculator.tsx` (Phase 1 calculator — 3 inputs + ROI result card + breakdown, localStorage persistence, `computeRoi` + `pickBestCycleEndingInDiv` pure helpers), `src/components/dashboard/storage-value-history-chart.tsx` (SVG chart template — `viewBox` + `PADDING` + `geometry` useMemo + `buildPath` with M/L commands for gaps + Y-axis ticks + X-axis ticks + legend), `backend/api/routes_arbitrage.py:943-1031` (TD-3 Phase 3 history route — reads SQLite via `HistoricalStore.read_triangular_cycles`, returns `TriangularCyclesHistoryResponse` with `points` + `available_cycle_keys` + `data_available` + `fetched_at`), `backend/api/response_models.py:1112-1145` (`TriangularCyclePoint` pydantic — 8 fields: timestamp, cycle_key, cycle_currencies, raw_profit_pct, executable_estimate, executable_profit, confidence, snapshot_age_sec — all numeric `float | None` / `int | None`; `TriangularCyclesHistoryResponse` — 7 fields: league, cycle_key, days, points, available_cycle_keys, data_available, fetched_at), `src/lib/types.ts:249-282` (existing `TriangularCycle` + `TriangularResponse` types), `src/app/api/flipper/triangular/route.ts` (existing live triangular proxy — uses `proxyWithFallback` with 45s timeout), `src/app/api/flipper/mirror-divine-arb/route.ts` (proxy template with empty fallback), `src/lib/flipper-proxy.ts:425-430` (proxy auto-transforms snake_case → camelCase via `transformKeys`), `src/lib/case-transform.ts:22-36` (`toCamelCase` + `transformKeys` — KI-20 fix matches `_<digit>` as well as `_<letter>`), `src/lib/utils.ts:38-46` (`formatLocaleDate(ts, locale)` — Intl.DateTimeFormat with locale-to-BCP47 mapping), `src/__tests__/storage-value-history-chart.test.tsx` (test template — `renderChart` helper wraps in I18nProvider, builds validPoints/mirrorOnly/hinekoraOnly/allNullRatios fixtures, asserts SVG presence + path count + legend labels), `src/__tests__/gold-map-roi-tab.test.tsx` (existing Phase 1 tab tests — uses `mockFetchApi` jest.fn with `jest.requireActual` spread, 9 tests covering offline/error/loading/no-cycles/fetched-at/calls-endpoint/refresh-button — must NOT regress).
- **P10 Phase 2 implementation — `src/lib/types.ts`:** Added 2 new TS interfaces after `TriangularResponse`. `TriangularCycleHistoryPoint` (8 fields, camelCase — `timestamp`, `cycleKey`, `cycleCurrencies`, `rawProfitPct`, `executableEstimate`, `executableProfit`, `confidence`, `snapshotAgeSec`; all numeric `number | null` to mirror backend `float | None` / `int | None` for legacy rows). `TriangularCyclesHistoryResponse` (7 fields — `league`, `cycleKey`, `days`, `points`, `availableCycleKeys`, `dataAvailable`, `fetchedAt`). The proxy route auto-transforms snake_case → camelCase via `transformKeys`, so the frontend types MUST use camelCase to match.
- **P10 Phase 2 implementation — `src/app/api/flipper/triangular/history/route.ts` (NEW):** Next.js proxy route. Calls `proxyWithFallback("/api/v1/arbitrage/triangular/history", ...)` with both `offlineFallback` and `insufficientDataFallback` returning `{ league: "", cycleKey, days, points: [], availableCycleKeys: [], dataAvailable: false, fetchedAt }` — frontend trend chart sees a graceful "no history yet" state instead of an error. NO 45s timeout override needed (history route just reads SQLite, ~10ms). Query params `days` + `cycleKey` forwarded verbatim.
- **P10 Phase 2 implementation — `src/components/dashboard/gold-map-roi-trend-chart.tsx` (NEW, ~310 lines):** Dependency-free SVG chart following the `storage-value-history-chart.tsx` template. Props: `backendOnline: boolean`, `initialDays?: DaysOption` (default 7). Component structure: (1) `useState<DaysOption>` for the days selector (1/7/14/30/90). (2) `useQuery<TriangularCyclesHistoryResponse>` with `queryKey: ["gold-map-roi-trend", days]`, `staleTime: 60_000` (history changes slowly — 5-min-bucket writes), `retry: 1`. (3) `points` wrapped in `useMemo` to keep stable identity when `data` is undefined (avoids `react-hooks/exhaustive-deps` warning). (4) `trendPoints = useMemo(() => pickBestPerTimestamp(points), [points])` — dedupes to one point per timestamp keeping the highest non-null `rawProfitPct`. (5) `geometry = useMemo(...)` — builds SVG path string + Y-axis ticks (5 values across padded min/max, always includes 0) + X-axis ticks (up to 5 timestamps formatted via `formatLocaleDate(ts, locale)`) + zero-line Y position. Y range ALWAYS includes 0 so the dashed zero-line is visible. (6) Render branches: offline (no fetch) → loading → error → "no history yet" (when `!dataAvailable || trendPoints.length < 2 || !geometry`) → full chart with SVG + legend + footer.
- **Pure helper `pickBestPerTimestamp` (exported for unit tests):** Map-based dedup keyed by epoch-ms (parses ISO timestamps via `new Date(p.timestamp).getTime()`, skips unparseable). For each timestamp, keeps the entry with the highest non-null `rawProfitPct`; prefers non-null over null. Returns array sorted ascending by timestamp (oldest first for SVG path drawing).
- **Chart signal decision — raw_profit_pct, NOT expected_div:** `expected_div` depends on user inputs (`goldAmount`, `mapCost`, `goldPerDiv`) that have NO historical persistence — those are localStorage values for "now", not a time-series. The historical signal we DO have is `raw_profit_pct`, which is exactly the multiplier the live `computeRoi` helper consumes. The user can mentally map "higher line = better ROI window" without us pretending to know their historical gold_per_div rate. Documented in component header + design doc §12 Phase 2.
- **P10 Phase 2 implementation — `src/components/dashboard/gold-map-roi-tab.tsx`:** Added `import { GoldMapRoiTrendChart } from "./gold-map-roi-trend-chart"`. Added `<GoldMapRoiTrendChart backendOnline={backendOnline} />` in the main render branch AFTER `<GoldMapRoiCalculator>` and BEFORE the fetched-at footer. Renders inside the same `backendOnline` gate as the calculator (offline/error branches short-circuit before reaching this point, so trend chart correctly hides when tab is in error state).
- **i18n keys — 13 new × 4 locales (1243 total per locale, parity preserved):** Added to `en.ts` / `ru.ts` / `ko.ts` / `zh.ts`: `goldMapTrendTitle`, `goldMapTrendSubtitle` (with `{0}` placeholder for days), `goldMapTrendLoading`, `goldMapTrendError`, `goldMapTrendOffline`, `goldMapTrendEmpty`, `goldMapTrendLegendBestCycle`, `goldMapTrendLegendZero`, `goldMapTrendPointCount` (with `{0}` placeholder for snapshot count), `goldMapTrendCycleKeysCount` (with `{0}` placeholder), `goldMapTrendFetchedAt` (with `{0}` placeholder), `goldMapTrendDaysLabel`, `goldMapTrendDaysValue` (with `{0}` placeholder for days). Verified parity via `grep -c "^[[:space:]]*[a-zA-Z]"` — all 4 locales at 1243. Russian copy is the source of truth per `PRODUCT_VISION.md` §3.1.
- **Test suite — `src/__tests__/gold-map-roi-trend-chart.test.tsx` (NEW, 13 tests):** 2 describe blocks. (1) `pickBestPerTimestamp` (5 tests): empty input returns [], dedup keeps highest profit at same timestamp, prefers non-null over null, sorts ascending by timestamp, skips unparseable timestamps. (2) `GoldMapRoiTrendChart` component (8 tests): offline card + no fetch attempted, loading text while query in flight, "no history yet" when dataAvailable=false, "no history yet" when fewer than 2 points, SVG chart + trend line + zero line + point count when valid data arrives, error card when fetch fails, Days selector renders + default days=7 used, calls `/api/flipper/triangular/history` with days param. Uses same `mockFetchApi` + `jest.requireActual` pattern as `gold-map-roi-tab.test.tsx`. Mock fixtures: `makePoint(offsetMin, profit)` builds a single `TriangularCycleHistoryPoint` with `offsetMin` minutes before `Date.now()`; `makeResponse(overrides)` builds a full `TriangularCyclesHistoryResponse` with sensible defaults.
- **Verification:** `npx tsc --noEmit` → 0 errors. `npx jest src/__tests__/gold-map-roi-trend-chart.test.tsx` → 13/13 pass. `npx jest src/__tests__/gold-map-roi-tab.test.tsx src/__tests__/gold-map-roi-calculator.test.ts` → 51/51 pass (no Phase 1 regressions). `npx jest src/__tests__/i18n.test.tsx` → 26/26 pass (i18n parity preserved). `npx jest` (full suite) → 698/698 pass, 0 regressions. `npx eslint` on all 4 new/modified files → 0 errors, 0 warnings after fixing: unused `fireEvent` import, unused `dayMs` constant, `react-hooks/exhaustive-deps` on `points` (fixed by wrapping `points` in its own `useMemo` with `[data]` dep).
- **Documentation updates:** `STATUS.md` — updated "Last updated" header to iter 132 SHIPPED; updated P10 entry in design-docs table (Phase 2 SHIPPED iter 132); added P10/Phase-2 closed-issue entry with full implementation summary including the "raw_profit_pct not expected_div" rationale; updated P10 entry in technical-debt backlog (Phase 1 + Phase 2 SHIPPED, Phase 3 optional); added new Quick Reference row for "Gold Map ROI trend chart shows 'No cycle history yet'"; added new "Key technical insights" paragraph for the dependency-free SVG line chart pattern (template reference: `storage-value-history-chart.tsx`; key shape: viewBox + PADDING + geometry useMemo + M/L path commands for gaps + Y-axis ticks + X-axis ticks; dedup recipe via `pickBestPerTimestamp`). `docs/design/P10-gold-map-roi-design.md` — updated Status header to "Phase 1 + Phase 2 SHIPPED"; updated §11.2 Q1 (resolved: Phase 1 shipped NEITHER disabled nor hidden — clean cut, no placeholder); updated §12 Phase 2 with full implementation notes (chart signal decision, Days selector, dedup helper, test counts). `worklog.md` — prepended iter-132 entry, trimmed to last 2 iterations (iter 131 + iter 132 — iter 129 moved to `git log`).

Stage Summary:
- **iter 132 SHIPPED — P10 Phase 2 complete.** `GoldMapRoiTrendChart` SVG chart wired into `gold-map-roi-tab.tsx` below the calculator. Consumes the iter-129 `GET /api/v1/arbitrage/triangular/history` route via new proxy `GET /api/flipper/triangular/history`. Charts the best-cycle `raw_profit_pct` per timestamp (deduped to highest non-null profit per 5-min snapshot bucket) with a dashed zero-line + Days selector (1/7/14/30/90, default 7). NO backend changes — reuses existing iter-129 read-only route.
- **Modified files (5):** `src/lib/types.ts` (+60 lines: 2 new TS interfaces), `src/components/dashboard/gold-map-roi-tab.tsx` (+6 lines: import + render), `src/lib/i18n/locales/en.ts` (+13 keys), `src/lib/i18n/locales/ru.ts` (+13 keys), `src/lib/i18n/locales/ko.ts` (+13 keys), `src/lib/i18n/locales/zh.ts` (+13 keys), `STATUS.md`, `docs/design/P10-gold-map-roi-design.md`, `worklog.md`.
- **New files (3):** `src/app/api/flipper/triangular/history/route.ts` (proxy), `src/components/dashboard/gold-map-roi-trend-chart.tsx` (chart + `pickBestPerTimestamp` pure helper), `src/__tests__/gold-map-roi-trend-chart.test.tsx` (13 tests).
- **Test counts:** jest 698 green (685 baseline + 13 new), 0 regressions. `tsc --noEmit` clean. ESLint clean. i18n parity preserved (1243 keys × 4 locales). pytest NOT run — no backend files touched.
- **Design doc §11.2 open questions — resolved:**
  - Q1 (trend chart disabled vs hidden): Phase 1 actually shipped NEITHER (no placeholder, no disabled card). Phase 2 added the chart cleanly once TD-3 Phase 3 landed. Documented as the resolved decision.
- **What was NOT done (intentionally deferred to iter 133+):**
  - **KI-26-audit (P4)** — verify 16 other `replace(tzinfo=utc)` call sites in `backend/`. Most are SAFE (reading naive UTC from SQLite), but each call site should be verified.
  - **TD-3 pipeline_cache optimization** — eliminate doubled compute cost by populating `pipeline_cache` from `_refresh()` (currently `find_triangular_arbitrage` runs both in `_refresh` AND in the live `/triangular` route's first-request cache).
  - **TD-9 fallback removal** — `deriveTrendSparklineData` should be deleted iter 133+ if production logs confirm no fallback path is hit for 2 iters.
  - **Production verification P10 Phase 2:** (1) verify `/api/flipper/triangular/history` proxy returns non-empty `points` once `triangular_cycles` table has ≥ 2 rows; (2) verify the trend chart renders an SVG line on a real backend (not just the jest mock); (3) verify the Days selector refetches with the new `days` query param.
  - **P10 Phase 3 (gold rate SQLite promotion)** — optional, defer until feature gets adoption.
- **Stopping point:** iter 132 = P10 Phase 2 SHIPPED, all baselines preserved (698 jest green, 0 regressions, tsc + eslint clean, i18n parity preserved). Next iter (iter 133+) candidates in priority order: (a) **KI-26-audit** (low-risk verification of 16 timezone call sites — no test failures, latent bug only); (b) **TD-3 pipeline_cache optimization** (eliminate doubled compute cost — backend-only, no UI impact); (c) **TD-9 fallback removal** (delete `deriveTrendSparklineData` once production logs confirm no fallback hits for 2 iters). Recommended: ship (a) alone in iter 133 (closes the KI-26 follow-up), then (b) + (c) in iter 134+.

---

Task ID: iter-131
Agent: main
Task: iter 131 — TD-5 Phase 4. Per iter 130's stopping-point brief: ship TD-5 Phase 4 alone (closes the LAST persistence gap from the TD-3/4/5/9 design doc). Scope = `daily_stats` SQLite table + `HistoricalStore.write/read_daily_stats*` methods + `backend/economy/daily_stats.py` pure helpers (transform_daily_stats + is_daily_stats_fresh + pick_top_items_by_volume) + `GET /api/v1/items/{item_id}/daily-stats` route with lazy-fetch provider fallback + `DataScheduler.refresh_daily_stats` hourly background job + `scripts/backfill_daily_stats.py` one-shot backfill. ~250 lines + script, medium-high risk. Design doc: §4 (schema), §5.2 (lazy fetch + background refresh), §6.3 (backfill via POE2Scout DayCount=90, 1 req/sec), §7 (365-day retention), §9 Phase 4 (file-by-file plan), §10 Q3 (item_id vs api_id — persist both).

Work Log:
- Cloned repo. Read `STATUS.md` (iter 130 SHIPPED — KI-26 hotfix, 1376 pytest green), `worklog.md` (iter 128 + iter 129 + iter 130), design doc `docs/design/TD-3-4-5-9-persistence-gaps-design.md` (full — §4 Option B `daily_stats` table schema with `idx_daily_stats_dedup` UNIQUE on `(date, league, item_id)`, §5.2 lazy fetch + hourly background refresh, §6.3 backfill script with 1 req/sec rate limit, §7 365-day retention, §8.3 rate-limit risk, §9 Phase 4 = ~250 lines + script, §10 Q3 persist both item_id + api_id).
- Read source files for context + templates: `backend/economy/triangular_cycles.py` (TD-3 pure helper template), `backend/economy/market_spreads.py` (TD-4 pure helper template), `backend/data/historical.py` (full — `_CREATE_TABLES_SQL`, `_prune_old_league_data`, `_prune_old_records`, `write/read_triangular_cycles*`, `write/read_market_spreads*`, singleton pattern), `backend/api/routes_market_spreads.py` (route template), `backend/api/routes_arbitrage.py:943-1029` (triangular history route), `backend/api/data_snapshot.py:54-122` (DataSnapshot fields incl `currency_metadata: list[CurrencyInfo]` with `item_id`), `backend/api/data_snapshot.py:617-672` (TD-4/TD-3 best-effort write blocks in `_refresh`), `backend/scheduler.py` (full — DataScheduler class, `collect_price_snapshot`, `start()` with 3 jobs, `_get_snapshot` import), `backend/api/shared.py` (`get_provider()` singleton), `backend/data/providers/poe2scout.py:735-761` (`get_daily_stats` provider method), `backend/data/providers/base.py:54-66` (abstract `get_daily_stats`), `backend/data/schemas.py:171-185` (`DailyStatsPoint`/`DailyStatsResponse` pydantic with PascalCase alias), `backend/api/response_models.py:1085-1146` (`MarketSpreadsHistoryResponse` + `TriangularCyclesHistoryResponse` templates), `backend/main.py:133-240` (lifespan startup — scheduler init), `backend/main.py:695-720` (router registration pattern), `backend/config.py:24-46,165-182` (DataConfig + SchedulerConfig), `backend/models/currency.py:99-107` (CurrencyInfo dataclass with `item_id`), `config.yaml` (full), `tests/conftest.py` (KI-18 process-pool patch), `tests/test_triangular_cycles.py` (test template — helpers + 3 test layers), `tests/test_triangular_cycles_route.py` (route test template with TestClient + patch pattern), `tests/test_scheduler.py` (scheduler test template with MockProvider + fixtures), `tests/test_daily_stats_history.py` (existing DailyStatsCache tests — MOCK_DAILY_STATS sample response reused).
- Installed missing deps into venv: aiosqlite, apscheduler, pydantic, pydantic-settings, pyyaml, numpy, pandas, scipy, scikit-learn, networkx, brotli, fastapi, uvicorn, httpx, cachetools, pytest-asyncio, statsmodels.
- Confirmed baseline: 1376 pytest green in 9.45s (matches iter 130 baseline).
- **TD-5 Phase 4 implementation — `backend/config.py`:** Added `DataConfig.daily_stats_retention_days: int = 365` (design doc §7 — longer than 90-day snapshots because daily candles are smaller + more valuable for long-term backtest). Added `SchedulerConfig.daily_stats_refresh_interval_hours: int = 1` + `SchedulerConfig.daily_stats_top_n_items: int = 50` (design doc §5.2 strategy 2).
- **TD-5 Phase 4 implementation — `backend/data/historical.py`:** Added `daily_stats` table to `_CREATE_TABLES_SQL` (columns: date, league, item_id, api_id, open, high, low, close, average, volume; 3 indexes: `idx_daily_stats_dedup` UNIQUE on `(date, league, item_id)`, `idx_daily_stats_item` on `(item_id, league, date)`, `idx_daily_stats_api_id` on `(api_id, league, date)`). Added 4 methods: `write_daily_stats_batch(league, rows)` — uses INSERT OR REPLACE (NOT INSERT OR IGNORE) because POE2Scout may revise a day's candle; `read_daily_stats(league, item_id, day_count)` — calendar-date filter `date >= date('now', '-N days')`; `read_daily_stats_latest_date(league, item_id)` — `MAX(date)` for lazy-fetch freshness check; `read_daily_stats_items(league)` — distinct `(item_id, api_id)` pairs. Extended `_prune_old_league_data` to also union `daily_stats` leagues + chunked-delete daily_stats for old leagues. Extended `_prune_old_records` with a 4th chunked-delete loop using the 365-day `daily_stats_retention_days` window (separate from the 90-day snapshot window). Updated module docstring.
- **TD-5 Phase 4 implementation — `backend/economy/daily_stats.py` (NEW, ~270 lines):** Three pure helpers. (1) `transform_daily_stats(provider_response, league, item_id, api_id) -> list[dict]` — converts POE2Scout PascalCase `DailyStats` array into persistence rows; accepts both PascalCase + lowercase keys defensively; preserves NULL OHLCV as None (days with no trades); skips non-dict entries + entries missing `Time`; never raises. (2) `is_daily_stats_fresh(latest_date_str, now=None, grace_days=1) -> bool` — returns True when latest row is today or yesterday (grace_days=1 default; "yesterday" allowed because POE2Scout publishes current UTC day's candle only after day rollover); returns False for None/unparseable/future dates. (3) `pick_top_items_by_volume(snapshot, n=50) -> list[tuple[int, str]]` — sums `volume_traded` per currency across all exchange pairs, ranks descending, resolves `item_id` from `snapshot.currency_metadata` (case-insensitive api_id match), dedups by item_id, skips item_id=0, respects n limit.
- **TD-5 Phase 4 implementation — `backend/api/response_models.py`:** Added `DailyStatsPoint` (date, item_id, api_id, open, high, low, close, average, volume — all OHLCV `float | None` for null-trade days) + `DailyStatsHistoryResponse` (league, item_id, api_id, day_count, points, available_item_ids, data_available, source, fetched_at). `source` field ∈ {"sqlite", "provider", "empty"} for debugging the lazy-fetch path.
- **TD-5 Phase 4 implementation — `backend/api/routes_daily_stats.py` (NEW, ~230 lines):** `GET /api/v1/items/{item_id}/daily-stats` route. Three-step read path: (1) `read_daily_stats_latest_date` + `is_daily_stats_fresh` → if fresh, serve from SQLite (`source="sqlite"`); (2) else lazy-fetch from provider via `_lazy_fetch_and_persist` (calls `provider.get_daily_stats` → `transform_daily_stats` → `write_daily_stats_batch` → returns rows, `source="provider"`); (3) if both empty, return whatever SQLite has (`source="empty"` or `"sqlite"`). `_resolve_api_id(item_id)` looks up api_id from snapshot's `currency_metadata` (best-effort, None on miss). All exceptions caught → 200 with `data_available=false` (NOT 500). Path param `item_id: int = Path(ge=1)`, query `day_count: int = Query(30, ge=1, le=365)`.
- **TD-5 Phase 4 implementation — `backend/main.py`:** Added try/except import + `app.include_router(daily_stats_router)` after the market_spreads router block.
- **TD-5 Phase 4 implementation — `backend/scheduler.py`:** Added `refresh_daily_stats() -> int` method to `DataScheduler` — picks top-N items via `pick_top_items_by_volume(snapshot, n=config.scheduler.daily_stats_top_n_items)`, fetches each via `provider.get_daily_stats(league, item_id, day_count=30)`, transforms + persists via `write_daily_stats_batch`; per-item failures logged at debug + continue; returns total rows persisted. Registered as Job 4 in `start()` with `IntervalTrigger(hours=daily_stats_refresh_interval_hours)`, `max_instances=1`, `misfire_grace_time=600`. Updated startup log to report 4 jobs.
- **TD-5 Phase 4 implementation — `scripts/backfill_daily_stats.py` (NEW, ~210 lines):** One-shot backfill script. Args: `--dry-run` (print league + item count, write nothing), `--league` (override config.yaml), `--day-count` (default 90, max 90 per POE2Scout), `--top-n` (0 = ALL items in snapshot's currency_metadata, >0 = top-N by volume). Rate-limited: `asyncio.sleep(1.0)` between requests. Progress log every 10 items. Idempotent (INSERT OR REPLACE). Exit code 0 on success, 2 if any item failed, 1 on snapshot resolution failure.
- **TD-5 Phase 4 implementation — `config.yaml`:** Added `data.daily_stats_retention_days: 365` + `scheduler.daily_stats_refresh_interval_hours: 1` + `scheduler.daily_stats_top_n_items: 50` with comments referencing design doc §5.2 + §7.
- **Test suite — `tests/test_daily_stats_persistence.py` (NEW, 56 tests):** 6 test classes. (1) `TestTransformDailyStats` (13 tests) — None/empty/missing-keys/normal/PascalCase+lowercase/null-OHLCV/non-dict-skipped/missing-Time-skipped/unparseable-becomes-None. (2) `TestIsDailyStatsFresh` (9 tests) — None/empty/unparseable/today/yesterday/2-days-ago/future/grace=0/grace=2. (3) `TestPickTopItemsByVolume` (10 tests) — n<=0/empty-snapshot/no-rates/no-metadata/single-item/ranked-descending/n-limit/item_id=0-skipped/duplicate-dedup/case-insensitive. (4) `TestHistoricalStoreDailyStats` (11 tests) — roundtrip/INSERT-OR-REPLACE-overwrites/empty-write/empty-read/latest-date-max/latest-date-none/read-items-distinct/league-isolation/day-count-filter/null-OHLCV-roundtrip/api_id-null-preserved. (5) `TestDailyStatsRoute` (6 tests) — empty/fresh-sqlite/stale-triggers-provider/provider-failure-fallback/item_id-validation/day_count-validation. (6) `TestSchedulerRefreshDailyStats` (5 tests) — no-items/happy-path/per-item-failure-continues/top-n-respected/snapshot-failure.
- **Verification:** Full pytest suite = 1432 passed (1376 baseline + 56 new), 0 failures, 0 regressions, 9.94s. Timezone-safe: re-ran TD-5 + TD-3 + scheduler tests (105 tests) under `TZ=Asia/Yekaterinburg` (UTC+5) — all green. Verified route registered via `app.routes` inspection. Verified config loads new fields. Verified backfill script `--help` works.
- **Documentation updates:** `STATUS.md` — updated Last updated header (iter 131 SHIPPED, 1432 pytest green), design-docs table (ALL PHASES SHIPPED), added TD-5/Phase-4 to closed-issues, updated TD-5 backlog row (Phase 4 SHIPPED + open verification items), added 2 new Quick Reference rows (daily-stats empty + always-provider), expanded "Three-layer persistence pattern" insight to cover iter 131 + added new "TD-5 Phase 4 adaptation" insight paragraph (two write paths + INSERT OR REPLACE + 365-day retention + freshness grace_days=1). `docs/design/TD-3-4-5-9-persistence-gaps-design.md` — updated Status header to ALL PHASES SHIPPED.

Stage Summary:
- **iter 131 SHIPPED — TD-5 Phase 4 complete.** Closes the LAST persistence gap from the TD-3/4/5/9 design doc. `daily_stats` SQLite table (INSERT OR REPLACE, 365-day retention) + 3 pure helpers (transform/freshness/top-N) + 4 HistoricalStore methods + `GET /api/v1/items/{item_id}/daily-stats` route with lazy-fetch provider fallback + `DataScheduler.refresh_daily_stats` hourly job + `scripts/backfill_daily_stats.py`. Three-layer pattern adapted: write happens in TWO places (route lazy-fetch + scheduler background refresh), NOT inside `_refresh()` — because daily OHLCV is daily cadence + expensive (1 HTTP call per item).
- **Test counts:** pytest 1432 green (1376 baseline + 56 new), 0 regressions. Timezone-safe in UTC+5. Jest/tsc/lint: not run (node_modules not present in env) — no frontend files touched.
- **Stopping point:** iter 131 = TD-5 Phase 4 SHIPPED. All 4 phases of the TD-3/4/5/9 design doc are now complete. Next iter (iter 132+) recommendations: P10 Phase 2 (frontend trend chart consuming `/api/v1/arbitrage/triangular/history`), KI-26-audit (16 `replace(tzinfo=utc)` call sites), TD-3 pipeline_cache optimization (eliminate doubled compute cost), TD-9 fallback removal (verify no fallback path hit in prod logs for 2 iters first).
