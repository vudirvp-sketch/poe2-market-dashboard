# iter 136 — files to `git rm` after extracting this archive.
#
# These files were deleted in iter 136 and are NOT included in the
# archive. Run the following commands from your repo root BEFORE
# running `git add -A`:
#
#   git rm ITER135_README.md
#   git rm DELETIONS.txt
#
# Alternatively, just run `git add -A` after extracting — git will
# automatically stage the deletions when it detects the files are gone
# from the working tree.

ITER135_README.md
DELETIONS.txt
