# Git commands for iter 91
# Run from the root of your local poe2-market-dashboard clone
# after extracting the 3 .md files from this archive.

cd /path/to/your/local/poe2-market-dashboard

# Stage the 3 changed documentation files
git add STATUS.md AGENT_NAVIGATION.md worklog.md

# Commit
git commit -m "iter 91: recon refinement — iter 90 critique + POE2Scout API capability map + refined iter 92-97 plan

Pure recon iteration (NO source code changes). Documentation only.

iter 90 critique — confirmed 6 findings, found 5 errors:
- E1: relative_price is per-currency vs BASE (Exalted), NOT per-pair
- E2: current_quantity (listings) ≠ volume_traded (24h trades) — iter 90 mixed them
- E3: find_price_24h_ago already exists in pricing.py
- E4: Triangular executable_estimate impossible without persistence first
- E5: CURRENCY_DISPLAY_NAMES hardcode is intentional fallback

POE2Scout API capability map (invariant #42):
- 12 endpoints catalogued
- 8 free-data fields already collected by DataSnapshot
- 5 not-available data points (real trades, order book depth, etc)

5 new Proposals F-J (data already collected, need exposition):
- F: Wall detection (highest_stock / current_quantity > 0.3)
- G: OHLCV from DailyStatsHistory (endpoint exists, not used)
- H: Cross-pair volume correlation (from existing price_logs)
- I: Spread persistence (add market_spread to HistoricalStore)
- J: Liquidity-tier UI (tiers.py already computes T1-T5)

3 new Known Issues:
- KI-7: TAB_MAP has 2 dead slots (arbitrage idx 4, graph idx 11)
- KI-8: watchlist-tab.tsx pnl column duplicates change column
- KI-9: Cross-rate inconsistency uses 2 thresholds (10% backend / 5% frontend)

8 new Tech Debt items (TD-1 through TD-8) — see STATUS.md

Refined iter 92-97 plan — see STATUS.md iter 91 row.

Files changed (3, documentation only):
- STATUS.md
- AGENT_NAVIGATION.md
- worklog.md

No tests run (no code changes)."

# Push to remote
git push origin main
