# Git Commands — iter 94

> Run these from your local clone root of `vudirvp-sketch/poe2-market-dashboard`.
> Assumes you've already extracted `iter94-poe2-market-dashboard.zip` over your working tree.

## Step 1 — Verify the merge

```bash
npx tsc --noEmit
# Expected: exit 0 (no errors). Was 8 TS1117 errors before iter 94 (KI-10).

npx jest
# Expected: 428/428 tests pass (was 412/412 before iter 94).
```

## Step 2 — Stage and review

```bash
git status
# Should show:
#   modified:   AGENT_NAVIGATION.md
#   modified:   STATUS.md
#   modified:   worklog.md
#   modified:   src/components/dashboard/flips-helpers.ts
#   modified:   src/components/dashboard/flips-tab.tsx
#   modified:   src/components/dashboard/flips-table.tsx
#   modified:   src/lib/i18n/locales/en.ts
#   modified:   src/lib/i18n/locales/ko.ts
#   modified:   src/lib/i18n/locales/ru.ts
#   modified:   src/lib/i18n/locales/zh.ts
#   new file:   src/__tests__/flips-helpers.test.ts

git diff --stat
```

## Step 3 — Commit

```bash
git add AGENT_NAVIGATION.md STATUS.md worklog.md \
        src/components/dashboard/flips-helpers.ts \
        src/components/dashboard/flips-tab.tsx \
        src/components/dashboard/flips-table.tsx \
        src/lib/i18n/locales/en.ts \
        src/lib/i18n/locales/ko.ts \
        src/lib/i18n/locales/ru.ts \
        src/lib/i18n/locales/zh.ts \
        src/__tests__/flips-helpers.test.ts

git commit -m "iter 94: KI-10 fix + Spread Capture view (Q4/Q5/Q6)

KI-10 (closed): Deleted duplicate flipsBid/flipsAsk entries (lines ~379-380)
from all 4 locale files. Kept iter 92 'Buy at'/'Sell at' entries with tooltips.
tsc --noEmit now exits 0 (was 8 TS1117 errors).

Q4 (Spread tier colors + filter):
- New pure helpers in flips-helpers.ts: classifySpreadTier, spreadTierColor,
  SPREAD_TIER_WIDE_THRESHOLD (5%), SPREAD_TIER_MEDIUM_THRESHOLD (2%).
- FlipsTable Spread cell color-coded: emerald (wide >=5%), amber (medium 2-5%),
  muted (tight <2%).
- New 'Spread tier' Select dropdown in flips-tab.tsx filters row.
- New spreadTierFilter state + filter logic in filteredOpportunities useMemo.

Q5 (Trend sparkline):
- New deriveTrendSparklineData(momentum, volatility) in flips-helpers.ts.
  6 deterministic synthetic points: linear slope from momentum + decaying
  wave from volatility. Uses sin(i*PI/2) (not sin(i*PI) which is always 0).
- New Trend column in FlipsTable (col 17 of 18, lg+ only).
  Uses existing Sparkline component. GRID_COLS expanded 18->19 tracks.
- Tooltip honestly labels this as 'derived indicator, NOT historical'.
- New TD-9: when backend adds priceHistoryShort to FlipOpportunity,
  the column switches to real data without UI changes.

Q6 (Intuitive labels):
- New flipperSpreadTooltip i18n key in 4 locales explaining spread-capture
  intent ('profit margin per round-trip trade before fees').
- Added title={t('profitExaTooltip')} to Profit column header (was missing).
- Added title={t('flipperSpreadTooltip')} to Spread column header (was missing).

i18n: 7 new keys x 4 locales = 28 new lines.
  flipperSpreadTooltip, flipsSpreadTierFilter, flipsSpreadTierWide,
  flipsSpreadTierMedium, flipsSpreadTierTight, flipsTrend, flipsTrendTooltip.

Tests: 16 new unit tests in src/__tests__/flips-helpers.test.ts covering
classifySpreadTier (5), spreadTierColor (3), deriveTrendSparklineData (8).
Jest baseline: 428/428 green (was 412/412).

Docs: STATUS.md (closed KI-10, added iter 94 row + TD-9 + 2 Quick Reference
rows), AGENT_NAVIGATION.md (added invariant #44), worklog.md (iter 94 entry,
trimmed iter 92 to summary)."
```

## Step 4 — Push

```bash
git push origin main
```

## Step 5 — Verify on remote

```bash
git log --oneline -3
# Should show iter 94 commit at HEAD.
```
