#!/bin/bash
# =============================================================================
# iter 105 — Git commands to update the repository
# =============================================================================
#
# Run these commands from your local poe2-market-dashboard/ directory
# AFTER merging the iter105_archive.zip files and running DELETE_obsolete_files.sh.
#
# Verification (run BEFORE committing — all must pass):
#   npx tsc --noEmit                          # clean exit
#   npx next build                            # succeeds in ~9s
#   npx jest                                  # 25 suites / 569 tests green
#   .venv/bin/python -m pytest -q             # 1161 tests green (~6s)
#
# =============================================================================

# 1. Stage all changes (modifications, additions, deletions, renames)
git add -A

# 2. Verify what will be committed
git status

# 3. Commit with descriptive message
git commit -m "iter 105: KI-16 partial (bridge moved to src/lib/) + KI-18 fix (pytest hang from BrokenProcessPool) + cache-snapshot regenerated

KI-16 long-term fix (partial):
- Moved scripts/flipper-backend-bridge.ts -> src/lib/flipper-backend-bridge.ts
- Updated instrumentation.ts import path
- NFT warning still appears (cosmetic) — bridge uses dynamic fs/path ops
- Deep fix tracked as KI-16-deep (P3) in STATUS.md

KI-18 (new, fixed):
- pytest hung indefinitely on test_triangular.py::TestTriangularArbitrageNoFees::test_simple_profitable_cycle_no_fees
- Root cause: ProcessPoolExecutor spawn worker crashed (BrokenProcessPool),
  asyncio.wait_for + pytest-asyncio did not propagate the exception
- Fix: tests/conftest.py autouse fixture patches get_process_pool -> None,
  forcing fast ThreadPoolExecutor fallback for tests
- All 1161 pytest tests now pass in ~6s (was hanging)

cache-snapshot.json regenerated:
- Fetched 14 endpoints from https://poe2scout.com/api on 2026-07-11
- Fresh timestamp 2026-07-11T00:18:32Z (was 2026-06-08)
- Size 448 KB, URL keys use correct https://poe2scout.com/api/ format

Docs cleanup:
- STATUS.md: KI-16 partial, KI-18 closed, KI-16-deep TD added, Quick Ref refreshed
- AGENT_NAVIGATION.md: header -> iter 105, new iter 105 changes section
- worklog.md: trimmed to last 2 iterations
- docs/ARCHITECTURE.md: bridge path updated
- Removed obsolete iter archives (MERGE_INSTRUCTIONS_iter101/102/103.md +
  git_commands_iter101/102/103.txt)
- Removed flipper-bridge.log (400KB runtime log), added to .gitignore

Verified: tsc clean, next build succeeds, jest 569 green, pytest 1161 green"

# 4. Push to remote
git push origin main

# 5. Verify the push
git log --oneline -3
