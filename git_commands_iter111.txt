# Git commands for iter 111 — fix KI-21 (fmtPrice rounding bug) + document KI-22 (ESLint v9 config missing)
#
# Архив с изменёнными файлами: https://tmpfiles.org/dl/w8wQYjWYQMaC/iter111_ki21_fmtprice_fix.tar.gz
# Размер архива: ~67K, 5 изменённых файлов + этот файл с git-командами
#
# Инструкция по слиянию:
# 1. Распаковать архив в корень локальной копии репозитория (сохраняет структуру папок):
#      tar -xzf iter111_ki21_fmtprice_fix.tar.gz
# 2. Проверить diff:
#      git diff --stat
#      Ожидаемый результат:
#        AGENT_NAVIGATION.md                             |   2 +-
#        STATUS.md                                       |  65 ++++++++------
#        src/__tests__/phase-hints-widget.test.tsx       |  31 +++++++
#        src/components/dashboard/phase-hints-widget.tsx |   6 +-
#        worklog.md                                      | 109 +++++++++++-------------
# 3. Запустить тесты локально (опционально):
#      python3 -m pytest -q
#        Ожидаемый результат: 1279 passed (без backend-изменений)
#      npx jest --silent
#        Ожидаемый результат: 582 passed (раньше 580 pass + 1 fail; fail исправлен, +1 regression-тест)
#      npx tsc --noEmit
#        Ожидаемый результат: без ошибок
#      npm run build
#        Ожидаемый результат: success (next build проходит)
#      npm run lint
#        Ожидаемый результат: FAIL (KI-22 — ESLint v9 flat config missing, отложено на след. iter)
# 4. Закоммитить и запушить командами ниже.

# === Stage all changed files ===
git add \
  AGENT_NAVIGATION.md \
  STATUS.md \
  src/__tests__/phase-hints-widget.test.tsx \
  src/components/dashboard/phase-hints-widget.tsx \
  worklog.md

# === Commit ===
git commit -m "iter 111: fix KI-21 (fmtPrice rounding) + document KI-22 (ESLint v9 config missing)

KI-21 (closed): phase-hints-widget.tsx fmtPrice() had a \`price >= 100\` branch
that called toFixed(0), rounding 115.5 -> \"116\" instead of \"115.50\". This
broke the iter-110 jest test 'renders current price with the tracked currency
label'. Fix: removed the >= 100 -> toFixed(0) branch; fmtPrice now always
uses toFixed(2) for >= 1 and toFixed(4) for < 1. Added regression test
'renders large price (>= 1000) with 2 decimals' (1234.5 -> \"1234.50\").

KI-22 (open, deferred): npm run lint fails with 'ESLint couldn't find an
eslint.config.(js|mjs|cjs) file'. Repo declares eslint ^9.39.4 but has no
eslint.config.js (ESLint v9 dropped .eslintrc.* support). Fix recipe
provided in STATUS.md. Low severity — tsc/build/jest/pytest all pass; lint
is not in the build pipeline.

Verification (expected):
- pytest: 1279 passed (no backend changes)
- jest: 582 passed (was 580 pass + 1 fail; fail fixed, +1 regression test)
- tsc --noEmit: pass
- next build: pass
- npm run lint: FAIL (KI-22, deferred)

Docs:
- STATUS.md: KI-21 closed, KI-22 open, header + Quick Reference + insights
- worklog.md: iter-111 entry added, iter-109 trimmed (last 2 iters kept)
- AGENT_NAVIGATION.md: header bumped to iter 111"

# === Push ===
git push origin main
