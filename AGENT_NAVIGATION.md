# PoE2 Market Dashboard — Agent Navigation Guide

> **Single entry point** for codebase navigation. Updated 2026-07-10 (iter 97 — Circuit Patterns full wire-up: new API route `/api/v1/circuit-patterns` in `backend/api/routes_circuit_patterns.py`, Next.js proxy at `src/app/api/flipper/circuit-patterns/route.ts`, new UI tab `src/components/dashboard/circuit-patterns-tab.tsx`, i18n × 4 locales (47 keys × 4), 20 jest tests + 4 pytest route smoke tests. F7 = Done, TD-10 = closed.).
> **Known issues live in [`STATUS.md`](./STATUS.md)** — check there before fixing anything.
> **Product direction lives in [`PRODUCT_VISION.md`](./PRODUCT_VISION.md)** — read it before proposing features.
> **Market patterns & roadmap live in [`docs/MARKET_PLAYBOOK.md`](./docs/MARKET_PLAYBOOK.md)** — read it before proposing new pattern detectors.

---

## 1. Where Things Are

| Directory | Purpose | Rules |
|-----------|---------|-------|
| `backend/` | Python FastAPI analytics engine | Tests mandatory (pytest) |
| `backend/api/` | Route handlers + response models + middleware | Import from `arbitrage/`, `economy/`, `predictors/`, `data/` |
| `backend/api/response_models.py` | Pydantic response models | Must match route return dicts exactly |
| `backend/api/data_snapshot.py` | DataSnapshot — shared TTL-cached snapshot | All routes use `get_snapshot()`, no direct provider calls |
| `backend/api/routes_sse.py` | SSE price stream (P0-1 fixed iter 55) | Sends `{pair, change_pct, new_price, old_price, timestamp}` per changed currency; filters by `threshold_pct` |
| `backend/api/routes_analyst.py` | League analyst summary (P0-3 fixed iter 54) | `_compute_trends` uses `find_price_24h_ago` from `backend.economy.pricing` |
| `backend/api/routes_arbitrage.py` | Flips + triangular + clustering (P1-4 iter 63, P1-9 iter 66, **P2-4 iter 67**) | `/flips` supports `max_score`, `min_spread`, `max_spread`, `cluster`, `currency`, `sort_by`, `sort_dir` (all optional with safe defaults). The standalone `/scanner/scan` endpoint was deleted in iter 68. |
| `backend/api/routes_optimizer.py` | Bellman-Ford conversion paths (P1-8 fixed iter 64) | `_detect_negative_cycle_nodes()` flags profitable arbitrage cycles; `_bellman_ford` returns `None` when target is on a cycle |
| `backend/api/routes_events.py` | Event CRUD + cache invalidation (P1-11 iter 59, P1-7 iter 61) | All 3 endpoints async-await EventManager; `unified_cache` invalidated after mutation |
| `backend/economy/pricing.py` | Unified pricing helpers (P0-5 fixed iter 57, F5 extended iter 77) | `compute_transitive_prices` (BFS) + `find_price_24h_ago` + `compute_zscore` (F5) + `compute_percentile` (F5) |
| `backend/economy/clustering_helpers.py` | Shared clustering data prep + executor function (P1-4 iter 63) | `prepare_clustering_data()` + `run_clustering_sync()` + `CLUSTER_LABELS_CACHE_KEY` |
| `backend/economy/events.py` | EventManager + StoredEvent (P1-7 iter 61, **P3-3 iter 71**) | `event_id`, `is_active`, `created_at`; 4 methods async; `_prune_expired` left sync intentionally. **All in-memory `_events` access guarded by `threading.RLock`**; SQLite writes await OUTSIDE the lock. |
| `backend/economy/lifecycle.py` | PhaseDetector (P0-4 fixed iter 54) | `_reference_date` returns `patch_reset_date` unconditionally when set. Used by `phase_hints.py` (iter 78) via `get_phase_detector()` singleton. |
| `backend/economy/phase_hints.py` | **F6 (iter 78)** — phase-aware advisory hints (Temporalis, skill gems, etc.) | Pure function `get_phase_hints(phase, days_since_reference, ...)`. Hardcoded `_PHASE_HINTS` table: 4 hints per phase (EARLY/MID/LATE) + `_PHASE_META` table for phase_label/summary. Does NOT depend on DataSnapshot — uses PhaseDetector only. Always returns `data_available=True`. Helpers `list_phases_with_hints()` + `hint_count_for_phase()` exposed for tests. |
| `backend/economy/content_pulse.py` | **F3 (iter 75) + iter 95 (TD-2 + Q13 Overheat Index)** — daily turnover per category + 7d/30d rolling + signal + overheat | Pure function `compute_content_pulse(snapshot, config)`. No side effects. Tunable thresholds at module top: `SIGNAL_RISING_THRESHOLD_PCT=10.0`, `SIGNAL_FALLING_THRESHOLD_PCT=-10.0`, `TOP_N_PER_CATEGORY=3`, `OVERHEAT_VOLUME_SPIKE_THRESHOLD=2.0`, `OVERHEAT_PRICE_DROP_THRESHOLD=-5.0` (iter 95). |
| `backend/economy/speculation.py` | **F5 (iter 77)** — per-item z-score + BUY/SELL/HOLD signals | Pure function `compute_speculation_signals(snapshot, config, days=30, limit=50, signal_filter="ALL")`. No side effects. Tunable thresholds at module top (`Z_BUY_THRESHOLD=-1.5`, `Z_SELL_THRESHOLD=1.5`, `MAX_HISTORY_POINTS=14`, `MIN_SAMPLE_SIZE=2`, `DEFAULT_DAYS=30`, `DEFAULT_LIMIT=50`). |
| `backend/economy/speculation_backtest.py` | **F5 follow-up (iter 79)** — backtest the z-score BUY/SELL/HOLD strategy on historical price_logs | Pure function `backtest_speculation_signals(snapshot, config, eval_days_ago=14, holding_days=7, lookback_days=30, limit=50, signal_filter="ALL")`. Reuses `compute_zscore` + `_extract_prices` + `_signal_from_zscore` + `Z_BUY_THRESHOLD`/`Z_SELL_THRESHOLD`/`MIN_SAMPLE_SIZE` from `speculation.py` — guarantees backtest uses the same strategy as the live signal. Returns per-trade results + per-signal aggregates (count, win_rate, mean/median/best/worst return_pct). Tolerance `TOLERANCE_HOURS=24` between target timestamp and nearest price log (matches `storage_value_history.py`). Baseline window strictly BEFORE entry timestamp (entry price not in baseline — no signal leak). |
| `backend/economy/storage_value_history.py` | **F2 follow-up (iter 75)** — time-series of currency/mirror + currency/hinekora ratios | Pure function `compute_storage_value_history(snapshot, currency, days=30)`. Uses 24h nearest-neighbor tolerance for matching timestamps across the three histories. |
| `backend/economy/circuit_patterns.py` | **F7 (iter 96)** — trajectory classification per currency (P8 from `docs/MARKET_PLAYBOOK.md`) | Pure function `compute_circuit_patterns(snapshot, config, *, days=30, limit=50, trajectory_filter="ALL", now=None)`. Classifies each currency into one of 7 archetypes: `EXPONENTIAL_GROWTH` / `LINEAR_GROWTH` / `PEAK_THEN_DECLINE` / `MEAN_REVERTING` / `VOLATILE` / `DECLINING` / `STABLE`. Uses OLS linear regression (stdlib only — no numpy/pandas) on price_logs day-index. Tunable thresholds at module top (`MIN_SAMPLE_SIZE=4`, `MIN_R_SQUARED=0.7`, `EXP_GROWTH_MIN_TOTAL_PCT=50`, `PEAK_DECLINE_MIN_PCT=20`, `STABLE_MAX_CV=0.15`, `VOLATILE_MIN_CV=0.5`). Each currency gets `recommended_action`: HOLD_FOR_GROWTH / SELL_NOW / AVOID / WATCH / NEUTRAL. **iter 97:** now also emits `price_history_short` (up to 14 most-recent price points, oldest-first) for the UI mini-sparkline. 75 pytest tests in `tests/test_circuit_patterns.py` (iter 96) + 4 route smoke tests (iter 97) = 79 total. |
| `backend/api/routes_circuit_patterns.py` | **F7 / P8 (iter 97)** — route handler `GET /api/v1/circuit-patterns?days=30&limit=50&trajectory=ALL` | Thin wrapper: fetch snapshot → call `compute_circuit_patterns` → inject `days` field into the response dict (the pure function does not echo `days` back — only route handlers need it for client cache keys) → shape response. Query params validated by FastAPI (`ge=1, le=90` for days, `ge=1, le=500` for limit, `pattern=^(ALL\|EXPONENTIAL_GROWTH\|LINEAR_GROWTH\|PEAK_THEN_DECLINE\|MEAN_REVERTING\|VOLATILE\|DECLINING\|STABLE)$` for trajectory). Returns `data_available=false` + empty `patterns` list when snapshot is not loaded or computation throws. 4 pytest route smoke tests in `tests/test_circuit_patterns.py::TestRouteHandler`. |
| `backend/api/routes_content_pulse.py` | **F3 (iter 75)** — route handler `GET /api/v1/content-pulse` | Thin wrapper: fetch snapshot → call `compute_content_pulse` → shape response. Returns `data_available=false` + empty `categories` list when snapshot is not loaded. |
| `backend/api/routes_speculation.py` | **F5 (iter 77)** — route handler `GET /api/v1/speculation?days=30&limit=50&signal=ALL` | Thin wrapper: fetch snapshot → call `compute_speculation_signals` → shape response. Query params validated by FastAPI (`ge=1, le=90` for days, `ge=1, le=500` for limit, `pattern=^(ALL\|BUY\|SELL\|HOLD)$` for signal). Returns `data_available=false` + empty `signals` list when snapshot is not loaded. |
| `backend/api/routes_speculation_backtest.py` | **F5 follow-up (iter 79)** — route handler `GET /api/v1/speculation/backtest?eval_days_ago=14&holding_days=7&lookback_days=30&limit=50&signal=ALL` | Thin wrapper: fetch snapshot → call `backtest_speculation_signals` → shape response. Query params validated by FastAPI (`ge=1, le=365` for eval_days_ago, `ge=1, le=90` for holding_days/lookback_days, `ge=1, le=500` for limit, `pattern=^(ALL\|BUY\|SELL\|HOLD)$` for signal). Returns `data_available=false` + empty `trades` + zeroed stats blocks when snapshot is not loaded. |
| `backend/api/routes_phase_hints.py` | **F6 (iter 78)** — route handler `GET /api/v1/phase-hints` | Thin wrapper: fetch PhaseDetector singleton → call `detector.get_phase_info()` → forward to `get_phase_hints()` pure function. Always returns `data_available=True` (hint table is hardcoded — does NOT depend on DataSnapshot). On exception returns minimal response with `data_available=False` (no 500). |
| `backend/api/routes_storage_value.py` | Storage value routes (existing `/storage-value/{currency}` + new `/storage-value/{currency}/history` iter 75) | Both routes return `data_available=false` with empty payload when snapshot not loaded — no 503. |
| `backend/data/historical.py` | SQLite store for price snapshots + events | Chunked delete (P1-6 + P3-2 iter 66) — `rowid IN (SELECT ... LIMIT ?)` pattern |
| `backend/data/unified_cache.py` | UnifiedCache with namespaces: `pipeline`, `daily_stats` | Shim modules deleted in iter 66 (P2-2) — import directly |
| `backend/data/currency_names_ru.py` | Thin loader (63 lines) for `currency_names.json` (P2-3 closed iter 70) | Edit `currency_names.json`, not the `.py` |
| `backend/api/data_snapshot.py` | DataSnapshot + SnapshotManager (**P3-4 iter 71**) | Atomic `(snapshot, ts)` swap via immutable `_SnapshotState` dataclass. `_history_cache` + `_active_currencies` guarded by separate `_cache_lock`. `last_snapshot` reads `_state` once. |
| `backend/arbitrage/scorer.py` | Opportunity scoring + quantized analysis (P1-5 fixed iter 66) | `compute_quantized_analysis` uses bounded linear scan `O(1/D)` |
| `backend/arbitrage/triangular.py` | Triangular arbitrage | `find_triangular_arbitrage(rates, min_profit_pct, ...)` — uses `get_process_pool()` (P2-13 fixed iter 65) |
| `backend/main.py` | FastAPI app + lifespan + lazy `process_pool` | `get_process_pool()` lazily creates/recreates the pool (P2-13). Backward-compat: `from backend.main import process_pool` still works via module `__getattr__` but emits `DeprecationWarning`. |
| `backend/predictors/time_series.py` | LightGBM forecaster (**P2-9 iter 67**) | `train()` has adaptive fallback: when `floor (5) <= len(log_prices) < min_points (15)`, uses minimal features (`price_lags=[1]` only) + lowered clean-rows threshold (2 instead of 10). Below `floor`, skips. |
| `backend/predictors/` | Time-series, anomaly, clustering, storage_value, model_store | CPU-heavy → ProcessPoolExecutor |
| `backend/data/providers/` | Poe2Scout, official, base | Network IO only |
| `backend/models/` | Core dataclass models | No framework imports |
| `backend/config.py` | Pydantic config models | `ForecastingConfig.lightgbm_min_data_points_floor` added iter 67 (default 5) |
| `src/app/api/flipper/` | Next.js proxy routes → FastAPI | **Proxy only, no business logic** |
| `src/app/api/flipper/health/circuit-breakers/route.ts` | **P2-6 (iter 67)** — exposes per-endpoint CB state as JSON | Read-only; calls `getAllEndpointCircuitBreakers()` from flipper-proxy |
| `src/app/api/flipper/prices/stream/route.ts` | SSE proxy | 5min timeout, streams body |
| `src/app/api/flipper/events/route.ts` | Events POST proxy with body transform | `eventType`→`event_type`, `expiryHours`→`expires_at` ISO |
| `src/components/dashboard/` | Tab components, dialogs, sidebar | Import from `@lib`, `@hooks` |
| `src/hooks/` | React hooks (19 hooks) | Import from `@lib` |
| `src/lib/` | Shared utilities, types, store, i18n, proxy, poe2api | **Types in `types.ts` ONLY** |
| `src/lib/flipper-proxy.ts` | Proxy with per-endpoint circuit breaker + dedup + mode-aware 5xx fallback (P1-10 iter 66, **P2-8 iter 69**) | `Map<path, EndpointCircuitBreaker>` keyed by normalized path; `proxyWithFallback` passes non-503 5xx through in dev, returns 200+`X-Flipper-Fallback` header in prod. Exports `isFlipperFallbackResponse`, `getFlipperFallbackOriginalStatus`, `FLIPPER_FALLBACK_HEADER` |
| `src/hooks/use-price-stream.ts` | SSE hook (P0-1 iter 55, P2-7 iter 59) | `invalidateCaches(pair)` — per-pair benchmark invalidation |
| `src/hooks/use-flipper-backend.ts` | **iter 81** — dashboard-level flipper backend status (Stage 1 of `useDashboardData` extraction) | Single source of truth for the 3 dashboard-level flipper queries: `/api/flipper/health` (always on, 30s polling), `/api/flipper/phase` (gated on online, 60s polling), `/api/flipper/events?active_only=true` (gated on online, 30s polling). Returns `{ flipperBackendOnline, flipperUpstreamReachable, flipperPhaseData, activeEventsCount, flipperHealthData?, flipperHealthPending, flipperHealthError, flipperEventsData? }`. All query keys go through `QUERY_KEYS` (no inline `useQuery` for these endpoints anywhere else). Inline `useQuery` for `/api/flipper/health`, `/api/flipper/phase`, or `/api/flipper/events` from `dashboard-page.tsx` is now FORBIDDEN — use the hook. |
| `src/hooks/use-realms-and-leagues.ts` | **iter 82** — realm/league selection + realms/leagues queries (Stage 2 of `useDashboardData` extraction) | Single source of truth for realm/league state and the two dropdown-populating queries: `/api/poe2/realms` (always on) and `/api/poe2/leagues` (gated on realm). Owns the `realm` + `league` useState so the auto-select useEffect (auto-pick first active league when none selected) can fire when `leagues` arrives. Returns `{ realm, setRealm, league, setLeague, realms, realmsLoading, leagues, leaguesLoading, effectiveLeague }`. `setRealm` clears the league (matches prior inline behaviour); `setLeague` persists to the Zustand store via `persistLeague`. `effectiveLeague` falls back to active league, then first league, then `""`. Inline `useQuery` for `/api/poe2/realms` or `/api/poe2/leagues` from `dashboard-page.tsx` is now FORBIDDEN — use the hook. |
| `src/hooks/use-filtered-exchange-pairs.ts` | **iter 83** — Exchange tab filter pipeline (Stage 3a of `useDashboardData` extraction) | Single source of truth for the search → quick-chip → extended-numeric-filter derivation that turns raw `exchangeData` into the `exchangePairs` array consumed by the Exchange tab. Pure hook — no queries, no store reads, no state. Takes `{ exchangeData, search, exchangeUiState }` (where `exchangeUiState` is the `uiState.exchange` slice from the Zustand store) and returns a derived `ExchangePair[]`. Filter rules (case-insensitive search on either currency name; `topVolume` chip = top 20 by volume desc, applied AFTER search; `favorites` chip = pairs in `exchangeUiState.favorites`; extended `minVolume`/`maxVolume` inclusive; `minChange`/`maxChange` ignored when 0; `changePercent` treated as -∞/+∞ when null) match the prior inline `useMemo` exactly — zero behavior change. Inline `useMemo` re-implementing this filter pipeline in `dashboard-page.tsx` is FORBIDDEN — use the hook. |
| `src/hooks/use-item-category-lists.ts` | **iter 83** — Currency/unique category-chip list derivation (Stage 3a of `useDashboardData` extraction) | Single source of truth for `currencyCategories` + `uniqueCategoriesList` — the two chip-strip arrays rendered above the Currencies and Uniques tabs. Pure hook — no queries, no store reads, no state. Takes `{ uniqueCategories, t }` (raw `useItemCategories()` response + the i18n `t` function for the synthetic "all" fallback label) and returns `{ currencyCategories, uniqueCategoriesList }`. Currency list = `name !== "Unique"`; unique list = `name === "Unique"` OR substring match on `Unique`/`Armour`/`Weapon`/`Accessory`/`Flask`/`Jewel`/`Gem`. Both lists push `{ name: "all", displayName: t("all"), count: 0 }` when empty so the chip strip always renders something. Inline `useMemo` re-implementing these filter rules in `dashboard-page.tsx` is FORBIDDEN — use the hook. |
| `src/hooks/use-optimal-payment.ts` | **iter 84** — §11 optimal-payment cluster (Stage 3b of `useDashboardData` extraction; COMPLETE) | Single source of truth for the optimal-payment pipeline. Owns (a) `optimalCurrencyData` useQuery (gated on `flipperBackendOnline`, queryKey `QUERY_KEYS.flipperOptimalCurrency`, 60s staleTime/refetchInterval, retry: 1 — UNCHANGED from prior inline call); (b) `clientOptimalResult` memo (client-side fallback using `findOptimalPayment` + `isItemCategory` from `@/lib/currency-optimal`, depends on `exchangeData` + `crossRates.relativePriceMap`/`anchorId`/`anchorRelPrice`/`crossRateFlips`); (c) backend/client merge memo (backend data takes priority when `dataAvailable && optimalPaymentByPair`, otherwise falls back to client; backend keys `"currencyFrom_currencyTo"` are remapped to frontend `pair.id`); (d) `optimalPaymentByDisplayName` memo (display-name-keyed `Map<string, OptimalPaymentResult>` for FlipsTab). Takes `{ exchangeData, crossRates, flipperBackendOnline }` (all owned by parent) and returns `{ optimalPaymentByPair, crossRateFlips, selectedAnchorId, optimalPaymentByDisplayName }`. Inline `useQuery` for `/api/flipper/optimal-currency` OR inline `useMemo` re-implementing the client fallback / merge / byDisplayName logic in `dashboard-page.tsx` is FORBIDDEN — use the hook. |
| `src/components/dashboard/dashboard-page.tsx` | Parent wiring component, **995 lines** (iter 84: −133 lines via `useOptimalPayment` extraction — extraction COMPLETE; iter 83: −41 lines via `useFilteredExchangePairs` + `useItemCategoryLists`; iter 82: −28 lines via `useRealmsAndLeagues`; iter 81: −35 lines via `useFlipperBackend`). 6 presentational subcomponents extracted in iter 71-73. | `useDashboardData` extraction COMPLETE — no further staged refactors planned. dashboard-page.tsx is now legitimate parent wiring. Future code-health work should be opportunistic (per-file) rather than dashboard-page-specific. |
| `src/components/dashboard/dashboard-toolbar.tsx` | Toolbar (TabsList + action buttons + category chips) (**P2-1 iter 73, step 4a**) | Pure presentational. Owns the tab strip + keyboard-shortcuts/alerts/comparison/pair-comparison buttons + the currencies/uniques category-filter chip strip. Tab switching still goes through the parent `<Tabs onValueChange=...>` scope. |
| `src/components/dashboard/dashboard-dialogs.tsx` | Dialog/sheet/banner wrappers (**P2-1 iter 73, step 4b**) | Pure presentational. Wraps DetailDialog, PairDetailDialog, ComparisonDialog, PairComparisonDialog, PriceAlertDialog, EventsSidebar, OfflineBanner, ShortcutsDialog — 8 primitives that sit at the bottom of the Dashboard render tree. Each open/close flag is a prop. |
| `src/components/dashboard/exchange-tab-content.tsx` | Exchange tab content (**P2-1 iter 71**) | Pure presentational component. Takes all state as props from `Dashboard` — no store/i18n imports of its own. Pattern reused by the iter 72-73 extractions. |
| `src/components/dashboard/currencies-tab-content.tsx` | Currencies tab content (**P2-1 iter 72**) | Pure presentational. Owns data-freshness badge, loading/empty/error states, virtual-vs-static grid switch, pagination. |
| `src/components/dashboard/uniques-tab-content.tsx` | Uniques tab content (**P2-1 iter 72**) | Pure presentational. Owns data-freshness badge, loading/empty/error states, UniqueTable, pagination. |
| `src/components/dashboard/overview-tab-content.tsx` | Overview tab content (**P2-1 iter 72**, updated iter 76) | Pure presentational. Composes 3 panels, each wrapped in its own ErrorBoundary: ContentPulseWidget (F4 iter 76, mounted FIRST for first-load visibility) + MarketOverview + ComparativeChart. |
| `src/components/dashboard/storage-value-tab.tsx` | Storage Value tab (**F2 iter 74 + iter 75**) | Wraps the existing `/api/v1/storage-value/{currency}` endpoint. Lazy-loaded, ErrorBoundary-wrapped. Currency picker (default list of 14 canonical currencies) + horizon picker (1/6/24/48/168h) + quantity input + Compute button. Renders: decision card (BUY_HOLD / SELL_CONVERT / NEUTRAL with hint), projection breakdown (current/projected/risk-discount/adjusted/net/ratio), holdings totals (×quantity), inputs panel (momentum/volatility/acceleration/liquidity/α/horizon), historical chart (iter 75). Full i18n (en/ru/zh/ko). Backend offline → offline card with start-backend hint. data_available=false → "no price history" notice. 12 jest tests in `src/__tests__/storage-value-tab.test.tsx`. |
| `src/components/dashboard/storage-value-history-chart.tsx` | Storage Value history chart (**F2 follow-up iter 75**) | Dependency-free SVG line chart (~290 lines) rendering two ratios: `currency/mirror` (blue) and `currency/hinekora` (emerald). Fetches via `useQuery` bound to `/api/flipper/storage-value/[currency]/history?days=30`. Graceful degradation: <2 points → "no history" notice; all-null ratios → "no reference data" notice; loading → spinner text. 11 jest tests in `src/__tests__/storage-value-history-chart.test.tsx`. |
| `src/components/dashboard/content-pulse-widget.tsx` | Content Pulse widget — «Что фармить сегодня» (**F4 iter 76**) | Two-column card showing top rising (emerald) + falling (red) league mechanic categories with per-category `delta_7d_pct` badge + top-3 movers (`trend_pct` per item). Fetches via `useQuery` (60s staleTime) bound to `/api/flipper/content-pulse`. Mounted FIRST in `overview-tab-content.tsx` (above MarketOverview) so it's visible on first dashboard load — wrapped in its own `<ErrorBoundary>`. Graceful degradation: backendOffline → compact amber notice (no full-card takeover); loading → spinner text; error → error card + refresh button; data_available=false → "no data yet"; all categories stable → "no signals today"; empty top_rising/top_falling → "no movers" per category. `maxPerSide` prop (default 2) caps category count per column. 16 jest tests in `src/__tests__/content-pulse-widget.test.tsx`. |
| `src/components/dashboard/speculation-tab.tsx` | Speculation tab — BUY/SELL/HOLD per item + collapsible Backtest panel (**F5 iter 77 live + iter 80 backtest UI**) | Renders a sortable list of currencies with per-item z-score (vs N-day rolling mean/std) + percentile + BUY/SELL/HOLD signal badge + horizon hint + dependency-free SVG mini-sparkline (last 14 price points). Filter chips (ALL/BUY/SELL/HOLD), days selector (7/14/30/90). Fetches via `useQuery` (30s staleTime) bound to `/api/flipper/speculation?days={days}&limit=50&signal={filter}`. Lazy-loaded via `next/dynamic`, wrapped in `<ErrorBoundary>`. Tab is in `TAB_MAP` at index 9 (between `storage-value` and `liquid-chain`) for keyboard-shortcut navigation. Tab trigger in `dashboard-toolbar.tsx` (Sparkles icon). **Below the signals list — collapsible Backtest panel (iter 80)**: toggle button (NOT autoload — `enabled: showBacktest && backendOnline` gates the `useQuery`), 3 day selectors (eval_days_ago 7/14/30/90 default 14; holding_days 1/3/7/14/30 default 7; lookback_days 7/14/30/90 default 30), 3 stats blocks (Overall/BUY/SELL with count+winRate+mean/median/best/worst return_pct), signal breakdown (BUY/SELL/HOLD counts + evaluated/unevaluated), top-trades list (capped by `limit=50`, sorted by |return_pct| desc from backend, each row shows signal badge + item name + category + entry → exit + colored return_pct). Parent's `signalFilter` forwarded as `signal` query param. Graceful degradation: collapsed (default) → only toggle button; expanded + loading → spinner; expanded + error → red notice; expanded + dataAvailable=false → "no data yet"; expanded + trades=[] → "no trades produced"; expanded + trades>0 → full content. 18 jest tests in `speculation-tab.test.tsx` + 15 jest tests in `speculation-backtest-panel.test.tsx`. |
| `src/components/dashboard/circuit-patterns-tab.tsx` | Circuit Patterns tab — trajectory classification per currency (**F7 / P8 iter 97**) | Renders a sortable list of currencies (sorted by `\|totalChangePct\|` desc from backend) with per-item trajectory badge (one of 7 archetypes: EXPONENTIAL_GROWTH / LINEAR_GROWTH / PEAK_THEN_DECLINE / MEAN_REVERTING / VOLATILE / DECLINING / STABLE) + recommended_action badge (HOLD_FOR_GROWTH / SELL_NOW / AVOID / WATCH / NEUTRAL) + signed total_change_pct + dependency-free SVG mini-sparkline (last 14 price points, colored by trajectory archetype). Filter chips (ALL + 7 archetypes), days selector (7/14/30/90). Fetches via `useQuery` (30s staleTime) bound to `/api/flipper/circuit-patterns?days={days}&limit=50&trajectory={filter}`. Lazy-loaded via `next/dynamic`, wrapped in `<ErrorBoundary fallbackTitle={t("fallbackCircuitPatterns")}>`. Tab is in `TAB_MAP` at index 9 (between `speculation` at 8 and `liquid-chain` at 10) for keyboard-shortcut navigation — shortcut "0" → Circuits (was Liquid Chain pre-iter 97). Tab trigger in `dashboard-toolbar.tsx` (Activity icon, between Sparkles and Droplets). Pattern rows show: trajectory badge (color-coded) + action badge (color-coded) + item name + category + total_change_pct + stats line (sampleSize · slope%/d · vol · R² · current · daysSincePeak for PEAK_THEN_DECLINE) + sparkline (colored by archetype — emerald for growth, amber for peak, red for declining, fuchsia for volatile, slate for stable/mean-reverting). Graceful degradation: backendOffline → offline card with start-backend hint; loading → "Classifying trajectories..."; error → red notice + refresh; dataAvailable=false → "no price history yet"; empty patterns → "no actionable patterns in the current window". 20 jest tests in `circuit-patterns-tab.test.tsx`. Mirrors the speculation-tab pattern but is simpler — no Backtest panel (trajectory classification is a single-pass computation, no replay variant needed). i18n: 47 keys × 4 locales (`circuitTitle`, `circuitSubtitle`, `circuitFilterAll`, `circuitFilterExpGrowth`, `circuitTrajExpGrowth`, `circuitActionHoldForGrowth`, `circuitDaysValue`, `circuitSampleSize`, `circuitSlope`, `circuitVolatility`, `circuitRSquared`, `circuitCurrent`, `circuitDaysSincePeak`, etc.). |
| `src/components/dashboard/phase-hints-widget.tsx` | Phase-aware hints widget — «League Phase Hints» (**F6 iter 78**) | Static info banner mounted BELOW ContentPulseWidget in `overview-tab-content.tsx`. Shows current phase badge (emerald/violet/amber for EARLY/MID/LATE) + day count + reference currency + phase summary + bulleted hint list (each row: bullet + title + detail + action). Fetches via `useQuery` (5min staleTime — phase only changes daily) bound to `/api/flipper/phase-hints`. Wrapped in `<ErrorBoundary fallbackTitle={t("fallbackPhaseHints")}>`. Graceful degradation: backendOffline → compact amber notice; loading → spinner; error → card + refresh; data_available=false → "no data" notice; empty hints → "no hints" notice. 26 jest tests in `src/__tests__/phase-hints-widget.test.tsx`. |
| `scripts/sync_currency_names_from_poe2db.py` | **F1 sync script (iter 85) + live-run verified (iter 86)** — CLI tool to extend `currency_names.json` with new RU translations from `poe2db.tw/ru/`. | Standalone Python script (stdlib only — no extra deps beyond `requirements.txt`). 4-stage pipeline, each stage is a separate subcommand: `--fetch-ids` (hits poe2scout.com API to enumerate all api_ids + EN names + categories, paginates ByCategory up to 250/page) → `--fetch-ru` (scrapes `poe2db.tw/ru/<Category>` HTML pages for EN→RU name pairs) → `--diff` (writes `scripts/.cache/currency_names_patch.json` with `action: "add"` / `"conflict"` / `"skip"` per api_id) → `--apply --confirm` (writes new entries to `backend/data/currency_names.json` ATOMICALLY via tmp+rename). Plus `--from-cache-snapshot` fallback (extracts the 138 api_ids from `src/data/cache-snapshot.json` — no network needed). The script is IDEMPOTENT (re-running --apply with the same patch is a no-op) and NEVER overwrites existing translations (conflicts go into the patch for manual review). After --apply, the maintainer MUST bump count assertions in `tests/test_currency_names_ru.py` (lines 30-33) if any new keys were added, then run `pytest tests/test_currency_names_ru.py`. 32 pytest tests in `tests/test_sync_currency_names.py`. **Iter 86 live-run results:** 639 poe2scout items enumerated, 186 EN→RU pairs scraped from poe2db.tw (8 of 17 categories returned data — currency/runes/ultimatum/abyss/incursion/idol returned 0 pairs; fragments/vaultkeys/uncutgems/lineagesupportgems 404'd), 297 already translated, 0 new candidates matched, 1 conflict (`against-the-darkness` — fixed inline by aligning EN to canonical full name), 342 no-match (poe2db.tw has no pages for these items). Final counts unchanged: ru=349, en=349. **NOTE on `normalize_api_id`**: the script PRESERVES hyphens (matches `currency_names.json` key convention — 315 of 349 keys use hyphens); this is DIFFERENT from the backend's `_normalize_api_id` in `poe2scout.py:58` which replaces hyphens with underscores for exchange-pair matching. Don't confuse the two. |

## 2. Build & Run Commands

```bash
npm install && npm run dev        # Frontend (port 3000)
npm run build && npm run test     # Build + Jest
pytest tests/ -v                  # Backend tests (skip e2e: --ignore=tests/e2e)
npx tsc --noEmit                  # TypeScript type check
npx playwright test               # E2E tests

# Backend (start.sh creates .venv automatically)
PYTHONPATH=. .venv/bin/python -m uvicorn backend.main:app --reload --port 8000

# Regenerate OpenAPI schema + TypeScript types (after API contract changes)
python -c "import json; from backend.main import app; from fastapi.openapi.utils import get_open_api; s=get_open_api(title=app.title,version=app.version,routes=app.routes); open('openapi_schema.json','w').write(json.dumps(s,indent=2,ensure_ascii=False))"
npx openapi-typescript openapi_schema.json --output src/lib/api-types.ts
```

## 3. Critical Rules (invariants)

1. **Backend responses use snake_case** — `flipper-proxy.ts` `transformKeys()` converts to camelCase.
2. **All REST paths use `/api/v1/` prefix.** (WS paths removed in iter 58.)
3. **Bridge health check uses `/api/v1/health/ping`** (NOT `/api/health/ping`).
4. **`response_model=` must match route return dict** — mismatch = 500.
5. **ProcessPoolExecutor: picklable args only** — no `sqlite3.Connection`, no `EventManager`, no `DataSnapshot`, no `AppConfig`. Use `FlipComputeBundle` pattern.
6. **CPU-bound Python → `run_in_executor()` with timeout** — never block event loop.
7. **Never hardcode league names or currency categories** — use `config.yaml`.
8. **`FLIPPER_WORKERS` env var** — controls ProcessPoolExecutor workers (default: 1).
9. **SSE proxy: return 200 + error event** — not 503 (prevents retry storms).
10. **Mock provider exchange_rates keys MUST be strings** — `"exalted/chaos"`, NOT `("exalted", "chaos")`.
11. **EventData uses `event_id`** (not `id`), `is_active` (not `active`), `created_at` — matches `StoredEvent.to_dict()`.
12. **EventType enum has 6 values**: `major_patch`, `minor_patch`, `league_start`, `economy_shift`, `streamer_hype`, `other`.
13. **Events POST proxy transforms body**: `eventType`→`event_type`, `affectedCurrencies`→`affected_currencies`, `expiryHours`→`expires_at` (ISO string).
14. **PhaseDetector: only `major_patch` resets phase clock** — `league_start` / `economy_shift` affect scoring only.
15. **Adaptive mode (`"_adaptive"`)**: `baseCurrencyApiId` can be `"_adaptive"` — `useDisplayPrice` auto-selects Div/Exa/Chaos per row.
16. **`pipeline_cache.invalidate()` clears only `pipeline` namespace** — `daily_stats` is separate.
17. **SSE payload contract** (P0-1): `{pair, change_pct, new_price, old_price, timestamp}` per changed currency — matches `SSEPriceUpdate`. `threshold_pct` query param is respected.
18. **Real-time updates = SSE + REST polling only** (iter 58+). WebSocket endpoints were removed in iter 58 — do NOT re-introduce them.
19. **Optimizer negative cycles** (P1-8): a negative cycle in `-log(rate)` space = profitable arbitrage. When `_bellman_ford` detects one and `target` lies on it, returns empty `path` with `data_available: true` — callers fall back to `direct_rate`.
20. **ProcessPoolExecutor is lazy** (P2-13): always call `get_process_pool()` at the call site — never cache the returned reference.
21. **LightGBM adaptive fallback** (P2-9, iter 67): `train()` proceeds with minimal features (`price_lags=[1]`) when `floor (5) <= len(log_prices) < min_points (15)`. Below `floor`, skips. Configurable via `ForecastingConfig.lightgbm_min_data_points_floor`.
22. **Scanner is deleted** (P2-4, iter 68 + iter 69): the standalone `/api/v1/scanner/scan` endpoint and `routes_scanner.py` were removed. All its filter/sort params live on `/api/v1/arbitrage/flips` since iter 67 — use that.
23. **`proxyWithFallback` 5xx handling is mode-aware** (P2-8, iter 69): non-503 5xx (500/502/504) passes through unchanged in dev (`NODE_ENV === "development"`) so devs see the real error; in prod it becomes 200 + fallback data + `X-Flipper-Fallback: <original-status>` header. 503 (backend_offline/insufficient_data) still returns 200 + fallback in both modes (otherwise dev is unusable when backend is down). Use `isFlipperFallbackResponse(res)` / `getFlipperFallbackOriginalStatus(res)` to detect fallback responses from the frontend.

24. **Localized currency/item names live in `backend/data/currency_names.json`** (P2-3, iter 70). `currency_names_ru.py` is a 63-line thin loader — do NOT add names there. Public API unchanged: `CATEGORY_NAMES_RU` / `CATEGORY_NAMES_EN` / `CURRENCY_NAMES_RU` / `CURRENCY_NAMES_EN` dicts + `get_ru_name` / `get_en_name` / `get_category_ru` / `get_category_en` helpers. TS-side mirror `src/lib/currency-names.ts` still exists as an offline fallback — keep both in sync. Regression tests in `tests/test_currency_names_ru.py` enforce RU/EN key parity (run them after every name edit).

25. **EventManager is thread-safe via `threading.RLock`** (P3-3, iter 71). All in-memory `_events` dict access (CRUD + read-side query interfaces like `is_event_active`, `get_event_score_penalty`, `list_events`, `_prune_expired`) is guarded by `self._lock`. The lock is **never held across an `await`** — SQLite writes (`write_event`, `delete_event`, `deactivate_event`, `clear_all_events`) are awaited OUTSIDE the lock. Multi-worker uvicorn still needs a shared external store (Redis/DB) for cross-process coordination; the lock only protects in-process concurrency.

26. **SnapshotManager atomic state swap** (P3-4, iter 71). `(snapshot, ts)` is wrapped in an immutable `@dataclass(frozen=True) _SnapshotState` and stored as a single `self._state` reference. Replacement is a single Python attribute assignment (atomic under the GIL), so a reader either sees the pre-refresh or post-refresh state — never a mixed (stale snapshot, fresh ts) pair. `_history_cache` and `_active_currencies` are guarded by a separate `_cache_lock` (NOT the asyncio lock — these are mutated from sync code paths inside `_refresh`).

27. **Storage Value tab** (F2, iter 74). The new tab at `src/components/dashboard/storage-value-tab.tsx` is a UI-only wrapper — it does NOT add any new backend route. It calls the existing `GET /api/v1/storage-value/{currency}` endpoint (via the existing `/api/flipper/storage-value/[currency]` proxy). The tab is lazy-loaded via `next/dynamic` (chunk only loads when user navigates to it) and wrapped in `<ErrorBoundary>` so a render error in the tab doesn't crash the whole dashboard. The tab is in `TAB_MAP` at index 9 (between `analyst` and `liquid-chain`) for keyboard-shortcut navigation. The tab trigger is in `dashboard-toolbar.tsx` (Gem icon). StorageValueResponse type extended with optional `totalCurrentValue` / `totalProjectedValue` / `totalNetValue` fields — these are returned by the backend (see `routes_storage_value.py` lines 134-137) but were missing from the TS type.

28. **Storage Value History endpoint** (F2 follow-up, iter 75). `GET /api/v1/storage-value/{currency}/history?days=30` returns a time-series of `price(currency) / price(mirror)` and `price(currency) / price(hinekora)` ratios. For each timestamp in the currency's price history, the backend finds the nearest mirror/hinekora price point within a **24h tolerance** (see `backend/economy/storage_value_history.py:_find_nearest_price`). Points beyond tolerance emit `mirror_price=None` / `hinekora_price=None` and corresponding `ratio_*=None` — the frontend chart renders gaps in the line for these points (using `M` instead of `L` in the SVG path). When ALL ratios are null (e.g. mirror/hinekora not traded in the current league), the chart shows a "no reference data" notice instead of an empty chart. The Next.js proxy lives at `src/app/api/flipper/storage-value/[currency]/history/route.ts` and returns an empty `points` array with `dataAvailable: false` when the backend is offline (no 503).

29. **Content Pulse endpoint** (F3, iter 75). `GET /api/v1/content-pulse` returns per-category trade volume + 7d/30d rolling deltas + top movers. The heavy lifting is in `backend/economy/content_pulse.py:compute_content_pulse()` — a **pure function** (no side effects, no I/O) that takes a `DataSnapshot` + `AppConfig` and returns a dict. The route handler in `routes_content_pulse.py` is a thin wrapper that fetches the snapshot and calls the function. This separation makes the logic testable without spinning up FastAPI. Signal thresholds are module-level constants (NOT in config.yaml): `SIGNAL_RISING_THRESHOLD_PCT=10.0`, `SIGNAL_FALLING_THRESHOLD_PCT=-10.0`. `delta_7d_pct` is `null` (not 0) when there's no historical data — the frontend should treat `null` as "no signal yet" rather than "stable". Categories with no items in the snapshot still emit a row (with `item_count: 0`) so the UI can render "0 items / no data" rather than hiding the category entirely.

30. **Content Pulse widget wiring** (F4, iter 76). The widget at `src/components/dashboard/content-pulse-widget.tsx` is mounted FIRST inside `overview-tab-content.tsx` — ABOVE MarketOverview — so users see actionable farming signals on first dashboard load. The widget is wrapped in its own `<ErrorBoundary fallbackTitle={t("fallbackContentPulse")}>` so a render failure in the widget doesn't blank out MarketOverview or ComparativeChart. The widget consumes `/api/flipper/content-pulse` (Next.js proxy → `/api/v1/content-pulse`) via `useQuery` with 60s staleTime (rolling 7d average changes slowly). `maxPerSide` prop (default 2) caps how many rising + falling categories are shown — keep this small to preserve the 1-glance UX per PRODUCT_VISION §3.6. The widget only surfaces categories with `signal="rising"` or `signal="falling"` (|delta_7d_pct| ≥ 10%); stable categories are filtered out as noise.

31. **Speculation tab wiring** (F5, iter 77). The tab at `src/components/dashboard/speculation-tab.tsx` is lazy-loaded via `next/dynamic` (chunk only loads when user navigates to it) and wrapped in `<ErrorBoundary fallbackTitle={t("fallbackSpeculation")}>` so a render error doesn't crash the whole dashboard. The tab is in `TAB_MAP` at index 9 (between `storage-value` at 8 and `liquid-chain` at 10) for keyboard-shortcut navigation. Tab trigger in `dashboard-toolbar.tsx` (Sparkles icon, between Gem and Droplets). The tab consumes `/api/flipper/speculation` (Next.js proxy → `/api/v1/speculation`) via `useQuery` with 30s staleTime. Query params: `days` (7/14/30/90, default 30), `limit=50`, `signal` (ALL/BUY/SELL/HOLD, default ALL). The proxy forwards all query params to the backend and returns empty `signals: []` + `dataAvailable: false` when the backend is offline (no 503). Z-score thresholds: BUY when z < -1.5, SELL when z > +1.5, HOLD when |z| ≤ 1.5 (inclusive boundaries → HOLD). Population std (ddof=0) used — minimum 2 valid price points required for non-null z-score. Items with std=0 (all prices identical) are excluded from the result list (no actionable signal).

32. **Phase-aware hints widget wiring** (F6, iter 78). The widget at `src/components/dashboard/phase-hints-widget.tsx` is mounted SECOND inside `overview-tab-content.tsx` — directly BELOW ContentPulseWidget and ABOVE MarketOverview — so users see phase-aware advisory context (Temporalis, skill gems 18-20 lvl, vault keys, etc.) alongside the live farming signals on first dashboard load. The widget is wrapped in its own `<ErrorBoundary fallbackTitle={t("fallbackPhaseHints")}>` so a render failure doesn't blank out ContentPulseWidget or MarketOverview. The widget consumes `/api/flipper/phase-hints` (Next.js proxy → `/api/v1/phase-hints`) via `useQuery` with **5min staleTime** — the league phase only changes once per day at most (when `days_since_reference` crosses the early/mid or mid/late boundary), so 5min is plenty. The backend route fetches the global `PhaseDetector` singleton via `get_phase_detector()` (from `backend/api/shared.py`), calls `detector.get_phase_info()` to get phase + days_since_reference + reference_currency, then forwards to the pure function `get_phase_hints()` in `backend/economy/phase_hints.py`. **Does NOT depend on DataSnapshot** — the hint table is hardcoded (`_PHASE_HINTS` dict in `phase_hints.py`) and always returns `data_available=True`. The only way `data_available` is false is if `get_phase_detector()` raises an exception (e.g. config.league.league_start_date is invalid). Phase badge color: early → emerald, mid → violet, late → amber, unknown → muted. Hint slugs (e.g. `mid-skill-gems-18-20`) are stable identifiers used in `data-testid` attributes — keep them stable across refactors so tests don't break.

33. **Speculation backtest is a SEPARATE endpoint** (F5 follow-up, iter 79 backend + iter 80 frontend UI). The backtest lives at `GET /api/v1/speculation/backtest` — NOT as a query-param mode on `/api/v1/speculation`. Rationale: a backtest iterates every item with enough price history and is significantly more expensive than the live signal computation. Keeping it as a separate route makes the cost opt-in — the live Speculation tab can still fetch `/api/v1/speculation` cheaply without triggering backtest logic. The pure function `backtest_speculation_signals()` lives in `backend/economy/speculation_backtest.py` (NOT `speculation.py`) and **reuses** `compute_zscore` (from `pricing.py`) + `_extract_prices` + `_signal_from_zscore` + the `Z_BUY_THRESHOLD` / `Z_SELL_THRESHOLD` / `MIN_SAMPLE_SIZE` constants from `speculation.py` — guarantees the backtest uses the same strategy as the live signal. The baseline window is **strictly BEFORE** the entry timestamp — entry price itself is NOT in the baseline (no signal leak). `TOLERANCE_HOURS=24` between target timestamp and nearest price log (matches `storage_value_history.py:_NEAREST_PRICE_TOLERANCE_HOURS`). Aggregates (`buy_stats` / `sell_stats` / `overall_stats`) are computed over ALL trades — the `limit` query param only narrows the per-trade `trades` list, NOT the stats. `unevaluated_count` counts items with an actionable signal (BUY/SELL) but no exit price within tolerance (e.g. holding period extends past the last observed price). Return sign convention: BUY → `(exit-entry)/entry*100` (profit when price rises); SELL → `(entry-exit)/entry*100` (profit when price falls — short-sale equivalent). HOLD signals never produce trades (no position taken) but ARE counted in `signal_breakdown.HOLD`. **Frontend UI (iter 80):** collapsible Backtest panel inside `speculation-tab.tsx` — mounted below the live signals list. **NOT autoload** — toggle button (default collapsed) gates `useQuery` via `enabled: showBacktest && backendOnline`. When collapsed, only the "Run backtest" button is visible and NO query fires. When expanded, fires `/api/flipper/speculation/backtest?eval_days_ago={evalDays}&holding_days={holdingDays}&lookback_days={lookbackDays}&limit=50&signal={parentSignalFilter}`. Parent's `signalFilter` is forwarded — if parent filters to BUY-only, the backtest also restricts to BUY trades. Next.js proxy at `src/app/api/flipper/speculation/backtest/route.ts` returns empty `trades: []` + zeroed stats blocks + `dataAvailable: false` when backend is offline (no 503).

34. **`useFlipperBackend` is the single source of truth for dashboard-level flipper status** (iter 81, Stage 1 of `useDashboardData` extraction). The three dashboard-level flipper queries (`/api/flipper/health`, `/api/flipper/phase`, `/api/flipper/events?active_only=true`) MUST go through `useFlipperBackend()` — NOT inline `useQuery` calls in `dashboard-page.tsx`. The hook returns: `flipperBackendOnline` (boolean — true when health is "ok" or "degraded"), `flipperUpstreamReachable` (boolean — true when `health.provider === "reachable"`), `flipperPhaseData` (header badge data), `activeEventsCount` (events button indicator), `flipperHealthData`/`flipperHealthPending`/`flipperHealthError` (raw health probe state, exposed for future loading indicators), `flipperEventsData` (raw events response). Phase and events queries are `enabled: flipperBackendOnline` — they do NOT fire when the backend is offline (matches pre-iter-81 behaviour). Query keys are `QUERY_KEYS.flipperHealth` / `flipperPhase` / `flipperEventsCount` (unchanged). **useDashboardData extraction COMPLETE in iter 84** (Stages 1-2 + 3a-3b all shipped iter 81-84; see invariants #35/#36/#37).

35. **`useRealmsAndLeagues` is the single source of truth for realm/league selection + realms/leagues queries** (iter 82, Stage 2 of `useDashboardData` extraction). The two dropdown-populating queries (`/api/poe2/realms`, `/api/poe2/leagues`) MUST go through `useRealmsAndLeagues()` — NOT inline `useQuery` calls in `dashboard-page.tsx`. The hook OWNS the `realm` and `league` useState so the auto-select useEffect (auto-pick first active league when none selected) can fire when `leagues` arrives. The hook returns: `realm` (string, default `"poe2"`), `setRealm` (also clears the league — matches prior inline behaviour), `league` (string, may be empty until auto-select runs), `setLeague` (persists to the Zustand store via `persistLeague`), `realms` / `leagues` (raw query data, undefined while loading), `realmsLoading` / `leaguesLoading`, `effectiveLeague` (string — falls back to active league, then first league, then `""`). The leagues query reuses Fix 5.4 (`defaultLeagueValue` from realms data to avoid a redundant `/Realms` request inside `getLeagues()`). Query keys are `QUERY_KEYS.realms` / `QUERY_KEYS.leagues` (unchanged). `setRealm` clears the league — same behaviour as the prior inline `setRealm(v); setLeague("")` call site. `setLeague` wraps `setLeagueLocal` + `persistLeague` — same wrapper that lived inline in `dashboard-page.tsx` before iter 82. **useDashboardData extraction COMPLETE in iter 84** (Stages 1-2 + 3a-3b all shipped iter 81-84; see invariants #34/#36/#37).

36. **`useFilteredExchangePairs` + `useItemCategoryLists` are the single source of truth for the Exchange tab filter pipeline + currency/unique category-chip list derivation** (iter 83, Stage 3a of `useDashboardData` extraction). Both hooks are PURE — no queries, no store reads, no useState. (a) `useFilteredExchangePairs({ exchangeData, search, exchangeUiState })` applies the search → quick-chip → extended-numeric-filter pipeline to the raw `exchangeData` and returns a derived `ExchangePair[]`. Inputs are owned by the parent: `exchangeData` comes from `useExchangePairs()`, `search` from the toolbar's `useState`, `exchangeUiState` is the `uiState.exchange` slice from `useDashboardStore()`. Filter rules match the prior inline `useMemo` exactly (case-insensitive search on either currency name; `topVolume` chip = top 20 by volume desc, applied AFTER search so chip respects search context; `favorites` chip = pairs in `exchangeUiState.favorites`; extended `minVolume`/`maxVolume` inclusive; `minChange`/`maxChange` ignored when 0; `changePercent` treated as -∞/+∞ when null). Inline `useMemo` re-implementing this pipeline in `dashboard-page.tsx` is FORBIDDEN. (b) `useItemCategoryLists({ uniqueCategories, t })` derives `currencyCategories` + `uniqueCategoriesList` (the two chip-strip arrays for the Currencies and Uniques tabs) from a single `useItemCategories()` response. Currency list = `name !== "Unique"`; unique list = `name === "Unique"` OR substring match on `Unique`/`Armour`/`Weapon`/`Accessory`/`Flask`/`Jewel`/`Gem`. Both lists push `{ name: "all", displayName: t("all"), count: 0 }` when empty so the chip strip always renders something. Inline `useMemo` re-implementing these filter rules in `dashboard-page.tsx` is FORBIDDEN. **Stage 3b shipped iter 84** (see invariant #37) — useDashboardData extraction COMPLETE.

37. **`useOptimalPayment` is the single source of truth for the §11 optimal-payment cluster** (iter 84, Stage 3b of `useDashboardData` extraction — COMPLETE). The hook owns the entire optimal-payment pipeline that previously lived inline in `dashboard-page.tsx` (4 blocks, ~138 lines). (1) `optimalCurrencyData` useQuery — `queryKey: [QUERY_KEYS.flipperOptimalCurrency]`, `queryFn: fetchApi<OptimalCurrencyResponse>("/api/flipper/optimal-currency")`, `enabled: flipperBackendOnline`, `staleTime: 60_000`, `refetchInterval: 60_000`, `retry: 1` (UNCHANGED from prior inline call). (2) `clientOptimalResult` memo — client-side fallback. Depends on `exchangeData` + `crossRates.relativePriceMap` + `crossRates.anchorId` + `crossRates.anchorRelPrice` + `crossRates.crossRateFlips` + `findOptimalPayment` + `isItemCategory` (both pure functions from `@/lib/currency-optimal`). Two-pass computation: (a) group pairs by `currency1Id` for currency-vs-currency pricing; (b) group pairs by `currency1CategoryApiId` for item-aware optimal payment (Omens, Soul Cores — items appear as `currency1` with payment currencies as `currency2`). Both passes require ≥2 pricing options per group — `findOptimalPayment` returns null for groups with <2 options. Deps array: `[exchangeData, crossRates.relativePriceMap, crossRates.anchorId, crossRates.anchorRelPrice, crossRates.crossRateFlips]` (UNCHANGED). (3) Backend/client merge memo — backend data takes priority when `optimalCurrencyData?.dataAvailable && optimalCurrencyData.optimalPaymentByPair`. When the backend path is used, the response's `"currencyFrom_currencyTo"` keys are remapped to frontend `pair.id` so downstream components can look up results by pair ID. Falls back to `clientOptimalResult` otherwise. Returns `{ optimalPaymentByPair, crossRateFlips, anchorId: selectedAnchorId }`. Deps array: `[optimalCurrencyData, exchangeData, clientOptimalResult]` (UNCHANGED). (4) `optimalPaymentByDisplayName` memo — display-name-keyed `Map<string, OptimalPaymentResult>` (key format: `"currency1Name/currency2Name"`) consumed by FlipsTab. Deps array: `[exchangeData, optimalPaymentByPair]` (UNCHANGED). The hook takes `{ exchangeData, crossRates, flipperBackendOnline }` (all owned by the parent — `exchangeData` from `useExchangePairs()`, `crossRates` from `useCrossRates()`, `flipperBackendOnline` from `useFlipperBackend()`) and returns `{ optimalPaymentByPair, crossRateFlips, selectedAnchorId, optimalPaymentByDisplayName }`. Inline `useQuery` for `/api/flipper/optimal-currency` OR inline `useMemo` re-implementing the client fallback / merge / byDisplayName logic in `dashboard-page.tsx` is FORBIDDEN — use the hook. Stage 3b was the highest interdependency risk in the entire extraction plan (merge memo consumes both the useQuery result AND the clientOptimalResult memo; byDisplayName memo consumes the merge memo's output) — a single hook suffices because the pipeline is internally linear (no need for the 3b-i / 3b-ii split suggested in iter 83's hand-off). With Stage 3b shipped, the `useDashboardData` extraction is COMPLETE; `dashboard-page.tsx` is now 995 lines (was 1128 in iter 83, was 1685 in iter 70).

38. **`scripts/sync_currency_names_from_poe2db.py` is the ONLY sanctioned path for bulk-extends of `backend/data/currency_names.json` with new RU translations** (iter 85, F1 enabler; iter 86 live-run verified + F1 CLOSED). The script implements a 4-stage pipeline: `--fetch-ids` (poe2scout.com API enumeration) → `--fetch-ru` (poe2db.tw/ru/ HTML scrape) → `--diff` (writes `scripts/.cache/currency_names_patch.json`) → `--apply --confirm` (atomic write to `currency_names.json`). The script MUST be run from a non-RU IP (both poe2scout.com and poe2db.tw are blocked from Russia per `docs/CORS_PROXY_GUIDE.md`). The `--from-cache-snapshot` flag is a fallback for maintainers without poe2scout access — it extracts the 138 api_ids already in `src/data/cache-snapshot.json` (no network). The script is IDEMPOTENT (re-running --apply with the same patch is a no-op) and NEVER overwrites existing translations — conflicts (where an api_id already has a RU name but poe2scout now reports a different EN name) go into the patch with `action: "conflict"` for manual review. After --apply, the maintainer MUST bump the count assertions in `tests/test_currency_names_ru.py` (lines 30-33) ONLY IF new keys were added, then run `pytest tests/test_currency_names_ru.py`. **Iter 86 live-run results:** 639 poe2scout items enumerated, 186 EN→RU pairs scraped (8 of 17 categories returned data), 297 already translated, 0 new candidates matched (poe2db.tw has no pages for the missing items), 1 conflict (`against-the-darkness` — fixed inline by aligning EN to "Zarokh's Reliquary Key: Against the Darkness" to match the existing RU translation + the sibling `temporalis` entry pattern), 342 no-match. Final counts unchanged: ru=349, en=349. The 342 no-match items remain untranslated — poe2db.tw simply doesn't have pages for them. Re-running the script in the future will pick up any new translations poe2db adds. **For one-off manual edits** (rare — e.g. iter 86 `against-the-darkness` EN alignment), edit `backend/data/currency_names.json` + the TS mirror `src/lib/currency-names.ts` directly and run `pytest tests/test_currency_names_ru.py`. **CRITICAL `normalize_api_id` invariant**: the script's `normalize_api_id` PRESERVES hyphens (matches `currency_names.json` key convention — 315 of 349 keys use hyphens, 0 use underscores). This is DIFFERENT from the backend's `_normalize_api_id` in `poe2scout.py:58` which replaces hyphens with underscores for exchange-pair matching. Don't confuse the two — they normalize for DIFFERENT purposes (translation lookup vs exchange-pair matching). 32 pytest tests in `tests/test_sync_currency_names.py` cover the parser, diff logic, apply logic, idempotency, RU/EN key parity, and CLI argument validation.

39. **i18n leakage fix pattern — `getCurrencyDisplayName(apiId, locale)` + `getCategoryDisplayName(slug, locale)` from `src/lib/currency-names.ts`** (iter 87). When rendering any currency name or POE2Scout category slug in a dashboard component, use these helpers — they look up the localized name in `CURRENCY_NAMES_RU` / `CURRENCY_NAMES_EN` (349 currencies) and `CATEGORY_NAMES_RU` / `CATEGORY_NAMES_EN` (17 categories) mirror tables. **CRITICAL contract:** `getCurrencyDisplayName()` returns `string | null` (NOT `string`) — it returns `null` when no mapping exists so callers can fall back to upstream `text` from the API via `||` (`getCurrencyDisplayName(apiId, locale) || upstreamText`). `getCategoryDisplayName()` returns `string` (slug as fallback — categories are a small fixed set, no upstream text). Pull `locale` from `useI18n().locale` (NOT a hardcoded `"en-US"` string). **Date formatting invariant:** inline `toLocaleDateString("en-US", ...)` calls are FORBIDDEN in chart components — map the active `locale` to a BCP-47 tag (`ru → ru-RU`, `zh → zh-CN`, `ko → ko-KR`, fallback `en-US`) and pass to `toLocaleDateString()` / `toLocaleTimeString()`. See `src/components/dashboard/events-sidebar.tsx:formatCreatedAt` (iter 87) for the canonical pattern. **Backend `?lang=` param pattern:** for endpoints that return hardcoded advisory text (phase hints, analyst facts), add `lang: str = Query("en", ...)` to the FastAPI route, keep a parallel `_XXX_RU` table with the SAME `id`/`category` slugs (so the frontend can swap tables by locale without breaking tests that pin slugs). The Next.js proxy route reads `lang` from the incoming query string and forwards it. The frontend widget pulls `locale` from `useI18n()`, maps to `lang`, includes it in `queryKey` (so locale switch triggers refetch), passes as query param to `fetchApi`. **Phase hints widget fires 2 fetches on mount** — this is correct: on mount the default locale ("ru") triggers fetch 1, then after hydration the stored locale (e.g. "en") triggers fetch 2. Tests should assert `expect.objectContaining({ lang: expect.stringMatching(/^(ru|en)$/) })` instead of pinning exact call count. 7 components wired in iter 87: `market-overview.tsx`, `currency-card.tsx`, `exchange-pair-card.tsx`, `exchange-table.tsx`, `content-pulse-widget.tsx`, `speculation-tab.tsx`, `analyst-tab.tsx`. Backend modules: `phase_hints.py` (+ `_PHASE_HINTS_RU` + `_PHASE_META_RU` + `lang` param to `get_phase_hints()`), `routes_phase_hints.py` (+ `lang` Query param). Next.js route: `phase-hints/route.ts` (forwards `lang`). Frontend widget: `phase-hints-widget.tsx` (forwards `lang` from `useI18n().locale`).

40. **iter 88 patterns — `formatLocaleDate` helper + analyst fact `template_id` + Speculation `/flips` join + Flips tab relabel** (iter 88). (a) **Date formatting helper:** `formatLocaleDate(value, locale, opts?)` + `formatLocaleDateTime(value, locale)` + `localeToBcp47(locale)` live in `src/lib/utils.ts`. They replace the per-component `toLocaleDateString("en-US", ...)` pattern from iter 87 with a single shared helper. 8 chart components migrated in iter 88: `comparative-chart.tsx`, `market-overview.tsx`, `comparison-dialog.tsx`, `detail-dialog.tsx`, `pair-comparison-dialog.tsx`, `pair-detail-dialog.tsx`, `watchlist-tab.tsx`, `storage-value-history-chart.tsx`. `events-sidebar.tsx` refactored to use the shared helper (same behaviour). All new chart components MUST use `formatLocaleDate` / `formatLocaleDateTime` — inline `toLocaleDateString("en-US", ...)` is FORBIDDEN. (b) **Analyst fact templates (KI-5):** backend `_generate_facts` in `routes_analyst.py` now emits `template_id` + `params` alongside the English `text` field on each fact dict. `FactData` in `response_models.py` extended with optional `template_id: str | None` + `params: dict[str, Any]`. The Next.js fallback route `analyst-fallback/route.ts` mirrors this. Frontend `analyst-tab.tsx` has a `TEMPLATE_ID_TO_I18N_KEY` map (snake_case → camelCase i18n key) + `formatFactText(fact, t, locale)` function that formats the fact via i18n template when `template_id` is present, falls back to `text` otherwise. Currency params are localized via `getCurrencyDisplayName(apiId, locale)`. 5 templates: `biggest_gainer`, `biggest_loser`, `anomaly_activity`, `tracking`, `stable_count`. Backward compatible — old clients fall back to `text`. To add a new template: backend adds entry to `_generate_facts` with `template_id` + `params`, frontend adds mapping to `TEMPLATE_ID_TO_I18N_KEY` + case to `formatFactText` + i18n key to all 4 locale files. 7 pytest tests in `tests/e2e/test_analyst.py::TestGenerateFactsTemplateId`. (c) **Speculation `/flips` join (KI-1):** `speculation-tab.tsx` now fires a parallel `useQuery` for `/api/flipper/flips` (60s staleTime, gated on `backendOnline`). Builds a `flipsByApiId: Map<string, FlipOpportunity>` lookup keyed by the FROM currency (first part of `FlipOpportunity.currency`, e.g. "divine" from "divine/exalted"). When multiple flips exist for the same from-currency, the highest-scored one wins. `SignalRow` accepts an optional `flip?: FlipOpportunity` prop — when present, renders an expandable "Spread Details" toggle button next to the sparkline. Expanded panel shows synthetic bid/ask/spread/mid + fair cross-rate + deviation + 24h volume + disclaimer. The join is best-effort since `/flips` indexes on PAIRS while `/speculation` indexes on ITEMS — some signals will not have a matching flip. 7 jest tests in `speculation-tab.test.tsx` cover toggle visibility, expand/collapse, value rendering, highest-scored-flip selection. (d) **Flips tab relabel (KI-4):** `tabFlips` i18n key changed from "Flips" / "Флипы" / "翻转" / "플립" to "Cross-rate Deviations" / "Отклонения кросс-курса" / "交叉汇率偏差" / "교차 환율 편차" in all 4 locales. Disclaimer banner (`arbitrageTheoretical` + `arbitrageTheoreticalDesc`) rewritten to clarify: tab shows cross-rate deviations, NOT arbitrage; PoE2 has no order book; deviations signal where a different payment currency could save money. (e) **Premium column tooltip (KI-3):** `exchange-table.tsx` Premium column header now has an Info icon + tooltip (`crossCurrencyPremiumTitle` + `crossCurrencyPremiumDesc` i18n keys) explaining what "Premium" means. The `—` cell in `CrossCurrencyPremiumCell` also has a tooltip (`crossCurrencyPremiumEmpty`) explaining no premium data is available. (f) **7d Change column tooltip (KI-2):** investigation confirmed `sevenDayChangePercent` is frontend-computed in `poe2api.ts:compute7dChangePercent()` and returns null by design on new leagues (<2 PriceLogs OR closest 7d-ago log drifts >16.8h OR 7d-ago price is 0). Added tooltip on column header (`change7dDesc`) + on `—` cell (`change7dEmpty`) explaining this. NOT a bug — by design.

41. **iter 89 patterns — shortcuts dialog `TAB_MAP` sync + dead i18n key cleanup workflow** (iter 89). (a) **Shortcuts dialog `TAB_MAP` sync (KI-6):** the keyboard shortcuts dialog (`src/components/dashboard/shortcuts-dialog.tsx`) MUST stay in sync with `TAB_MAP` in `src/components/dashboard/dashboard-page.tsx`. The mapping is `1..9` → indices 0..8 + `0` → index 9 (10 total). If you add/remove a tab in `TAB_MAP`, you MUST also update the `<kbd>N</kbd> {t("tabXxx")}` rows in `shortcuts-dialog.tsx` to match — otherwise the dialog misinforms users. **Pre-existing limitation (NOT a bug):** `TAB_MAP` currently has 13 entries (overview, currencies, uniques, exchange, arbitrage, flips, optimizer, analyst, storage-value, speculation, liquid-chain, graph [dead], watchlist), but shortcuts only cover indices 0–9. Tabs at indices ≥10 (`liquid-chain`, `watchlist`) are NOT reachable via keyboard shortcuts — users must click them. To make them keyboard-reachable, either reorder `TAB_MAP` (place them earlier) or extend `use-keyboard-shortcuts.ts` to support additional key bindings (e.g. `Shift+1`..`Shift+9` for indices 10–18). (b) **Dead i18n key cleanup workflow:** when removing a tab/dialog/feature, the corresponding `tabXxx`, `fallbackXxx`, and any feature-specific i18n keys become dead. Remove them from ALL 4 locale files (`en.ts`, `ru.ts`, `zh.ts`, `ko.ts`) in the same iter — do NOT leave them as "harmless dead weight" (the iter 87 → iter 89 gap showed this accumulates: 30 dead keys × 4 locales = 120 dead lines, ~3.5KB per locale). After removal, leave a comment marker in place of the old section header (e.g. `// ---- Currency Graph Tab ---- REMOVED iter 89 (tab removed iter 87, keys were dead)`) so future agents searching for the section know it was intentionally deleted. **Before removing a key:** grep the entire `src/` tree (not just the obvious component) — some keys are referenced indirectly (e.g. `fallbackItemDetails` was used in `dashboard-dialogs.tsx`, NOT in `dashboard-page.tsx`). The cleanup script at `/home/z/my-project/scripts/cleanup_dead_i18n_keys.py` (iter 89) is the canonical tool — re-run it whenever a tab/dialog/feature is removed. (c) **`store.ts:validTabs` migration safety:** `src/lib/store.ts:validTabs` (lines 148-152) includes dead tab IDs (`forecast`, `portfolio`, `graph`) INTENTIONALLY — this is migration safety so users with old localStorage entries don't lose their state on tab ID rename/removal. DO NOT remove dead tab IDs from `validTabs` unless you're certain no production user has a localStorage entry pointing to them (typically: wait 2-3 iterations after the tab removal before cleaning up `validTabs`).

42. **iter 91 recon patterns — POE2Scout API capability map + iter 90 critique + refined iter 91+ roadmap** (iter 91). Pure recon iter — NO code changes, NO tests, NO archive, NO git commits. Documentation only (STATUS.md + AGENT_NAVIGATION.md + worklog.md). (a) **POE2Scout `relative_price` semantics (iter 90 ERROR #1):** `relative_price` is a per-currency field expressed in BASE currency (Exalted for PoE2), NOT per-pair. Each `SnapshotPair` contains `currency_one_data.relative_price` and `currency_two_data.relative_price` — both are "value of 1 unit of this currency in Exalted". Cross-rate between two currencies = `from_rel / to_rel`. iter 90 incorrectly described this as "per-pair relative price". (b) **`current_quantity` vs `volume_traded` (iter 90 ERROR #2):** these are DIFFERENT metrics. `current_quantity` (CurrencyItemExtended.current_quantity) = listings snapshot count right now (supply). `volume_traded` (PairDataDetails.volume_traded) = sum of trades in last 24h (activity). For "market activity" / "overheat" metrics use `volume_traded`. `content_pulse.py:_category_today_volume` currently uses `current_quantity` (TD-2) — must be fixed when implementing Overheat Index. (c) **`find_price_24h_ago` already exists** (iter 90 ERROR #3): in `backend/economy/pricing.py`. Used by `routes_analyst.py` for 24h change %. Adding `volume_24h_delta_pct` to `/content-pulse` is a 10-line change, not a new endpoint. (d) **Triangular `executable_estimate` requires persistence first** (iter 90 ERROR #4): cannot build "fast/medium/slow" formula without backtesting. Must add SQLite persistence `(cycle_hash, timestamp, profit_pct, snapshot_id)` + 30-min-later re-check (TD-3, iter 96) BEFORE attempting executable_estimate. (e) **`CURRENCY_DISPLAY_NAMES` hardcode in `optimizer-tab.tsx` is intentional fallback** (iter 90 ERROR #5): `getCurrencyDisplayName` returns `null` for unmapped currencies; the hardcode prevents UI from falling back to raw `api_id`. Not a duplication bug. (f) **POE2Scout API endpoints (12 total)** — see STATUS.md Quick Reference "Need to check what data POE2Scout API actually returns" row for summary. Full list in `backend/data/providers/poe2scout.py`: `get_leagues`, `get_realms`, `get_reference_currencies`, `get_exchange_rates` (SnapshotPairs), `get_snapshot_history`, `get_currency_metadata` (ByCategory), `get_historical_prices` (single currency), `get_pair_history`, `get_bulk_price_histories` (all items), `get_daily_stats` (OHLCV), `get_price_history` (per-item, LogCount must be multiple of 4), `get_all_currencies_with_prices`. (g) **Free-data fields already collected by DataSnapshot** (every 5 min, ~16 API calls): `relative_price`, `volume_traded`, `stock_value`, `highest_stock`, `current_price`, `current_quantity`, `price_logs[]` (Time/Price/Quantity). 8 fields — most are NOT surfaced in UI (TD-1, TD-6, TD-7, TD-8). (h) **NOT available from POE2Scout:** real trade count, order book depth/levels, individual bid/ask per order, seller identifiers (only `highest_stock`), real-time push, GGG official trade API latency. All "bid/ask" in our backend are SYNTHETIC — estimated from `volume_traded` via formula in `poe2scout.py:378-382`. (i) **5 new Proposals F-J** (data already collected, just need exposition): F=Wall detection (`highest_stock/current_quantity > 0.3`), G=OHLCV from `DailyStatsHistory` (already an endpoint, just not used), H=Cross-pair volume correlation (from existing `price_logs`), I=Spread persistence (add `market_spread` to HistoricalStore), J=Liquidity-tier UI (`tiers.py` already computes T1-T5 per currency). (j) **Refined iter 91-97 plan** — see STATUS.md iter 91 row + worklog iter 91 entry. Key changes vs iter 90 plan: iter 91 adds `validTabs` cleanup (iter 90 forgot); iter 95 Overheat Index uses `volume_traded` not `current_quantity` (iter 90 had wrong formula); iter 96 triangular persistence is PREREQUISITE for executable_estimate (iter 90 wanted to skip this); iter 97+ adds Proposal F-J (iter 90 didn't see these opportunities).

43. **iter 93 — Best Payment primary view on Exchange** (iter 93). Implements user's Q1 ("Пример А" — Exchange opens with top-10 "Pay in Divine, save 47% vs Chaos"), Q2 ("для всего" — covers currencies + uniques + craft items), Q3 ("скрывать" — savings <1% not shown). (a) **New component `src/components/dashboard/best-payment-top-list.tsx`:** horizontal-scroll cards strip rendered at the TOP of the Exchange tab (ABOVE the filter chips/table), inside `<ErrorBoundary fallbackTitle={t("fallbackBestPayment")}>` so a bug in the new component never blanks the whole tab. Each card shows: item name (localized via `getCurrencyDisplayName`), "craft" badge when `isItemCategory(categoryApiId)` is true, savings badge (color-coded: emerald ≥10%, amber ≥3%, muted otherwise), "Pay in X · vs Y · save Z anchor" line, click → opens representative pair's detail dialog. The representative pair is the pair whose `currency2Id === result.bestCurrencyId` — clicking the card lands the user on the actual cheapest-payment trade. Header has a count badge + collapse toggle (`bestPaymentHide`/`bestPaymentShow` i18n keys). Empty state: component returns `null` when `items.length === 0` — no header, no "no data" message; the Exchange tab stays compact. (b) **`useOptimalPayment` hook extended** with `bestPaymentTopList: BestPaymentTopListItem[]` — a NEW return value alongside `optimalPaymentByPair` / `crossRateFlips` / `selectedAnchorId` / `optimalPaymentByDisplayName`. Computed by a 5th `useMemo` block: groups `exchangeData` by `currency1Id` (covers both pure currencies AND craft items — currency1Id is unique per item, regardless of category), looks up the `OptimalPaymentResult` from any pair in the group (all pairs in a group share the same result), filters `savingsPct < BEST_PAYMENT_MIN_SAVINGS_PCT` (1%) — Q3, sorts by `savingsPct desc` then `volume desc` (tiebreak), takes top `BEST_PAYMENT_TOP_LIMIT` (10) — Q1. Both constants exported from the hook file so tests / future agents can pin them. **The hook's other 4 outputs are UNCHANGED** — verified by 412/412 jest baseline. (c) **`ExchangeTabContent` props extended** with `bestPaymentTopList: BestPaymentTopListItem[]` — passed in by `dashboard-page.tsx` from the `useOptimalPayment` destructure (added one line `bestPaymentTopList,` to the destructure). The component renders `<BestPaymentTopList>` inside the main `else` branch (after loading/error/empty states resolve) so it appears once real data is present. (d) **`dashboard-page.tsx` wiring:** one new destructure line + one new prop pass — no other changes. (e) **i18n keys (13 new × 4 locales = 52 new lines):** `bestPaymentTitle`, `bestPaymentSubtitle`, `bestPaymentPayIn`, `bestPaymentVs`, `bestPaymentSave`, `bestPaymentHide`, `bestPaymentShow`, `bestPaymentCraftItem`, `bestPaymentCountTooltip`, `bestPaymentSavingsTooltip`, `bestPaymentCardAria` (template with `{item}/{best}/{worst}/{pct}` params), `fallbackBestPayment`. Added to `en.ts` / `ru.ts` / `zh.ts` / `ko.ts` in the "Cross-rate Flip Tooltip i18n" section. (f) **Q3 ("скрывать") was ALREADY implemented** in `best-payment-badge.tsx:40` (`if (result.savingsPct < 1) return null;`) — iter 93 just extends the same rule to the new top-list (via `BEST_PAYMENT_MIN_SAVINGS_PCT` constant in the hook). (g) **Found pre-existing KI-10** (NOT introduced by iter 93): duplicate `flipsBid` / `flipsAsk` i18n keys in all 4 locale files (introduced iter 92 when TD-1 relabeled "Bid"→"Buy at" / "Ask"→"Sell at" without deleting the old entries). Causes `tsc --noEmit` TS1117 errors. Documented in STATUS.md — fix in iter 94. (h) **No new tests added in iter 93** — the `bestPaymentTopList` memo is a pure derivation from `optimalPaymentByPair` (already covered by `currency-optimal.test.ts`) + `exchangeData` (already covered by `use-optimal-payment` integration paths). Jest baseline 412/412 green. A future iter should add direct tests for `BestPaymentTopList` rendering + the `bestPaymentTopList` memo's sort/filter edge cases (empty input, single group, ties). (i) **What this DOES NOT do:** the top-list is READ-ONLY — clicking a card opens the existing pair detail dialog. It does NOT add a "pay in X" button that triggers a trade (PoE2 has no trade API — see invariant #42(h)). It does NOT persist user preferences (collapse state is `useState`, resets on tab switch — could be promoted to the store in a future iter if users request it). It does NOT show anchor-currency savings in absolute chaos-equivalent (the `savingsAnchor` field uses the selected anchor, which is mirror > divine > exalted > chaos in priority order — see `selectAnchor` in `currency-optimal.ts`).

44. **iter 94 — KI-10 fix + Spread Capture view** (iter 94). Implements the Spread Capture view from iter 90 recon (Q4 colors/filter, Q5 sparkline, Q6 intuitive labels) and closes KI-10 (pre-existing duplicate i18n keys from iter 92). (a) **KI-10 fix:** Deleted the OLD `flipsBid` / `flipsAsk` entries (lines ~379-380 in each locale — the pre-iter-92 "Bid"/"Ask", "Покупка"/"Продажа", "买价"/"卖价", "매수"/"도" labels) from all 4 locale files. KEPT the NEW iter 92 entries (lines ~631-634 — "Buy at"/"Sell at" with tooltips). Result: `npx tsc --noEmit` now exits 0 (was 8 TS1117 errors). The OLD entries were introduced before iter 92; iter 92's TD-1 relabel added NEW entries without deleting the old ones, causing silent duplicate-key collisions where the second definition won. (b) **Q4 (Spread tier colors + filter):** Added `classifySpreadTier(spread)` + `spreadTierColor(tier)` pure helpers to `flips-helpers.ts`. Thresholds exported as constants: `SPREAD_TIER_WIDE_THRESHOLD = 0.05` (5%), `SPREAD_TIER_MEDIUM_THRESHOLD = 0.02` (2%). FlipsTable Spread cell now color-coded: emerald (wide), amber (medium), muted (tight). Added `spreadTierFilter` state to `flips-tab.tsx` + a new "Spread tier" Select dropdown in the filters row (between Cluster and Min Spread) with options All / Wide ≥5% / Medium 2-5% / Tight <2%. Filter logic added to `filteredOpportunities` useMemo + included in dep array. This is DIFFERENT from the existing numeric `minSpread` input — the tier filter is a quick picker for spread-capture use cases ("show me only wide-spread pairs worth capturing"). (c) **Q5 (Trend sparkline):** Added `deriveTrendSparklineData(momentum, volatility)` to `flips-helpers.ts`. Generates 6 deterministic synthetic points: linear slope from momentum + decaying wave from volatility. Formula: `trend = m * t`, `wave = v * sin(i*PI/2) * (1-t) * 0.5`, `point = trend + wave`. CRITICAL: uses `sin(i * PI/2)` NOT `sin(i * PI)` — the latter is always 0 for integer i (caught by unit test). Exported `FLIPS_TREND_SPARKLINE_POINTS = 6` constant. Added Trend column to FlipsTable (col 17 of 18, before detail arrow), hidden on smaller screens (lg+), uses existing `Sparkline` component (`./sparkline`) with width=60, height=20, color=#94a3b8 (slate-400, neutral). Expanded `GRID_COLS` from 18 to 19 tracks (added 70px between 60px and 30px detail arrow). Tooltip `flipsTrendTooltip` HONESTLY labels this as "Momentum × volatility indicator (derived from current snapshot — NOT historical price data). When backend adds priceHistoryShort (TD-9), this will switch to real recent price points." This is a CONSERVATIVE implementation: the column structure is in place, the sparkline shows meaningful shape from existing fields, and switching to real historical data later requires NO UI changes — only `flips-helpers.ts:deriveTrendSparklineData` needs to be replaced with a passthrough. (d) **Q6 (intuitive labels):** Added `flipperSpreadTooltip` i18n key in 4 locales explaining "Gross spread (ask − bid) / mid price. This is the profit margin per round-trip trade before fees. Wide ≥5% = strong spread-capture opportunity; Medium 2-5% = marginal; Tight <2% = skip." Added `title={t("profitExaTooltip")}` to the Profit column header (was missing — the tooltip text already existed). Added `title={t("flipperSpreadTooltip")}` to the Spread column header (was missing). (e) **i18n keys (7 new × 4 locales = 28 new lines):** `flipperSpreadTooltip`, `flipsSpreadTierFilter`, `flipsSpreadTierWide`, `flipsSpreadTierMedium`, `flipsSpreadTierTight`, `flipsTrend`, `flipsTrendTooltip`. Added to `en.ts` / `ru.ts` / `zh.ts` / `ko.ts` in a new "iter 94: Spread Capture view" section immediately after the iter 92 TD-1 section. (f) **Unit tests (16 new):** Created `src/__tests__/flips-helpers.test.ts` covering `classifySpreadTier` (5 tests: thresholds, defensive null, exported-threshold consistency), `spreadTierColor` (3 tests: tier-to-color mapping), `deriveTrendSparklineData` (8 tests: point count, determinism, zero inputs, positive/negative momentum slope, volatility wave behavior with sin(i*PI/2), null inputs, intermediate deviation). All 16 pass. Jest baseline now 428/428 green (was 412/412). (g) **New TD-9:** `FlipOpportunity` lacks `priceHistoryShort?: { timestamp: string; price: number }[]`. When backend adds it (per-pair recent price points, similar to `SpeculationSignal.priceHistoryShort`), the FlipsTable Trend column can switch from the derived `momentum × volatility` indicator to real recent price data. This requires NO UI changes — only `flips-helpers.ts:deriveTrendSparklineData` needs to be replaced with a passthrough that returns the `priceHistoryShort` array directly. (h) **What this DOES NOT do:** the Spread tier filter and Trend sparkline are READ-ONLY UI improvements. They do NOT trigger trades (PoE2 has no trade API — see invariant #42(h)). They do NOT persist user preferences (filter state is `useState`, resets on tab switch). The Trend sparkline is NOT a real price history — it's a derived indicator (clearly labeled). The Spread tier thresholds (5% / 2%) are NOT configurable — they're exported constants in `flips-helpers.ts` (could be promoted to settings in a future iter if users request different thresholds). (i) **What this DOES NOT address from iter 90 (deferred to iter 95+):** Q13 Overheat Index (indirect signals: streamer influence → volume spike → price drop) — needs TD-2 fix first (use `volume_traded` not `current_quantity` in `content_pulse._category_today_volume`). Triangular persistence (TD-3 + TD-4) — iter 96. Real FlipsTable sparkline (TD-9) — future iter when backend adds `priceHistoryShort`.

45. **iter 95 — TD-2 fix + Overheat Index (Q13)** (iter 95). Closes TD-2 (the longest-standing tech debt item from iter 91 recon) and implements the Overheat Index from iter 90 recon Q13 (indirect signals: streamer influence → volume spike → price drop). (a) **TD-2 fix (the critical correctness change):** `content_pulse._category_today_volume()` previously summed `CurrentQuantity` (CurrencyItemExtended.current_quantity — listings snapshot count right now, a SUPPLY metric). This was semantically inconsistent with `rolling_7d` / `rolling_30d` (which are derived from `price_logs[].Quantity` — an ACTIVITY metric). The mismatch meant `delta_7d_pct = (today_volume / rolling_7d - 1) * 100` compared SUPPLY to ACTIVITY — meaningless for overheat detection. Fix: introduced `_build_currency_volume_map(snapshot)` which iterates `snapshot.exchange_rates` (dict of `ExchangeRate` dataclass instances) and attributes each pair's `volume_traded` (24h ACTIVITY metric) to BOTH `currency_from` and `currency_to`. The map is passed to `_category_today_volume(items, volume_map)` which sums `volume_map[api_id.lower()]` across all items in the category. Items absent from the map (no exchange_rates data) contribute 0. The legacy fallback (sum `current_quantity`) is preserved ONLY when `volume_map is None` — used by isolated unit tests that don't model `exchange_rates`. Production code in `compute_content_pulse` always passes a real volume_map. (b) **Overheat Index computation:** Two new pure functions in `content_pulse.py`. `_category_price_change_pct(items)` — mean per-item % price change across items with ≥2 price points (uses existing `_price_trend_pct`). `_overheat_signal(volume_spike_ratio, price_change_pct)` — classifies as `hot` (volume spiking AND prices dropping) / `warm` (only one) / `cool` (neither or insufficient data). Thresholds: `OVERHEAT_VOLUME_SPIKE_THRESHOLD = 2.0` (today > 2x rolling 7d), `OVERHEAT_PRICE_DROP_THRESHOLD = -5.0` (mean price change < -5%). Both strict (>, <). `_overheat_index_score(volume_spike_ratio, price_change_pct)` — 0-100 composite score: `vol_component = clamp((volume_spike_ratio - 1) * 25, 0, 100)` + `price_component = clamp(-price_change_pct * 4, 0, 100)`, averaged. Returns 0.0 when either input is None. (c) **Backend response shape extended:** `ContentPulseCategoryData` (Pydantic in `response_models.py`) gains 4 new fields: `overheat_index` (float, default 0.0), `overheat_signal` (str, default "cool"), `volume_spike_ratio` (float | None, default None), `price_change_pct` (float | None, default None). Frontend `ContentPulseCategory` (TypeScript in `types.ts`) mirrors with camelCase: `overheatIndex`, `overheatSignal`, `volumeSpikeRatio`, `priceChangePct`. The proxy auto-transforms snake_case → camelCase via existing `transformKeys()` in `case-transform.ts` — no proxy changes needed. (d) **UI: Overheat badge on Content Pulse categories.** `content-pulse-widget.tsx:CategoryBlock` — when `category.overheatSignal === "hot"` OR `"warm"`, renders a Badge next to the existing 7d delta badge. Hot = orange ("Overheated" + Flame icon), warm = amber ("Warming up" + Flame icon). Cool = no badge. The badge is wrapped in `<span title={tooltip}>` so hover reveals the breakdown: `overheatIndex.toFixed(1)`, `volumeSpikeRatio.toFixed(2)`, `priceChangePct.toFixed(2)%`. The badge has `data-testid="content-pulse-overheat-badge-{category}"` for test selectors. (e) **i18n keys (4 new × 4 locales = 16 new lines):** `contentPulseOverheatBadge`, `contentPulseOverheatTooltip` (template with `{0}/{1}/{2}` = index/spike/price), `contentPulseOverheatWarmBadge`, `contentPulseOverheatWarmTooltip` (same template). Added to `en.ts` / `ru.ts` / `zh.ts` / `ko.ts` immediately after `contentPulseFetchedAt` in the F4 section. (f) **Tests — Python (38 new in `tests/test_content_pulse.py`):** Updated existing helpers `_make_currency` (adds optional `volume_traded` param) + `_make_snapshot` (auto-builds `exchange_rates` from currencies' `VolumeTraded` field when not explicitly provided) + new helper `_make_rate` (builds fake ExchangeRate). Updated 5 existing integration tests to use `volume_traded=` instead of `current_quantity=` (semantics changed). Added 4 new test classes: `TestBuildCurrencyVolumeMap` (6 tests: empty, no attr, single pair, multiple pairs sum, zero vol skipped, lowercased keys), `TestCategoryPriceChangePct` (5 tests: empty, <2 points, single trend, mean unweighted, partial), `TestOverheatSignal` (7 tests: insufficient data, hot, warm-only-vol, warm-only-price, cool, strict thresholds, constants exported), `TestOverheatIndexScore` (9 tests: insufficient, zero, vol component, price component, combined, caps, clamps). Added `TestComputeContentPulseOverheat` (7 integration tests: fields present, cool when no history, hot when spike+drop, warm when only spike, cool when normal, None when today_volume=0, TD-2 regression test verifying current_quantity=999 is IGNORED when volume_traded=200 is set). Python baseline 795/795 green (was 757/757 — but 44 content_pulse tests became 82). (g) **Tests — Jest (4 new in `src/__tests__/content-pulse-widget.test.tsx`):** Updated existing fixtures (`mixedResponse`, `allStableResponse`, `responseWithEmptyMovers`) with `...COOL_OVERHEAT` spread helper. Added 4 new tests: "does NOT render overheat badge when 'cool'", "renders 'Overheated' badge when 'hot'" (verifies badge text + tooltip contains index/spike/price), "renders 'Warming up' badge when 'warm'", "renders overheat badge alongside the 7d delta badge" (verifies both badges coexist). Jest baseline 432/432 green (was 428/428). `tsc --noEmit` still green (0 errors). (h) **What this DOES NOT do:** the overheat badge is READ-ONLY — it doesn't trigger trades or open a separate view. It does NOT persist user preferences. The thresholds (2.0x spike, -5% drop) are NOT configurable — they're module-level constants in `content_pulse.py` (could be promoted to config.yaml in a future iter). The price_change_pct is UNWEIGHTED mean (each item contributes equally regardless of trade volume — a future iter could weight by `volume_traded` if that proves more useful). The Overheat Index does NOT use streamer-specific signals (no Twitch API integration) — it's purely a market-data proxy for the post-streamer pattern. (i) **What this DOES NOT address from iter 90 (deferred to iter 96+):** Triangular arbitrage persistence (TD-3 + TD-4) — iter 96. Proposal F-J exposition (Wall detection UI, OHLCV candlestick, cross-pair correlation, liquidity-tier UI, real FlipsTable sparkline via TD-9) — iter 97+.

46. **iter 97 — Circuit Patterns API + UI wire-up (F7 / P8)** (iter 97). Closes TD-10 (the only open tech-debt item from iter 96). Implements the API + UI wire-up for the trajectory classification pure function `compute_circuit_patterns()` introduced in iter 96 (75 unit tests). (a) **Backend route:** `backend/api/routes_circuit_patterns.py` — thin wrapper by exact analogy with `routes_speculation.py`. Query params: `days` (1..90, default 30), `limit` (1..500, default 50), `trajectory` (regex `^(ALL|EXPONENTIAL_GROWTH|LINEAR_GROWTH|PEAK_THEN_DECLINE|MEAN_REVERTING|VOLATILE|DECLINING|STABLE)$`, default `ALL`). Returns `data_available=false` + empty `patterns` list when snapshot is not loaded OR when computation throws (try/except wraps `compute_circuit_patterns`). The route also injects `days` into the response dict — the pure function does NOT echo `days` back (only route handlers need it for client cache keys). Router registered in `backend/main.py` via the standard `try/except ImportError` wrapper (matches the convention used by every other feature router). (b) **Pydantic response models:** `CircuitPatternData` + `CircuitPatternsResponse` in `backend/api/response_models.py`. `CircuitPatternData` has 12 fields: `api_id`, `text`, `category`, `trajectory`, `total_change_pct`, `recent_slope_pct_per_day`, `volatility_cv`, `r_squared`, `days_since_peak` (int | None), `recommended_action`, `sample_size`, `current_price`, plus `price_history_short: list[SpeculationPriceHistoryPoint]` (iter 97 — reuses the existing model from Speculation to avoid duplicate type definitions). (c) **Pure function extension:** `compute_circuit_patterns()` in `backend/economy/circuit_patterns.py` was extended to also emit `price_history_short` per currency (up to 14 most-recent price points, oldest-first, as `{"date": iso, "price": float}` dicts). This is an **additive change** — existing 75 tests stay green because they assert specific fields, not exact shape. The field is needed for the UI mini-sparkline. (d) **Next.js proxy:** `src/app/api/flipper/circuit-patterns/route.ts` — by analogy with `speculation/route.ts`. Forwards `days` / `limit` / `trajectory` query params to the backend. Returns empty `patterns: []` + `dataAvailable: false` + the requested `days` echoed back when backend is offline (no 503). (e) **TypeScript types:** `CircuitPattern`, `CircuitPatternsResponse`, `CircuitTrajectory` (union of 7 string literals), `CircuitRecommendedAction` (union of 5 string literals) in `src/lib/types.ts`. Field names are camelCase after `transformKeys()` (e.g. `total_change_pct` → `totalChangePct`, `r_squared` → `rSquared`, `days_since_peak` → `daysSincePeak`). (f) **UI tab:** `src/components/dashboard/circuit-patterns-tab.tsx` — by analogy with `speculation-tab.tsx`, but simpler (no Backtest panel — trajectory classification is single-pass, no replay variant). Renders a sortable list (sorted by `|totalChangePct|` desc from backend) of currencies. Each row has: trajectory badge (color-coded — emerald for growth, amber for peak, red for declining, fuchsia for volatile, slate for stable/mean-reverting) + recommended_action badge (same color scheme) + item name (localized via `getCurrencyDisplayName`) + category (title-cased) + signed `total_change_pct` (green for positive, red for negative) + stats line (`sampleSize pts · slope X%/d · vol X · R² X · current X · peak Xd ago` for PEAK_THEN_DECLINE only) + mini-sparkline (dependency-free SVG, colored by trajectory archetype — same component pattern as speculation-tab). Filter chips at top: ALL + 7 archetypes (test-id suffixes: `circuit-filter-all` / `-exponential` / `-linear` / `-peak` / `-mean-rev` / `-volatile` / `-declining` / `-stable`). Days selector: 7/14/30/90 (default 30). Fetches via `useQuery` (30s staleTime, retry 1) bound to `/api/flipper/circuit-patterns?days={days}&limit=50&trajectory={filter}`. Graceful degradation: backendOffline → offline card with start-backend hint; loading → "Classifying trajectories..."; error → red notice + refresh; dataAvailable=false → "no price history yet"; empty patterns → "no actionable patterns in the current window". 20 jest tests in `src/__tests__/circuit-patterns-tab.test.tsx`. (g) **Tab wiring:** `dashboard-page.tsx` — added `CircuitPatternsTab` dynamic import (lazy-loaded with `TabSkeleton` fallback), added `"circuit-patterns"` to `TAB_MAP` at index 9 (between `speculation` and `liquid-chain` — extends the analytics cluster), added `<TabsContent value="circuit-patterns">` with `<ErrorBoundary fallbackTitle={t("fallbackCircuitPatterns")}>`. `dashboard-toolbar.tsx` — added `Activity` icon to lucide imports + `<TabsTrigger value="circuit-patterns">` between Speculation and Liquid Chain. `shortcuts-dialog.tsx` — updated the shortcut mapping: shortcut "0" → Circuits (was Liquid Chain pre-iter 97); Liquid Chain + Watchlist are now click-only (was already the case for Watchlist pre-iter 97). Comment block in `dashboard-page.tsx:TAB_MAP` documents the new mapping. (h) **i18n keys (47 new × 4 locales = 188 new lines):** `tabCircuitPatterns`, `fallbackCircuitPatterns`, `circuitTitle`, `circuitSubtitle` (template `{0}` = days), `circuitOffline`, `circuitOfflineHint`, `circuitLoading`, `circuitError`, `circuitNoData`, `circuitNoPatterns`, `circuitRefresh`, `circuitFetchedAt` (template `{0}` = timestamp), `circuitPatternCount` (template `{0}` = count), `circuitFilterLabel`, `circuitFilterAll` + 7 archetype filter labels (`circuitFilterExpGrowth` etc.), `circuitDaysLabel`, `circuitDaysValue` (template `{0}` = days), 6 tooltip keys (`circuitTotalChangeTitle` etc.), 7 trajectory label keys (`circuitTrajExpGrowth` etc.), 5 action label keys (`circuitActionHoldForGrowth` etc.), 6 stat-format keys (`circuitSampleSize` etc.). Added to all 4 locale files (en/ru/zh/ko) at the end of the existing F6 (Phase hints) section. All 4 locales have parity (47 keys each, total locale file count = 1011 keys each). (i) **Tests — Python (4 new in `tests/test_circuit_patterns.py::TestRouteHandler`):** `test_route_returns_empty_when_no_snapshot`, `test_route_returns_data_when_snapshot_available`, `test_route_returns_empty_on_exception`, `test_route_echoes_days_param`. Mirror the pattern in `test_content_pulse.py::TestRouteHandler`. Python baseline in test_circuit_patterns.py: 79/79 green (was 75/75). (j) **Tests — Jest (20 new in `src/__tests__/circuit-patterns-tab.test.tsx`):** offline / loading / error / no-data / no-patterns / patterns-list-rendering / trajectory-badges / action-badges / total-change-pct-signed / filter-chips-8 / days-selector / sparkline-non-empty / sparkline-empty-fallback / pattern-count-footer / fetched-at / proxy-path / filter-click / stats-line / days-since-peak-only-for-peak / all-5-actions-rendered. Jest baseline: 452/452 green (was 432/432). (k) **What this DOES NOT do:** the tab is READ-ONLY — no trade actions, no persistence of user filter preferences (filter state is `useState`, resets on tab switch). Sparkline is real backend data (`priceHistoryShort`), NOT a derived indicator (unlike FlipsTable's trend sparkline which is `momentum × volatility` per TD-9). The trajectory classification thresholds (`MIN_SAMPLE_SIZE=4`, `MIN_R_SQUARED=0.7`, etc.) are NOT configurable — they're module-level constants in `circuit_patterns.py` (could be promoted to config.yaml in a future iter). The trajectory filter is AND-filter — when set to a specific archetype, only currencies with that archetype appear (no "show me everything EXCEPT STABLE" negation filter). (l) **What this DOES NOT address from iter 90+ recon (deferred to iter 98+):** P4 Time-of-day pattern detector (intraday heatmap) — iter 98. P5 Weekday/weekend pattern detector — iter 99. P3 Leveling uniques lifecycle widget — iter 100. P7 Mirror/Divine arb detector — iter 101. P9 Phase-aware investment advisor (dynamic, live-prices) — iter 102. P10 Gold Map ROI calculator — iter 103. See `docs/MARKET_PLAYBOOK.md` §C.3-C.8 for the full roadmap. (m) **Keyboard shortcut remapping is a behavioral change:** pre-iter 97, shortcut "0" → Liquid Chain (idx 9 in TAB_MAP). Post-iter 97, shortcut "0" → Circuits (new idx 9 in TAB_MAP), and Liquid Chain moves to idx 10 (no keyboard shortcut — click only). Documented in `shortcuts-dialog.tsx` comment block and in STATUS.md "Quick Reference — Frequent Problems" table.

## 4. Known Issues

iter 91 recon found **3 new Known Issues** (KI-7/8/9) and **8 new Tech Debt items** (TD-1 through TD-8) — all documented in `STATUS.md`. **All KI-7/8/9, KI-10, TD-1, and TD-2 were CLOSED in iter 92/94/95.** Quick summary of what was fixed:

- **KI-7** ✅ Closed iter 92 — Removed dead `"arbitrage"` and `"graph"` from TAB_MAP. Cleaned validTabs. Updated shortcuts-dialog. All 10 keyboard shortcuts now work.
- **KI-8** ✅ Closed iter 92 — Watchlist pnl now uses entry price tracking (`WatchlistEntry.entryPrice`, store v5). Real P&L = `(currentPrice - entryPrice) / entryPrice * 100`.
- **KI-9** ✅ Closed iter 92 — Cross-rate threshold unified to 7% (backend+frontend). `affectedCurrencies` truncated to top-5 + `"and N more"`.
- **KI-10** ✅ Closed iter 94 — Deleted old `flipsBid`/`flipsAsk` entries (lines ~379-380) from all 4 locale files. Kept new iter 92 entries with tooltips. `tsc --noEmit` now green.
- **TD-1** ✅ Closed iter 92 — FlipsTable now shows volume24h, bid, ask, deviationPct, fairRate. 4 new SortField types. Intuitive i18n labels in 4 locales.
- **TD-2** ✅ Closed iter 95 — `content_pulse._category_today_volume()` now uses `volume_traded` (activity) from `snapshot.exchange_rates` instead of `current_quantity` (supply). Semantically consistent with `rolling_7d`/`rolling_30d`. See invariant #45.
- **TD-3 through TD-9** — see `STATUS.md` Technical-debt backlog table. TD-9 (real FlipsTable sparkline data) added iter 94. Most are data already collected by DataSnapshot but not surfaced in UI.

All earlier P0-P4 issues remain closed (iter 54-73). All F1-F6 product features remain shipped (iter 74-89).

Quick reference for the most common symptoms:

| Symptom | Cause | STATUS.md ID |
|---------|-------|--------------|
| Adding a new Russian translation | (iter 85) Use `scripts/sync_currency_names_from_poe2db.py` from a non-RU IP. 4 stages: `--fetch-ids` → `--fetch-ru` → `--diff` → review patch → `--apply --confirm`. After --apply, bump count assertions in `tests/test_currency_names_ru.py` (lines 30-33). Manual edits to `currency_names.json` are still allowed for one-off fixes, but bulk imports MUST go through the script. | F1 (done iter 86) |
| Frontend shows fallback data without notice | check `X-Flipper-Fallback` header via `isFlipperFallbackResponse(res)` | — |
| `/scanner/scan` 404 | Endpoint deleted in iter 68 — use `/api/v1/arbitrage/flips` with the same params | — |
| Need to inspect circuit breaker state | `GET /api/flipper/health/circuit-breakers` | — |
| LightGBM skips for new currency | (Fixed iter 67 — adaptive fallback from `floor=5`) | — |
| `/optimizer/path` returns empty path with `data_available: true` | Profitable arbitrage cycle — fall back to `direct_rate` | — |
| Concurrent EventManager access raises `KeyError` / `dict changed size during iteration` | (Fixed iter 71) `threading.RLock` guards all in-memory `_events` access | — |
| `SnapshotManager.get_snapshot` returns stale snapshot paired with fresh ts | (Fixed iter 71) `(snapshot, ts)` wrapped in immutable `_SnapshotState` swapped atomically | — |
| `dashboard-page.tsx` is 995 lines | `useDashboardData` extraction COMPLETE (iter 81-84). dashboard-page.tsx is now legitimate parent wiring. No further staged refactors planned. | — |
| Storage Value tab shows "no price history" | Backend reachable but `price_histories[currency]` is empty. Try `divine` / `exalted` / `chaos` first. | F2 (done) |
| Storage Value history chart shows "no history" | Currency has <2 price points in last 30 days, OR all mirror/hinekora ratios are null. | F2 (done) |
| `/api/v1/content-pulse` returns `data_available: false` | Snapshot not loaded yet, OR no items in any configured category. | F3 (done) |
| Content Pulse `delta_7d_pct` is `null` | No historical price_logs for any item in that category — only today's volume is known. Not a bug. | F3 (done) |
| Content Pulse widget shows "no signals today" | All categories have `signal="stable"` (|delta_7d_pct| < 10%). Correct behavior — widget only surfaces strong signals. | F4 (done) |
| Content Pulse widget shows "no movers" for a category | Category has signal but its items lack ≥2 price points to compute per-item trend. Will populate as scheduler collects more data. | F4 (done) |
| Content Pulse shows orange "Overheated" / amber "Warming up" badge | (iter 95, Q13) By design — Overheat Index = volume spike (today > 2x rolling 7d) AND price drop (< -5%) → "hot"; only one condition → "warm"; neither → "cool" (no badge). Tooltip shows the breakdown. | TD-2 (closed iter 95) |
| Content Pulse `today_volume` differs from pre-iter-95 values | (iter 95, TD-2 fix) Was `current_quantity` (supply metric), now `volume_traded` (activity metric, summed across all `snapshot.exchange_rates` pairs containing the currency). Semantically consistent with `rolling_7d`/`rolling_30d`. | TD-2 (closed iter 95) |
| `/api/v1/speculation` returns `data_available: false` | Snapshot not loaded, OR no item has ≥2 valid price points in the requested `days` window. | F5 (done) |
| Speculation tab shows "no actionable signals" | All items have `|z_score| < 1.5` — prices are within ±1.5σ of their recent mean. Try widening days (90 instead of 30). | F5 (done) |
| Speculation z-score is null for an item | Item has <2 valid price points, OR all prices are identical (std=0). Both → excluded from the result list. | F5 (done) |
| Speculation "Spread Details" toggle is missing for an item | (iter 88) No matching `FlipOpportunity` for that item's `api_id` in `/api/flipper/flips`. The join is by from-currency. Not a bug. | F5 (done) |
| `/api/v1/phase-hints` returns `data_available: false` | Only happens if PhaseDetector cannot be constructed (e.g. config.league.league_start_date is invalid). Otherwise always True — hint table is hardcoded. | F6 (done) |
| Phase hints widget shows wrong phase | Phase computed from `days_since_reference` since `league_start_datetime` (or last `major_patch` event). Check `config.yaml:league.league_start_date` matches actual league start. | F6 (done) |
| `/api/v1/speculation/backtest` returns `data_available: false` | Snapshot not loaded, OR no item has price_logs spanning both the eval timestamp and exit timestamp. Try widening `eval_days_ago`. | F5 (done) |
| Speculation backtest returns `evaluated_count=0` but `unevaluated_count>0` | Items have actionable signal at entry but no price log within 24h of exit — holding period extends past last observed price. Decrease `holding_days` or wait for more data. | F5 (done) |
| Speculation backtest `trades` list is shorter than `overall_stats.count` | The `limit` query param caps the per-trade list (default 50, max 500), but `*_stats` blocks are computed over ALL trades. If you need the full list, raise `limit`. | F5 (done) |
| Exchanges tab "7d Change" column shows "—" | (iter 88) Frontend computes `sevenDayChangePercent` from `PriceLogs`. Returns null when <2 PriceLogs OR closest 7d-ago log drifts >16.8h OR 7d-ago price is 0. Common on new leagues. Not a bug. Tooltip explains. | KI-2 (closed iter 88) |
| Premium column shows large % (50%+) | (iter 88) By design — shows how much market rate deviates from cross-rate-derived fair rate. Large % is normal for low-liquidity pairs and signals an opportunity. Tooltip explains. | KI-3 (closed iter 88) |
| Analyst tab `fact.text` shows English from backend | (iter 88, KI-5) Backend now sends `template_id` + `params`. Frontend formats via i18n keys. Old clients fall back to `text`. To add a new template: see invariant #40(b). | KI-5 (closed iter 88) |
| Date in chart / dialog shows English ("Jun 13") instead of locale | (iter 88) Use `formatLocaleDate(value, locale, opts?)` or `formatLocaleDateTime(value, locale)` from `src/lib/utils.ts`. Inline `toLocaleDateString("en-US", ...)` is FORBIDDEN — see invariant #40(a). | — |
| Shortcuts dialog shows wrong tab labels | (iter 92, KI-7 closed) `shortcuts-dialog.tsx` MUST stay in sync with `TAB_MAP` in `dashboard-page.tsx`. Current mapping: 1→Overview, 2→Currencies, 3→Uniques, 4→Exchange, 5→Flips, 6→Optimizer, 7→Analyst, 8→Storage Value, 9→Speculation, 0→Liquid Chain. Watchlist (idx 10) NOT reachable via keyboard. | KI-7 (closed iter 92) |
| Need to remove dead i18n keys after a tab/dialog/feature removal | (iter 89) Run `scripts/cleanup_dead_i18n_keys.py` — it removes the dead `tabXxx`/`fallbackXxx`/feature-specific keys from all 4 locale files. Leave a `REMOVED iter NN` comment marker in place of the old section header. See invariant #41(b). | — |
| Need to localize a currency name / category slug in a frontend component | (iter 87) Use `getCurrencyDisplayName(apiId, locale)` / `getCategoryDisplayName(slug, locale)` from `src/lib/currency-names.ts`. Always provide a fallback (`?? apiId` or `\|\| upstreamText`) since the helper returns `null` when the api_id isn't in the mapping. Pull `locale` from `useI18n().locale`. | — |
| Need to add `?lang=` support to a backend hint endpoint | (iter 87) Add `lang: str = Query("en", ...)` to the FastAPI route signature, forward to the pure function. Keep a parallel Russian table (`_XXX_RU`) with the SAME `id`/`category` slugs. Add the `lang` query param to the Next.js proxy route too. Frontend widget should pull `locale` from `useI18n()`, map to `lang`, include in `queryKey`, pass as query param to `fetchApi`. | — |
| Phase hints widget fires 2 fetches on mount instead of 1 | (iter 87) The widget uses `locale` in its `queryKey` — on mount the default locale ("ru") triggers fetch 1, then after hydration the stored locale (e.g. "en") triggers fetch 2. This is correct behaviour. | — |
| Speculation tab shows no "Run backtest" button | Backtest panel is collapsed by default — toggle only renders after the live signals list loads (parent must be in `online` + `dataAvailable=true` state). | F5 (done) |
| Backtest panel "Run backtest" click does nothing | Query is gated on `enabled: showBacktest && backendOnline`. If parent received `backendOnline=false`, toggle expands UI but `useQuery` never fires. Check dashboard-level backend health. | F5 (done) |
| FlipsTable Spread cell color changes between emerald/amber/muted | (iter 94, Q4) By design — color-coded by spread tier: emerald ≥5% (wide), amber 2-5% (medium), muted <2% (tight). Same thresholds power the "Spread tier" filter dropdown. | — |
| FlipsTable "Trend" column shows synthetic-looking sparkline | (iter 94, Q5) By design — derived from `momentum × volatility` (NOT historical price data). Tooltip explicitly states this. Switches to real data when backend adds `priceHistoryShort` (TD-9). | TD-9 |
| `tsc --noEmit` fails with TS1117 on `flipsBid`/`flipsAsk` | (iter 94, KI-10 CLOSED) Was caused by duplicate i18n keys from iter 92 TD-1 relabel. Fixed by deleting old "Bid"/"Ask" entries, keeping new "Buy at"/"Sell at" with tooltips. | KI-10 (closed iter 94) |

## 5. API Endpoints (all REST under `/api/v1/`)

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/v1/health` | Detailed health check |
| GET | `/api/v1/health/ping` | Ultra-lightweight ping |
| GET | `/api/v1/phase` | League phase info |
| GET | `/api/v1/currencies` | Currency metadata |
| GET | `/api/v1/prices` | All exchange rates + metrics |
| GET | `/api/v1/prices/heatmap` | 24h price change heatmap |
| GET | `/api/v1/prices/stream` | SSE live price updates |
| GET | `/api/v1/prices/{pair}` | Price for specific pair |
| GET | `/api/v1/tiers` | Currency tier classifications |
| GET | `/api/v1/benchmarks/{currency}` | Historical benchmarks |
| GET | `/api/v1/arbitrage/flips` | Scored flip opportunities (**P2-4 iter 67**: +`max_score`, `min_spread`, `max_spread`, `cluster`, `currency`, `sort_by`, `sort_dir`) |
| GET | `/api/v1/arbitrage/triangular` | Triangular arbitrage cycles |
| GET | `/api/v1/arbitrage/optimal-currency` | Optimal payment currency |
| POST | `/api/v1/batch` | Batch multiple GET requests |
| POST | `/api/v1/events` | Create event |
| GET | `/api/v1/events` | List events |
| GET | `/api/v1/events/summary` | Event summary |
| GET | `/api/v1/events/{event_id}` | Get event by ID |
| DELETE | `/api/v1/events/{event_id}` | Delete event |
| POST | `/api/v1/events/{event_id}/deactivate` | Deactivate event |
| GET | `/api/v1/anomalies` | Anomaly detection |
| GET | `/api/v1/storage-value/{currency}` | Hold/sell decision |
| GET | `/api/v1/storage-value/{currency}/history` | **F2 follow-up (iter 75)** — time-series of currency/mirror + currency/hinekora ratios |
| GET | `/api/v1/optimizer/path` | Optimal conversion path (P1-8 iter 64) |
| GET | `/api/v1/optimizer/matrix` | Conversion matrix |
| GET | `/api/v1/analyst/summary` | League analyst summary |
| GET | `/api/v1/portfolio/correlation` | Correlation matrix |
| GET | `/api/v1/liquid-chain/analysis` | Liquid chain analysis |
| GET | `/api/v1/liquid-chain/opportunities` | Liquid chain opportunities |
| GET | `/api/v1/content-pulse` | **F3 (iter 75)** — per-category turnover + 7d/30d rolling + top movers |
| GET | `/api/v1/speculation` | **F5 (iter 77)** — per-item z-score + BUY/SELL/HOLD signals. Query: `days` (1-90, default 30), `limit` (1-500, default 50), `signal` (ALL/BUY/SELL/HOLD, default ALL). |
| GET | `/api/v1/speculation/backtest` | **F5 follow-up (iter 79)** — backtest z-score signals on historical price_logs. Query: `eval_days_ago` (1-365, default 14), `holding_days` (1-90, default 7), `lookback_days` (1-90, default 30), `limit` (1-500, default 50), `signal` (ALL/BUY/SELL/HOLD, default ALL). Returns per-trade results + per-signal aggregates (win_rate, mean/median/best/worst return_pct). Backend-only — no frontend UI yet. |
| GET | `/api/v1/phase-hints` | **F6 (iter 78)** — phase-aware advisory hints (Temporalis, skill gems, etc.). No query params. Always returns `data_available=True` (hint table is hardcoded). |
| GET | `/api/v1/circuit-patterns` | **F7 / P8 (iter 97)** — per-currency trajectory classification + recommended action. Query: `days` (1-90, default 30), `limit` (1-500, default 50), `trajectory` (ALL/EXPONENTIAL_GROWTH/LINEAR_GROWTH/PEAK_THEN_DECLINE/MEAN_REVERTING/VOLATILE/DECLINING/STABLE, default ALL). Each pattern includes `price_history_short` (up to 14 most-recent points) for the UI mini-sparkline. |

**Frontend-only routes:**

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/flipper/health` | Proxies backend `/health` |
| GET | `/api/flipper/health/circuit-breakers` | **P2-6 iter 67** — JSON snapshot of per-endpoint circuit breaker state |
| GET | `/api/flipper/content-pulse` | **F4 (iter 76)** — Next.js proxy to `/api/v1/content-pulse`. Returns empty `categories: []` + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/speculation` | **F5 (iter 77)** — Next.js proxy to `/api/v1/speculation`. Forwards `days`/`limit`/`signal` query params. Returns empty `signals: []` + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/speculation/backtest` | **F5 follow-up (iter 80)** — Next.js proxy to `/api/v1/speculation/backtest`. Forwards `eval_days_ago`/`holding_days`/`lookback_days`/`limit`/`signal` query params. Returns empty `trades: []` + zeroed stats blocks + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/phase-hints` | **F6 (iter 78)** — Next.js proxy to `/api/v1/phase-hints`. No query params. Returns empty `hints: []` + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/{resource}` | Proxies to FastAPI `{resource}` (currencies, prices, flips, etc.) |

(WebSocket endpoints removed in iter 58.)

## 6. Documentation Map

| File | Purpose |
|------|---------|
| `STATUS.md` | **Known issues & refactoring backlog** — read first |
| `PRODUCT_VISION.md` | **Product direction** — analytics helper, NOT a poe2scout/poe2ninja clone. Read before proposing features. |
| `worklog.md` | Shared multi-agent work log (append-only). Each iter appends a section starting with `---`. Read the last 1-2 entries before starting a new iter. |
| `docs/ARCHITECTURE.md` | Layers, data flow, invariants, principles |
| `docs/DATA_CONTRACTS.md` | TypeScript types, API contracts |
| `docs/DATA_FLOW.md` | Data flow traces, field transforms |
| `docs/BACKEND_GUIDE.md` | FastAPI backend internals |
| `docs/CORS_PROXY_GUIDE.md` | CORS proxy setup |
| `PoE2_Flipper_Canonical_Formulas.md` | Mathematical formulas |

> **Note:** `REFACTOR_PLAN.md` was deleted in iter 73 (P3-7) after all P2/P3 issues were closed. `worklog.md` was deleted in iter 73 then re-created in iter 74 as the shared multi-agent work log (append-only, sections separated by `---`). For old task history before iter 73 see `git log`.


