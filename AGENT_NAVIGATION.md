# PoE2 Market Dashboard — Agent Navigation Guide

> **Single entry point** for codebase navigation. Updated 2026-07-13 (iter 145 — closes candidate (a) from iter 144 stop point: fixed `instrumentation.ts:7` + `flipper-backend-bridge.ts:313` comment drift (`/api/health` → `/api/v1/health/ping`). Added `--audit` flag to `scripts/sync_currency_names_from_poe2db.py` (READ-ONLY audit of all 686 existing RU translations vs poe2db current RU `<title>`). Ran full audit on 634 translated items: 601 match (94.8%), 32 mismatch (5.0%), 1 no-Cyrillic (`aldurs-saga`). Opened KI-32 (translation drift) + KI-33 (non-ASCII slug crash, fixed same iter) + TD-6 (translation alignment + unique-items RU support, 3-phase plan). 8 new audit tests + 6 KI-33 regression tests added to `tests/test_sync_currency_names.py` (57 total, all green). 1466 pytest green baseline preserved.).
> **Known issues live in [`STATUS.md`](./STATUS.md)** — check there before fixing anything.
> **Product direction lives in [`PRODUCT_VISION.md`](./PRODUCT_VISION.md)** — read it before proposing features.
> **Market patterns & roadmap live in [`docs/MARKET_PLAYBOOK.md`](./docs/MARKET_PLAYBOOK.md)** — read it before proposing new pattern detectors.

---

## 1. Where Things Are

| Directory | Purpose | Rules |
|-----------|---------|-------|
| `backend/` | Python FastAPI analytics engine | Tests mandatory (pytest) |
| `backend/api/` | Route handlers + response models + middleware | Import from `arbitrage/`, `economy/`, `predictors/`, `data/` |
| `backend/api/response_models.py` | Pydantic response models | Must match route return dicts exactly |
| `backend/api/data_snapshot.py` | DataSnapshot — shared TTL-cached snapshot | All routes use `get_snapshot()`, no direct provider calls |
| `backend/api/routes_sse.py` | SSE price stream (P0-1 fixed iter 55, KI-13 fixed iter 107) | Sends `{pair, change_pct, new_price, old_price, timestamp}` per changed currency; filters by `threshold_pct`. **KI-13:** `sse_router` MUST be registered before `prices_router` in `main.py` — the greedy `{pair:path}` route shadows `/stream` if registered first. |
| `backend/api/routes_analyst.py` | League analyst summary (P0-3 fixed iter 54) | `_compute_trends` uses `find_price_24h_ago` from `backend.economy.pricing` |
| `backend/api/routes_arbitrage.py` | Flips + triangular + clustering (P1-4 iter 63, P1-9 iter 66, **P2-4 iter 67**) | `/flips` supports `max_score`, `min_spread`, `max_spread`, `cluster`, `currency`, `sort_by`, `sort_dir` (all optional with safe defaults). The standalone `/scanner/scan` endpoint was deleted in iter 68. |
| `backend/api/routes_optimizer.py` | Bellman-Ford conversion paths (P1-8 fixed iter 64) | `_detect_negative_cycle_nodes()` flags profitable arbitrage cycles; `_bellman_ford` returns `None` when target is on a cycle |
| `backend/api/routes_events.py` | Event CRUD + cache invalidation (P1-11 iter 59, P1-7 iter 61) | All 3 endpoints async-await EventManager; `unified_cache` invalidated after mutation |
| `backend/economy/pricing.py` | Unified pricing helpers (P0-5 fixed iter 57, F5 extended iter 77) | `compute_transitive_prices` (BFS) + `find_price_24h_ago` + `compute_zscore` (F5) + `compute_percentile` (F5) |
| `backend/economy/clustering_helpers.py` | Shared clustering data prep + executor function (P1-4 iter 63) | `prepare_clustering_data()` + `run_clustering_sync()` + `CLUSTER_LABELS_CACHE_KEY` |
| `backend/economy/events.py` | EventManager + StoredEvent (P1-7 iter 61, **P3-3 iter 71**) | `event_id`, `is_active`, `created_at`; 4 methods async; `_prune_expired` left sync intentionally. **All in-memory `_events` access guarded by `threading.RLock`**; SQLite writes await OUTSIDE the lock. |
| `backend/economy/lifecycle.py` | PhaseDetector (P0-4 fixed iter 54) | `_reference_date` returns `patch_reset_date` unconditionally when set. Used by `phase_hints.py` (iter 78) via `get_phase_detector()` singleton. |
| `backend/economy/phase_hints.py` | **F6 (iter 78) + iter 110 (P9 live-price binding)** — phase-aware advisory hints (Temporalis, skill gems, etc.) | Pure function `get_phase_hints(phase, days_since_reference, *, reference_currency="", league_name="", now=None, lang="en", snapshot=None)`. Hardcoded `_PHASE_HINTS` table: 4 hints per phase (EARLY/MID/LATE) + `_PHASE_META` table for phase_label/summary. Each hint now has `tracked_currency` field (iter 110) — 3 of 12 hints tracked ("exalted" for early-quick-flips, "divine" for mid-triangular-arb + late-portfolio-hold). When `snapshot` is provided, tracked hints enriched with `current_price` / `change_pct_week` (7d signed %) / `change_pct_month` (30d signed %) / `momentum` (UP/DOWN/FLAT) / `recommendation` (phase-aware: BUY_OPPORTUNITY/HOLD/WATCH/SELL_INTO_STRENGTH/SELL_NOW/NEUTRAL). When snapshot=None, hints are static-only (backward-compat). Tunable constants: `MOMENTUM_UP_THRESHOLD_PCT=5.0`, `MOMENTUM_DOWN_THRESHOLD_PCT=-5.0`, `CHANGE_WEEK_DAYS=7`, `CHANGE_MONTH_DAYS=30`, `NEAREST_PRICE_TOLERANCE_HOURS=24.0`. Helpers: `_find_price_near()` / `_compute_change_pct()` / `_momentum_from_change()` / `_recommendation_from_phase_momentum()` / `_enrich_hint_with_live_price()` / `list_tracked_hints()` exposed for tests. **Field naming note (KI-20 closed iter 116):** uses `change_pct_week` / `change_pct_month` (NOT `change_pct_7d` / `change_pct_30d`) — originally a workaround for the case-transform `_<digit>` regex bug. KI-20 is now fixed (regex matches `_<digit>`), but the naming is kept as a defensive measure (cleaner field names, no transformation quirk to remember). 109 pytest tests in `tests/test_phase_hints.py` (61 original + 48 iter-110 enrichment). |
| `backend/economy/content_pulse.py` | **F3 (iter 75) + iter 95 (TD-2 + Q13 Overheat Index)** — daily turnover per category + 7d/30d rolling + signal + overheat | Pure function `compute_content_pulse(snapshot, config)`. No side effects. Tunable thresholds at module top: `SIGNAL_RISING_THRESHOLD_PCT=10.0`, `SIGNAL_FALLING_THRESHOLD_PCT=-10.0`, `TOP_N_PER_CATEGORY=3`, `OVERHEAT_VOLUME_SPIKE_THRESHOLD=2.0`, `OVERHEAT_PRICE_DROP_THRESHOLD=-5.0` (iter 95). |
| `backend/economy/speculation.py` | **F5 (iter 77)** — per-item z-score + BUY/SELL/HOLD signals | Pure function `compute_speculation_signals(snapshot, config, days=30, limit=50, signal_filter="ALL")`. No side effects. Tunable thresholds at module top (`Z_BUY_THRESHOLD=-1.5`, `Z_SELL_THRESHOLD=1.5`, `MAX_HISTORY_POINTS=14`, `MIN_SAMPLE_SIZE=2`, `DEFAULT_DAYS=30`, `DEFAULT_LIMIT=50`). |
| `backend/economy/speculation_backtest.py` | **F5 follow-up (iter 79)** — backtest the z-score BUY/SELL/HOLD strategy on historical price_logs | Pure function `backtest_speculation_signals(snapshot, config, eval_days_ago=14, holding_days=7, lookback_days=30, limit=50, signal_filter="ALL")`. Reuses `compute_zscore` + `_extract_prices` + `_signal_from_zscore` + `Z_BUY_THRESHOLD`/`Z_SELL_THRESHOLD`/`MIN_SAMPLE_SIZE` from `speculation.py` — guarantees backtest uses the same strategy as the live signal. Returns per-trade results + per-signal aggregates (count, win_rate, mean/median/best/worst return_pct). Tolerance `TOLERANCE_HOURS=24` between target timestamp and nearest price log (matches `storage_value_history.py`). Baseline window strictly BEFORE entry timestamp (entry price not in baseline — no signal leak). |
| `backend/economy/storage_value_history.py` | **F2 follow-up (iter 75)** — time-series of currency/mirror + currency/hinekora ratios | Pure function `compute_storage_value_history(snapshot, currency, days=30)`. Uses 24h nearest-neighbor tolerance for matching timestamps across the three histories. |
| `backend/economy/circuit_patterns.py` | **F7 (iter 96)** — trajectory classification per currency (P8 from `docs/MARKET_PLAYBOOK.md`) | Pure function `compute_circuit_patterns(snapshot, config, *, days=30, limit=50, trajectory_filter="ALL", now=None)`. Classifies each currency into one of 7 archetypes: `EXPONENTIAL_GROWTH` / `LINEAR_GROWTH` / `PEAK_THEN_DECLINE` / `MEAN_REVERTING` / `VOLATILE` / `DECLINING` / `STABLE`. Uses OLS linear regression (stdlib only — no numpy/pandas) on price_logs day-index. Tunable thresholds at module top (`MIN_SAMPLE_SIZE=4`, `MIN_R_SQUARED=0.7`, `EXP_GROWTH_MIN_TOTAL_PCT=50`, `PEAK_DECLINE_MIN_PCT=20`, `STABLE_MAX_CV=0.15`, `VOLATILE_MIN_CV=0.5`). Each currency gets `recommended_action`: HOLD_FOR_GROWTH / SELL_NOW / AVOID / WATCH / NEUTRAL. **iter 97:** now also emits `price_history_short` (up to 14 most-recent price points, oldest-first) for the UI mini-sparkline. 75 pytest tests in `tests/test_circuit_patterns.py` (iter 96) + 4 route smoke tests (iter 97) = 79 total. |
| `backend/economy/intraday_patterns.py` | **P4 (iter 98)** — time-of-day price pattern detector per currency | Pure function `compute_intraday_patterns(snapshot, config, *, days=14, limit=50, now=None)`. For each currency: aggregates price_logs by UTC hour (0..23) over the lookback window → per-hour mean/std/count. `buy_window_hour` = hour with min mean (Asia-wake dump). `sell_window_hour` = hour with max mean (US/EU-wake spike). `has_significant_pattern` = `intraday_range_pct >= 10%` (SIGNIFICANT_RANGE_PCT). Filters: `MIN_SAMPLE_SIZE=4` total points AND `MIN_HOURS_COVERED=2` distinct hours. Always emits 24 hourly_stats entries per currency (empty hours have mean=null, count=0) for the UI heatmap. Tunable thresholds at module top. 89 pytest tests in `tests/test_intraday_patterns.py` (iter 98). |
| `backend/economy/weekly_patterns.py` | **P5 (iter 99)** — weekday/weekend price pattern detector per currency | Pure function `compute_weekly_patterns(snapshot, config, *, weeks=4, limit=50, now=None)`. For each currency: aggregates price_logs by ISO weekday (1=Mon..7=Sun) over the lookback window → per-weekday mean/std/count. `buy_window_day` = weekday with min mean (mid-week supply). `sell_window_day` = weekday with max mean (weekend demand spike). `has_significant_pattern` = `weekly_range_pct >= 10%` (SIGNIFICANT_RANGE_PCT). `weekday_delta_pct` = signed % difference `(weekend_mean - weekday_mean) / overall_mean × 100` (positive = weekends more expensive, negative = weekdays more expensive). Filters: `MIN_SAMPLE_SIZE=4` total points AND `MIN_DAYS_COVERED=2` distinct weekdays. Always emits 7 daily_stats entries per currency (empty days have mean=null, count=0) for the UI heatmap. Tunable thresholds at module top. 99 pytest tests in `tests/test_weekly_patterns.py` (iter 99). |
| `backend/economy/leveling_uniques.py` | **P3 (iter 100)** — leveling-uniques lifecycle (spike-then-crash) detector | Pure function `compute_leveling_uniques_lifecycle(phase, days_since_reference, *, reference_currency="", league_name="", now=None, lang="en")`. Static table `_LEVELING_UNIQUES` of 10 well-known leveling uniques (Polcirkeln Sapphire Ring, Megalomaniac Diamond, Wall of Brambles, Mana Leech Support, Feeding Frenzy Support, Echoes of Worldstone, Mind of the Council, Boots of Momentum, Wings of Entropy, Soul Tether Amulet) with `peak_day` / `peak_price_exalted` / `decay_pct` / `pattern` / `notes`. For each unique: computes `current_lifecycle_stage` (PRE_PEAK / AT_PEAK [2-day window peak_day..peak_day+1] / POST_PEAK) + `recommendation` (BUY_OR_HOLD / SELL_NOW / AVOID_BUYING) + `estimated_current_price_exalted` (piecewise-linear heuristic — NOT a live market price) + `days_until_peak` (positive/zero/negative). Parallel `_LEVELING_UNIQUES_NOTES_RU` table for Russian `notes` (only notes translated — id/name/category/peak_day/peak_price_exalted/decay_pct/pattern are identical across locales). Does NOT depend on DataSnapshot — uses PhaseDetector only (immune to KI-11). Always returns `data_available=True`. Helpers `list_leveling_uniques()` + `leveling_unique_count()` exposed for tests. 86 pytest tests in `tests/test_leveling_uniques.py` (iter 100). |
| `backend/api/routes_circuit_patterns.py` | **F7 / P8 (iter 97)** — route handler `GET /api/v1/circuit-patterns?days=30&limit=50&trajectory=ALL` | Thin wrapper: fetch snapshot → call `compute_circuit_patterns` → inject `days` field into the response dict (the pure function does not echo `days` back — only route handlers need it for client cache keys) → shape response. Query params validated by FastAPI (`ge=1, le=90` for days, `ge=1, le=500` for limit, `pattern=^(ALL\|EXPONENTIAL_GROWTH\|LINEAR_GROWTH\|PEAK_THEN_DECLINE\|MEAN_REVERTING\|VOLATILE\|DECLINING\|STABLE)$` for trajectory). Returns `data_available=false` + empty `patterns` list when snapshot is not loaded or computation throws. 4 pytest route smoke tests in `tests/test_circuit_patterns.py::TestRouteHandler`. |
| `backend/api/routes_intraday_patterns.py` | **P4 (iter 98)** — route handler `GET /api/v1/intraday-patterns?days=14&limit=50` | Thin wrapper: fetch snapshot → call `compute_intraday_patterns` → inject `days` field into the response dict → shape response. Query params validated by FastAPI (`ge=1, le=90` for days, `ge=1, le=500` for limit). Returns `data_available=false` + empty `patterns` list when snapshot is not loaded or computation throws. 4 pytest route smoke tests in `tests/test_intraday_patterns.py::TestRouteHandler`. |
| `backend/api/routes_weekly_patterns.py` | **P5 (iter 99)** — route handler `GET /api/v1/weekly-patterns?weeks=4&limit=50` | Thin wrapper: fetch snapshot → call `compute_weekly_patterns` → inject `weeks` field into the response dict → shape response. Query params validated by FastAPI (`ge=1, le=26` for weeks — each week = 7 days, so weeks=4 → 28-day window; `ge=1, le=500` for limit). Returns `data_available=false` + empty `patterns` list when snapshot is not loaded or computation throws. 4 pytest route smoke tests in `tests/test_weekly_patterns.py::TestRouteHandler`. |
| `backend/api/routes_leveling_uniques.py` | **P3 (iter 100)** — route handler `GET /api/v1/leveling-uniques?lang=en\|ru` | Thin wrapper: fetch PhaseDetector singleton via `get_phase_detector()` → call `detector.get_phase_info()` → forward to `compute_leveling_uniques_lifecycle()`. Query param `lang` (default "en", "ru" returns Russian notes). Does NOT fetch DataSnapshot — uses PhaseDetector only (immune to KI-11). Always returns `data_available=true` (hardcoded table). On exception, returns minimal response with empty `uniques` + `data_available=false`. 4 pytest route smoke tests in `tests/test_leveling_uniques.py::TestRouteHandler`. |
| `backend/economy/mirror_divine_arb.py` | **P7 (iter 108)** — Mirror/Divine arbitrage window detector | Pure function `compute_mirror_divine_arb(snapshot, config, *, days=30, mirror_api_id="mirror", divine_api_id="divine", now=None)`. For each timestamp in mirror_history, finds nearest divine_price (24h tolerance — reuses `_find_nearest_price` from `storage_value_history.py`). Computes rate series → current_rate / mean_rate / std_rate (sample, ddof=1) / min/max / z_score / deviation_pct / profit_potential_per_mirror_div = \|current - mean\|. Signal: SELL_MIRROR_BUY_DIVINE (z ≥ +1.5) / SELL_DIVINE_BUY_MIRROR (z ≤ -1.5) / NEUTRAL. is_actionable = profit_potential ≥ 100 Div (PROFIT_THRESHOLD_DIV). recommended_action: EXECUTE_ARB (actionable AND \|z\| ≥ 1.5) / WATCH (actionable AND \|z\| in [1.0, 1.5)) / HOLD. price_history_short = up to 14 most-recent rate points (oldest-first) for UI sparkline. SINGLE-OBJECT response (Mirror:Divine is one market, not a per-currency list) — differs from circuit_patterns/speculation/intraday/weekly which return per-currency lists. Tunable constants: MIN_SAMPLE_SIZE=4, PROFIT_THRESHOLD_DIV=100.0, Z_BUY=-1.5, Z_SELL=+1.5, Z_WATCH=1.0, MAX_HISTORY_POINTS=14. 70 pytest tests in `tests/test_mirror_divine_arb.py` (iter 108). |
| `backend/api/routes_mirror_divine_arb.py` | **P7 (iter 108)** — route handler `GET /api/v1/mirror-divine-arb?days=30` | Thin wrapper: fetch snapshot → call `compute_mirror_divine_arb` → shape response. Query param `days` validated by FastAPI (`ge=1, le=90`). Returns `data_available=false` with all-None numeric fields + `signal="NEUTRAL"` + `recommended_action="HOLD"` when snapshot is not loaded, when < MIN_SAMPLE_SIZE rate points are in the window, or when computation throws. 2 pytest route smoke tests in `tests/test_mirror_divine_arb.py::TestRouteHandler`. |
| `backend/economy/market_spreads.py` | **TD-4 Phase 2 (iter 128)** — per-pair spread persistence helper | Pure function `compute_market_spreads(snapshot, config) -> list[dict]`. Mirrors the spread formula from `routes_arbitrage.py:274-308` verbatim (liquidity_spread + vol_spread, clamped to [min, max] × BFS factor, then × (1 + momentum_factor) clamped to max_total_spread). Only direct pairs persisted (BFS factor = 1.0 — design doc §10 Q2 default). Returns list of dicts with keys: pair_key, currency_from, currency_to, raw_rate, volume_24h, market_spread, total_spread, momentum_factor, bfs_widening_factor. Called by `SnapshotManager._refresh()` as best-effort post-build write (try/except, non-fatal). Formula-parity test in `tests/test_market_spreads.py::TestComputeMarketSpreadsFormulaParity` guards against drift. 17 pure-helper pytest + 10 persistence + 2 SnapshotManager integration + 1 E2E in `tests/test_market_spreads.py`. |
| `backend/api/routes_market_spreads.py` | **TD-4 Phase 2 (iter 128)** — route handler `GET /api/v1/market-spreads/history?pair=&days=30` | Read-only thin wrapper around `HistoricalStore.read_market_spreads(league, pair_key=None, days=30)` + `read_market_spreads_pairs(league)`. Query params: `pair` (optional, directional e.g. "exalted/divine"), `days` (default 30, max 90). Returns `data_available=false` + empty `points` + empty `available_pairs` when no rows match OR when HistoricalStore raises (graceful 200, NOT 500). Response model: `MarketSpreadsHistoryResponse` with `MarketSpreadPoint` list. 10 route pytest in `tests/test_market_spreads_route.py`. |
| `backend/api/data_snapshot.py:SnapshotManager._refresh` | **TD-4 Phase 2 (iter 128)** — best-effort `market_spreads` write integration point | After the snapshot is built (currencies + price_histories + tiers populated), BEFORE `return snapshot`, calls `compute_market_spreads(snapshot, config)` → `HistoricalStore.write_market_spreads_batch(league, spreads, timestamp=snapshot.fetched_at)`. Wrapped in `try/except Exception` that logs a warning and continues — design doc §5.1 invariant: "persistence MUST NOT block the snapshot publish". Uses `INSERT OR IGNORE` with 5-min-bucket dedup index so a retry on the next tick is a no-op. |
| `backend/data/historical.py` | **TD-4 Phase 2 (iter 128)** — `market_spreads` table + write/read methods | New SQLite table `market_spreads(timestamp, league, pair_key, currency_from, currency_to, raw_rate, volume_24h, market_spread, total_spread, momentum_factor, bfs_widening_factor)` with 3 indexes (ts, pair+league, dedup on `strftime('%Y-%m-%d %H:%M', timestamp), league, pair_key`). New methods: `write_market_spreads_batch(league, spreads, timestamp)` (INSERT OR IGNORE, returns row count), `read_market_spreads(league, pair_key=None, days=30)` (oldest-first), `read_market_spreads_pairs(league)` (distinct pair_keys, alphabetical). Extended `_prune_old_records` + `_prune_old_league_data` to also prune `market_spreads` (same chunked-delete pattern as `price_snapshots`). 10 persistence pytest in `tests/test_market_spreads.py::TestHistoricalStoreMarketSpreads`. |
| `backend/economy/triangular_cycles.py` | **TD-3 Phase 3 (iter 129) + iter 134 pipeline_cache optimization** — triangular arbitrage cycle persistence helper | Async pure function `compute_triangular_cycles(snapshot, config, *, min_profit_pct=1.0, cross_rate_threshold_pct=7.0, pipeline_cache: PipelineCache \| None = None) -> list[dict]`. Builds `rates_for_bf` + `pair_volumes` from `snapshot.exchange_rates` (mirrors `routes_arbitrage.py:829-848` verbatim — parity test guards against drift), calls `find_triangular_arbitrage()` with the route's defaults, returns list of dicts with keys: cycle_key, cycle_currencies (JSON), raw_profit_pct, executable_estimate, executable_profit, confidence, snapshot_age_sec. cycle_key = `sorted(set(cycle))` joined with `->` (collapses rotations to one key for backtest grouping — design doc §4.3 + §8.4). Only profitable cycles persisted (min_profit_pct=1.0 — design doc §10 Q1 default). Never raises — find_triangular_arbitrage failure (incl. 90s timeout) logs warning + returns []. **iter 134:** when `pipeline_cache` is provided, ALSO populates the live `/triangular` route's cache with `(opportunities, cross_rate_warning)` under key `f"triangular_arbitrage_{min_profit_pct}"` — eliminates doubled compute cost (the route gets a cache HIT on first request after each refresh). Also exports `build_cross_rate_warning(suspicious_triples) -> dict \| None` pure helper (extracted verbatim from `routes_arbitrage.py:878-898` — DRY parity guarantee between refresh path and live route). 24 pure-helper pytest + 9 helper-parity pytest + 10 pipeline_cache pytest in `tests/test_triangular_cycles.py`. |
| `backend/api/routes_arbitrage.py` (extended) | **TD-3 Phase 3 (iter 129)** — added `GET /api/v1/arbitrage/triangular/history?cycle_key=&days=30` route | Read-only thin wrapper around `HistoricalStore.read_triangular_cycles(league, cycle_key=None, days=30)` + `read_triangular_cycles_keys(league)`. Query params: `cycle_key` (optional, sorted-unique join e.g. "divine->exalted->mirror"), `days` (default 30, max 90). Returns `data_available=false` + empty `points` + empty `available_cycle_keys` when no rows match OR when HistoricalStore raises (graceful 200, NOT 500). Response model: `TriangularCyclesHistoryResponse` with `TriangularCyclePoint` list. **Distinct from the live `/triangular` route** — `/history` reads from SQLite, does NOT call `find_triangular_arbitrage`. The live route is unchanged. 13 route pytest in `tests/test_triangular_cycles_route.py` (incl. distinct-from-live tests). |
| `backend/api/data_snapshot.py:SnapshotManager._refresh` (extended) | **TD-3 Phase 3 (iter 129) + iter 134 pipeline_cache optimization** — best-effort `triangular_cycles` write + cache pre-population integration point | AFTER the TD-4 market_spreads block, BEFORE `return snapshot`, calls `compute_triangular_cycles(snapshot, config, pipeline_cache=get_pipeline_cache())` → `HistoricalStore.write_triangular_cycles_batch(league, cycles, timestamp=snapshot.fetched_at)`. Wrapped in `try/except Exception` that logs a warning and continues — design doc §5.1 invariant. Uses `INSERT OR IGNORE` with 5-min-bucket dedup index. **iter 134 optimization:** the `pipeline_cache` arg makes `compute_triangular_cycles` pre-populate the live `/triangular` route's cache on every refresh, so the route gets a cache HIT on first request (was a 30-60s cache MISS pre-optimization). Eliminates the doubled `find_triangular_arbitrage` compute cost. |
| `backend/data/historical.py` (extended) | **TD-3 Phase 3 (iter 129)** — `triangular_cycles` table + write/read methods | New SQLite table `triangular_cycles(timestamp, league, cycle_key, cycle_currencies, raw_profit_pct, executable_estimate, executable_profit, confidence, snapshot_age_sec)` with 3 indexes (ts, cycle_key+league, dedup on `strftime('%Y-%m-%d %H:%M', timestamp), league, cycle_key`). New methods: `write_triangular_cycles_batch(league, cycles, timestamp)` (INSERT OR IGNORE, returns row count), `read_triangular_cycles(league, cycle_key=None, days=30)` (oldest-first), `read_triangular_cycles_keys(league)` (distinct cycle_keys, alphabetical). Extended `_prune_old_records` + `_prune_old_league_data` to also prune `triangular_cycles` (same chunked-delete pattern). League-distinct query now UNIONs `price_snapshots` + `market_spreads` + `triangular_cycles`. 9 persistence pytest in `tests/test_triangular_cycles.py::TestHistoricalStoreTriangularCycles`. |
| `backend/api/routes_content_pulse.py` | **F3 (iter 75)** — route handler `GET /api/v1/content-pulse` | Thin wrapper: fetch snapshot → call `compute_content_pulse` → shape response. Returns `data_available=false` + empty `categories` list when snapshot is not loaded. |
| `backend/api/routes_speculation.py` | **F5 (iter 77)** — route handler `GET /api/v1/speculation?days=30&limit=50&signal=ALL` | Thin wrapper: fetch snapshot → call `compute_speculation_signals` → shape response. Query params validated by FastAPI (`ge=1, le=90` for days, `ge=1, le=500` for limit, `pattern=^(ALL\|BUY\|SELL\|HOLD)$` for signal). Returns `data_available=false` + empty `signals` list when snapshot is not loaded. |
| `backend/api/routes_speculation_backtest.py` | **F5 follow-up (iter 79)** — route handler `GET /api/v1/speculation/backtest?eval_days_ago=14&holding_days=7&lookback_days=30&limit=50&signal=ALL` | Thin wrapper: fetch snapshot → call `backtest_speculation_signals` → shape response. Query params validated by FastAPI (`ge=1, le=365` for eval_days_ago, `ge=1, le=90` for holding_days/lookback_days, `ge=1, le=500` for limit, `pattern=^(ALL\|BUY\|SELL\|HOLD)$` for signal). Returns `data_available=false` + empty `trades` + zeroed stats blocks when snapshot is not loaded. |
| `backend/api/routes_phase_hints.py` | **F6 (iter 78) + iter 110 (P9 snapshot enrichment)** — route handler `GET /api/v1/phase-hints?lang=en\|ru` | Thin wrapper: fetch PhaseDetector singleton → call `detector.get_phase_info()` → best-effort fetch DataSnapshot (skips enrichment when `last_snapshot is None` or `get_snapshot()` raises — preserves "immune to KI-11") → forward to `get_phase_hints()` pure function with snapshot. Always returns `data_available=True` (hint table is hardcoded). On exception returns minimal response with `data_available=False` (no 500). 4 pytest route smoke tests in `tests/test_phase_hints.py::TestRouteHandler` (iter 78) + 4 in `TestRouteHandlerWithSnapshot` (iter 110). |
| `backend/api/routes_storage_value.py` | Storage value routes (existing `/storage-value/{currency}` + new `/storage-value/{currency}/history` iter 75) | Both routes return `data_available=false` with empty payload when snapshot not loaded — no 503. |
| `backend/data/historical.py` | SQLite store for price snapshots + events | Chunked delete (P1-6 + P3-2 iter 66) — `rowid IN (SELECT ... LIMIT ?)` pattern |
| `backend/data/unified_cache.py` | UnifiedCache with namespaces: `pipeline`, `daily_stats` | Shim modules deleted in iter 66 (P2-2) — import directly |
| `backend/data/currency_names_ru.py` | Thin loader (63 lines) for `currency_names.json` (P2-3 closed iter 70) | Edit `currency_names.json`, not the `.py` |
| `backend/api/data_snapshot.py` | DataSnapshot + SnapshotManager (**P3-4 iter 71**) | Atomic `(snapshot, ts)` swap via immutable `_SnapshotState` dataclass. `_history_cache` + `_active_currencies` guarded by separate `_cache_lock`. `last_snapshot` reads `_state` once. |
| `backend/arbitrage/scorer.py` | Opportunity scoring + quantized analysis (P1-5 fixed iter 66) | `compute_quantized_analysis` uses bounded linear scan `O(1/D)` |
| `backend/arbitrage/triangular.py` | Triangular arbitrage | `find_triangular_arbitrage(rates, min_profit_pct, ...)` — uses `get_process_pool()` (P2-13 fixed iter 65) |
| `backend/main.py` | FastAPI app + lifespan + lazy `process_pool` | `get_process_pool()` lazily creates/recreates the pool (P2-13). Backward-compat: `from backend.main import process_pool` still works via module `__getattr__` but emits `DeprecationWarning`. |
| `backend/predictors/time_series.py` | LightGBM forecaster (**P2-9 iter 67**) | `train()` has adaptive fallback: when `floor (5) <= len(log_prices) < min_points (15)`, uses minimal features (`price_lags=[1]` only) + lowered clean-rows threshold (2 instead of 10). Below `floor`, skips. |
| `backend/predictors/` | Time-series, anomaly, clustering, storage_value, model_store | CPU-heavy → ProcessPoolExecutor |
| `backend/data/providers/` | Poe2Scout, official, base | Network IO only |
| `backend/models/` | Core dataclass models | No framework imports |
| `backend/config.py` | Pydantic config models | `ForecastingConfig.lightgbm_min_data_points_floor` added iter 67 (default 5) |
| `src/app/api/flipper/` | Next.js proxy routes → FastAPI | **Proxy only, no business logic** |
| `src/app/api/flipper/health/circuit-breakers/route.ts` | **P2-6 (iter 67)** — exposes per-endpoint CB state as JSON | Read-only; calls `getAllEndpointCircuitBreakers()` from flipper-proxy |
| `src/app/api/flipper/prices/stream/route.ts` | SSE proxy | 5min timeout, streams body |
| `src/app/api/flipper/events/route.ts` | Events POST proxy with body transform | `eventType`→`event_type`, `expiryHours`→`expires_at` ISO |
| `src/components/dashboard/` | Tab components, dialogs, sidebar | Import from `@lib`, `@hooks` |
| `src/hooks/` | React hooks (19 hooks) | Import from `@lib` |
| `src/lib/` | Shared utilities, types, store, i18n, proxy, poe2api | **Types in `types.ts` ONLY** |
| `src/lib/flipper-proxy.ts` | Proxy with per-endpoint circuit breaker + dedup + mode-aware 5xx fallback (P1-10 iter 66, **P2-8 iter 69**) | `Map<path, EndpointCircuitBreaker>` keyed by normalized path; `proxyWithFallback` passes non-503 5xx through in dev, returns 200+`X-Flipper-Fallback` header in prod. Exports `isFlipperFallbackResponse`, `getFlipperFallbackOriginalStatus`, `FLIPPER_FALLBACK_HEADER` |
| `src/hooks/use-price-stream.ts` | SSE hook (P0-1 iter 55, P2-7 iter 59, iter 114 KI-24 refs fix, iter 115 KI-24 set-state-in-effect fix) | `invalidateCaches(pair)` — per-pair benchmark invalidation. iter 114: both latest-ref writes (`thresholdRef` + `connectRef`) moved into `useEffect` to satisfy `react-hooks/refs`. Sync-effects declared BEFORE the connect-on-mount effect to preserve ref-reads ordering. iter 115: `backendOnline` transitions effect no longer calls setState directly — `status`/`lastError` are derived in the return statement (`backendOnline === false ? "disconnected" : status`), and `setReconnectCount(0)`/`setLastError(null)` moved into `connect()` via a `freshSessionRef` signal-ref pattern (set in the effect, consumed at the top of `connect()` before any early-return guards). |
| `src/hooks/use-flipper-backend.ts` | **iter 81** — dashboard-level flipper backend status (Stage 1 of `useDashboardData` extraction) | Single source of truth for the 3 dashboard-level flipper queries: `/api/flipper/health` (always on, 30s polling), `/api/flipper/phase` (gated on online, 60s polling), `/api/flipper/events?active_only=true` (gated on online, 30s polling). Returns `{ flipperBackendOnline, flipperUpstreamReachable, flipperPhaseData, activeEventsCount, flipperHealthData?, flipperHealthPending, flipperHealthError, flipperEventsData? }`. All query keys go through `QUERY_KEYS` (no inline `useQuery` for these endpoints anywhere else). Inline `useQuery` for `/api/flipper/health`, `/api/flipper/phase`, or `/api/flipper/events` from `dashboard-page.tsx` is now FORBIDDEN — use the hook. |
| `src/hooks/use-realms-and-leagues.ts` | **iter 82** — realm/league selection + realms/leagues queries (Stage 2 of `useDashboardData` extraction) | Single source of truth for realm/league state and the two dropdown-populating queries: `/api/poe2/realms` (always on) and `/api/poe2/leagues` (gated on realm). Owns the `realm` + `league` useState so the auto-select useEffect (auto-pick first active league when none selected) can fire when `leagues` arrives. Returns `{ realm, setRealm, league, setLeague, realms, realmsLoading, leagues, leaguesLoading, effectiveLeague }`. `setRealm` clears the league (matches prior inline behaviour); `setLeague` persists to the Zustand store via `persistLeague`. `effectiveLeague` falls back to active league, then first league, then `""`. Inline `useQuery` for `/api/poe2/realms` or `/api/poe2/leagues` from `dashboard-page.tsx` is now FORBIDDEN — use the hook. |
| `src/hooks/use-filtered-exchange-pairs.ts` | **iter 83** — Exchange tab filter pipeline (Stage 3a of `useDashboardData` extraction) | Single source of truth for the search → quick-chip → extended-numeric-filter derivation that turns raw `exchangeData` into the `exchangePairs` array consumed by the Exchange tab. Pure hook — no queries, no store reads, no state. Takes `{ exchangeData, search, exchangeUiState }` (where `exchangeUiState` is the `uiState.exchange` slice from the Zustand store) and returns a derived `ExchangePair[]`. Filter rules (case-insensitive search on either currency name; `topVolume` chip = top 20 by volume desc, applied AFTER search; `favorites` chip = pairs in `exchangeUiState.favorites`; extended `minVolume`/`maxVolume` inclusive; `minChange`/`maxChange` ignored when 0; `changePercent` treated as -∞/+∞ when null) match the prior inline `useMemo` exactly — zero behavior change. Inline `useMemo` re-implementing this filter pipeline in `dashboard-page.tsx` is FORBIDDEN — use the hook. |
| `src/hooks/use-item-category-lists.ts` | **iter 83** — Currency/unique category-chip list derivation (Stage 3a of `useDashboardData` extraction) | Single source of truth for `currencyCategories` + `uniqueCategoriesList` — the two chip-strip arrays rendered above the Currencies and Uniques tabs. Pure hook — no queries, no store reads, no state. Takes `{ uniqueCategories, t }` (raw `useItemCategories()` response + the i18n `t` function for the synthetic "all" fallback label) and returns `{ currencyCategories, uniqueCategoriesList }`. Currency list = `name !== "Unique"`; unique list = `name === "Unique"` OR substring match on `Unique`/`Armour`/`Weapon`/`Accessory`/`Flask`/`Jewel`/`Gem`. Both lists push `{ name: "all", displayName: t("all"), count: 0 }` when empty so the chip strip always renders something. Inline `useMemo` re-implementing these filter rules in `dashboard-page.tsx` is FORBIDDEN — use the hook. |
| `src/hooks/use-optimal-payment.ts` | **iter 84** — §11 optimal-payment cluster (Stage 3b of `useDashboardData` extraction; COMPLETE) | Single source of truth for the optimal-payment pipeline. Owns (a) `optimalCurrencyData` useQuery (gated on `flipperBackendOnline`, queryKey `QUERY_KEYS.flipperOptimalCurrency`, 60s staleTime/refetchInterval, retry: 1 — UNCHANGED from prior inline call); (b) `clientOptimalResult` memo (client-side fallback using `findOptimalPayment` + `isItemCategory` from `@/lib/currency-optimal`, depends on `exchangeData` + `crossRates.relativePriceMap`/`anchorId`/`anchorRelPrice`/`crossRateFlips`); (c) backend/client merge memo (backend data takes priority when `dataAvailable && optimalPaymentByPair`, otherwise falls back to client; backend keys `"currencyFrom_currencyTo"` are remapped to frontend `pair.id`); (d) `optimalPaymentByDisplayName` memo (display-name-keyed `Map<string, OptimalPaymentResult>` for FlipsTab). Takes `{ exchangeData, crossRates, flipperBackendOnline }` (all owned by parent) and returns `{ optimalPaymentByPair, crossRateFlips, selectedAnchorId, optimalPaymentByDisplayName }`. Inline `useQuery` for `/api/flipper/optimal-currency` OR inline `useMemo` re-implementing the client fallback / merge / byDisplayName logic in `dashboard-page.tsx` is FORBIDDEN — use the hook. |
| `src/components/dashboard/dashboard-page.tsx` | Parent wiring component, **995 lines** (iter 84: −133 lines via `useOptimalPayment` extraction — extraction COMPLETE; iter 83: −41 lines via `useFilteredExchangePairs` + `useItemCategoryLists`; iter 82: −28 lines via `useRealmsAndLeagues`; iter 81: −35 lines via `useFlipperBackend`). 6 presentational subcomponents extracted in iter 71-73. | `useDashboardData` extraction COMPLETE — no further staged refactors planned. dashboard-page.tsx is now legitimate parent wiring. Future code-health work should be opportunistic (per-file) rather than dashboard-page-specific. |
| `src/components/dashboard/dashboard-toolbar.tsx` | Toolbar (TabsList + action buttons + category chips) (**P2-1 iter 73, step 4a**) | Pure presentational. Owns the tab strip + keyboard-shortcuts/alerts/comparison/pair-comparison buttons + the currencies/uniques category-filter chip strip. Tab switching still goes through the parent `<Tabs onValueChange=...>` scope. |
| `src/components/dashboard/dashboard-dialogs.tsx` | Dialog/sheet/banner wrappers (**P2-1 iter 73, step 4b**) | Pure presentational. Wraps DetailDialog, PairDetailDialog, ComparisonDialog, PairComparisonDialog, PriceAlertDialog, EventsSidebar, OfflineBanner, ShortcutsDialog — 8 primitives that sit at the bottom of the Dashboard render tree. Each open/close flag is a prop. |
| `src/components/dashboard/exchange-tab-content.tsx` | Exchange tab content (**P2-1 iter 71**) | Pure presentational component. Takes all state as props from `Dashboard` — no store/i18n imports of its own. Pattern reused by the iter 72-73 extractions. |
| `src/components/dashboard/currencies-tab-content.tsx` | Currencies tab content (**P2-1 iter 72**) | Pure presentational. Owns data-freshness badge, loading/empty/error states, virtual-vs-static grid switch, pagination. |
| `src/components/dashboard/uniques-tab-content.tsx` | Uniques tab content (**P2-1 iter 72**) | Pure presentational. Owns data-freshness badge, loading/empty/error states, UniqueTable, pagination. |
| `src/components/dashboard/overview-tab-content.tsx` | Overview tab content (**P2-1 iter 72**, updated iter 76) | Pure presentational. Composes 3 panels, each wrapped in its own ErrorBoundary: ContentPulseWidget (F4 iter 76, mounted FIRST for first-load visibility) + MarketOverview + ComparativeChart. |
| `src/components/dashboard/storage-value-tab.tsx` | Storage Value tab (**F2 iter 74 + iter 75**) | Wraps the existing `/api/v1/storage-value/{currency}` endpoint. Lazy-loaded, ErrorBoundary-wrapped. Currency picker (default list of 14 canonical currencies) + horizon picker (1/6/24/48/168h) + quantity input + Compute button. Renders: decision card (BUY_HOLD / SELL_CONVERT / NEUTRAL with hint), projection breakdown (current/projected/risk-discount/adjusted/net/ratio), holdings totals (×quantity), inputs panel (momentum/volatility/acceleration/liquidity/α/horizon), historical chart (iter 75). Full i18n (en/ru/zh/ko). Backend offline → offline card with start-backend hint. data_available=false → "no price history" notice. 12 jest tests in `src/__tests__/storage-value-tab.test.tsx`. |
| `src/components/dashboard/storage-value-history-chart.tsx` | Storage Value history chart (**F2 follow-up iter 75**) | Dependency-free SVG line chart (~290 lines) rendering two ratios: `currency/mirror` (blue) and `currency/hinekora` (emerald). Fetches via `useQuery` bound to `/api/flipper/storage-value/[currency]/history?days=30`. Graceful degradation: <2 points → "no history" notice; all-null ratios → "no reference data" notice; loading → spinner text. 11 jest tests in `src/__tests__/storage-value-history-chart.test.tsx`. |
| `src/components/dashboard/content-pulse-widget.tsx` | Content Pulse widget — «Что фармить сегодня» (**F4 iter 76**) | Two-column card showing top rising (emerald) + falling (red) league mechanic categories with per-category `delta_7d_pct` badge + top-3 movers (`trend_pct` per item). Fetches via `useQuery` (60s staleTime) bound to `/api/flipper/content-pulse`. Mounted FIRST in `overview-tab-content.tsx` (above MarketOverview) so it's visible on first dashboard load — wrapped in its own `<ErrorBoundary>`. Graceful degradation: backendOffline → compact amber notice (no full-card takeover); loading → spinner text; error → error card + refresh button; data_available=false → "no data yet"; all categories stable → "no signals today"; empty top_rising/top_falling → "no movers" per category. `maxPerSide` prop (default 2) caps category count per column. 16 jest tests in `src/__tests__/content-pulse-widget.test.tsx`. |
| `src/components/dashboard/speculation-tab.tsx` | Speculation tab — BUY/SELL/HOLD per item + collapsible Backtest panel (**F5 iter 77 live + iter 80 backtest UI**) | Renders a sortable list of currencies with per-item z-score (vs N-day rolling mean/std) + percentile + BUY/SELL/HOLD signal badge + horizon hint + dependency-free SVG mini-sparkline (last 14 price points). Filter chips (ALL/BUY/SELL/HOLD), days selector (7/14/30/90). Fetches via `useQuery` (30s staleTime) bound to `/api/flipper/speculation?days={days}&limit=50&signal={filter}`. Lazy-loaded via `next/dynamic`, wrapped in `<ErrorBoundary>`. Tab is in `TAB_MAP` at index 9 (between `storage-value` and `liquid-chain`) for keyboard-shortcut navigation. Tab trigger in `dashboard-toolbar.tsx` (Sparkles icon). **Below the signals list — collapsible Backtest panel (iter 80)**: toggle button (NOT autoload — `enabled: showBacktest && backendOnline` gates the `useQuery`), 3 day selectors (eval_days_ago 7/14/30/90 default 14; holding_days 1/3/7/14/30 default 7; lookback_days 7/14/30/90 default 30), 3 stats blocks (Overall/BUY/SELL with count+winRate+mean/median/best/worst return_pct), signal breakdown (BUY/SELL/HOLD counts + evaluated/unevaluated), top-trades list (capped by `limit=50`, sorted by |return_pct| desc from backend, each row shows signal badge + item name + category + entry → exit + colored return_pct). Parent's `signalFilter` forwarded as `signal` query param. Graceful degradation: collapsed (default) → only toggle button; expanded + loading → spinner; expanded + error → red notice; expanded + dataAvailable=false → "no data yet"; expanded + trades=[] → "no trades produced"; expanded + trades>0 → full content. 18 jest tests in `speculation-tab.test.tsx` + 15 jest tests in `speculation-backtest-panel.test.tsx`. |
| `src/components/dashboard/circuit-patterns-tab.tsx` | Circuit Patterns tab — trajectory classification per currency (**F7 / P8 iter 97**) | Renders a sortable list of currencies (sorted by `\|totalChangePct\|` desc from backend) with per-item trajectory badge (one of 7 archetypes: EXPONENTIAL_GROWTH / LINEAR_GROWTH / PEAK_THEN_DECLINE / MEAN_REVERTING / VOLATILE / DECLINING / STABLE) + recommended_action badge (HOLD_FOR_GROWTH / SELL_NOW / AVOID / WATCH / NEUTRAL) + signed total_change_pct + dependency-free SVG mini-sparkline (last 14 price points, colored by trajectory archetype). Filter chips (ALL + 7 archetypes), days selector (7/14/30/90). Fetches via `useQuery` (30s staleTime) bound to `/api/flipper/circuit-patterns?days={days}&limit=50&trajectory={filter}`. Lazy-loaded via `next/dynamic`, wrapped in `<ErrorBoundary fallbackTitle={t("fallbackCircuitPatterns")}>`. Tab is in `TAB_MAP` at index 9 (between `speculation` at 8 and `intraday-patterns` at 10) for keyboard-shortcut navigation — shortcut "0" → Circuits (was Liquid Chain pre-iter 97). Tab trigger in `dashboard-toolbar.tsx` (Activity icon, between Sparkles and Clock). Pattern rows show: trajectory badge (color-coded) + action badge (color-coded) + item name + category + total_change_pct + stats line (sampleSize · slope%/d · vol · R² · current · daysSincePeak for PEAK_THEN_DECLINE) + sparkline (colored by archetype — emerald for growth, amber for peak, red for declining, fuchsia for volatile, slate for stable/mean-reverting). Graceful degradation: backendOffline → offline card with start-backend hint; loading → "Classifying trajectories..."; error → red notice + refresh; dataAvailable=false → "no price history yet"; empty patterns → "no actionable patterns in the current window". 20 jest tests in `circuit-patterns-tab.test.tsx`. Mirrors the speculation-tab pattern but is simpler — no Backtest panel (trajectory classification is a single-pass computation, no replay variant needed). i18n: 47 keys × 4 locales (`circuitTitle`, `circuitSubtitle`, `circuitFilterAll`, `circuitFilterExpGrowth`, `circuitTrajExpGrowth`, `circuitActionHoldForGrowth`, `circuitDaysValue`, `circuitSampleSize`, `circuitSlope`, `circuitVolatility`, `circuitRSquared`, `circuitCurrent`, `circuitDaysSincePeak`, etc.). |
| `src/components/dashboard/intraday-patterns-tab.tsx` | Intraday Patterns tab — time-of-day heatmap per currency (**P4 iter 98**) | Renders a heatmap (rows = currencies, cols = UTC hours 0..23) using dependency-free CSS divs (no recharts). Cell color = deviation from `overallMean`: emerald ≥5% below (buy zone), light emerald 2-5% below, muted ±2% (neutral), light red 2-5% above, red ≥5% above (sell zone), very muted = no data (count=0). Buy window cell (min mean hour) highlighted with emerald ring, sell window cell (max mean hour) with amber ring. Per-row: item name (localized via `getCurrencyDisplayName`) + category (title-cased) + optional "Significant" badge (amber, when `hasSignificantPattern=true` and `intradayRangePct ≥ 10%`) + BUY badge (emerald, shows `fmtHour(buyWindowHour)`) + SELL badge (red, shows `fmtHour(sellWindowHour)`) + signed `intradayRangePct` (amber when significant, muted otherwise) + heatmap row (24 cells, each with `data-hour`/`data-count`/`data-is-buy`/`data-is-sell` test attributes) + stats line (sampleSize · overallMean · currentPrice · buyMean · sellMean). Filter: "Significant only" toggle badge (hides currencies with `hasSignificantPattern=false`). Days selector: 7/14/30/90 (default 14). Hour axis header (00..23) above the heatmap. Legend with 6 swatches (Buy zone / Mild buy / Neutral / Mild sell / Sell zone / No data). Fetches via `useQuery` (30s staleTime, retry 1) bound to `/api/flipper/intraday-patterns?days={days}&limit=50`. Lazy-loaded via `next/dynamic`, wrapped in `<ErrorBoundary fallbackTitle={t("fallbackIntradayPatterns")}>`. Tab is in `TAB_MAP` at index 10 (between `circuit-patterns` at 9 and `weekly-patterns` at 11) — click-only (no keyboard shortcut, outside the 1-9+0 range). Tab trigger in `dashboard-toolbar.tsx` (Clock icon, between Activity and Calendar). Graceful degradation: backendOffline → offline card; loading → "Aggregating hourly patterns..."; error → red notice + refresh; dataAvailable=false → "no price history yet"; empty patterns → "no patterns in the current window"; empty after significant-filter → "no significant patterns". 23 jest tests in `intraday-patterns-tab.test.tsx`. i18n: 43 keys × 4 locales (`intradayTitle`, `intradaySubtitle`, `intradayFilterSignificant`, `intradayBuyWindow`, `intradaySellWindow`, `intradayLegendBuy`, `intradayLegendSell`, `intradayLegendNoData`, `intradaySampleSize`, `intradayOverallMean`, `intradayCurrent`, `intradayBuyMean`, `intradaySellMean`, etc.). |
| `src/components/dashboard/weekly-patterns-tab.tsx` | Weekly Patterns tab — weekday/weekend heatmap per currency (**P5 iter 99**) | Renders a heatmap (rows = currencies, cols = 7 weekdays Mon..Sun) using dependency-free CSS divs (no recharts). Same `cellColor` helper logic as `intraday-patterns-tab.tsx` — emerald ≥5% below (buy zone), red ≥5% above (sell zone), muted = neutral/no data. Buy day cell (min mean weekday) highlighted with emerald ring, sell day cell (max mean weekday) with amber ring. Per-row: item name (localized via `getCurrencyDisplayName`) + category (title-cased) + optional "Significant" badge (amber, when `hasSignificantPattern=true` and `weeklyRangePct ≥ 10%`) + BUY badge (emerald, shows localized weekday name `Mon`/`Tue`/`Wed`/`Thu`/`Fri`/`Sat`/`Sun`) + SELL badge (red, shows localized weekday name) + signed `weeklyRangePct` (amber when significant, muted otherwise) + heatmap row (7 cells, each with `data-weekday`/`data-count`/`data-is-buy`/`data-is-sell` test attributes) + stats line (sampleSize · overallMean · currentPrice · buyMean · sellMean · Δ weekend). `weekdayDeltaPct` colored red when positive (weekends more expensive → sell on weekend), emerald when negative (weekdays more expensive → sell on weekday), muted when zero. Filter: "Significant only" toggle badge (hides currencies with `hasSignificantPattern=false`). Weeks selector: 1/2/4/8/12/26 (default 4). Weekday axis header (Mon..Sun, localized) above the heatmap. Legend with 6 swatches. Fetches via `useQuery` (5min staleTime, retry 1) bound to `/api/flipper/weekly-patterns?weeks={weeks}&limit=50`. Lazy-loaded via `next/dynamic`, wrapped in `<ErrorBoundary fallbackTitle={t("fallbackWeeklyPatterns")}>`. Tab is in `TAB_MAP` at index 11 (between `intraday-patterns` at 10 and `mirror-divine-arb` at 12) — click-only (no keyboard shortcut, outside the 1-9+0 range). Tab trigger in `dashboard-toolbar.tsx` (Calendar icon, between Clock and ArrowUpDown). Graceful degradation: backendOffline → offline card; loading → "Aggregating weekday patterns..."; error → red notice + refresh; dataAvailable=false → "no price history yet"; empty patterns → "no patterns in the current window"; empty after significant-filter → "no significant patterns". 25 jest tests in `weekly-patterns-tab.test.tsx`. i18n: 50 keys × 4 locales (`weeklyTitle`, `weeklySubtitle`, `weeklyFilterSignificant`, `weeklyBuyDay`, `weeklySellDay`, `weeklyWeekdayMon`..`weeklyWeekdaySun`, `weeklyWeekendDelta`, `weeklyLegendBuy`, `weeklyLegendSell`, `weeklyLegendNoData`, `weeklySampleSize`, `weeklyOverallMean`, `weeklyCurrent`, `weeklyBuyMean`, `weeklySellMean`, etc.). |
| `src/components/dashboard/mirror-divine-arb-tab.tsx` | Mirror/Divine Arbitrage tab — single-object rate detector (**P7 iter 109**) | ***DIFFERS from speculation/circuit/intraday/weekly tabs: renders a SINGLE OBJECT, not a per-currency list.*** Mirror:Divine is ONE market. Layout (one big card): (1) header row — ArrowUpDown icon + title + subtitle ("Current Mirror:Divine rate vs the last N-day window mean") + days selector (7/14/30/90, default 30) + refresh button; (2) signal+action badge row (2-column grid) — signal badge (SELL_MIRROR_BUY_DIVINE=red, SELL_DIVINE_BUY_MIRROR=emerald, NEUTRAL=muted) + action badge (EXECUTE_ARB=fuchsia, WATCH=amber, HOLD=muted); (3) hero metrics block (3-column grid) — current rate (huge, font-mono) / z-score (red ≥+1.5, emerald ≤-1.5, muted otherwise) / deviation % (red positive, emerald negative, muted zero); (4) profit+actionable row — "Profit / Mirror: X Div" + "Actionable: Yes/No" badge (fuchsia when actionable); (5) 5-cell stats grid — mean / std / min / max / sample size; (6) rate sparkline (dependency-free SVG, 360×48, colored by signal — red/emerald/slate); (7) footer — fetched-at + sample size + days window. Fetches via `useQuery` (30s staleTime, retry 1) bound to `/api/flipper/mirror-divine-arb?days={days}`. Lazy-loaded via `next/dynamic`, wrapped in `<ErrorBoundary fallbackTitle={t("fallbackMirrorDivineArb")}>`. Tab is in `TAB_MAP` at index 12 (between `weekly-patterns` at 11 and `liquid-chain` at 13) — click-only (no keyboard shortcut, outside the 1-9+0 range). Tab trigger in `dashboard-toolbar.tsx` (ArrowUpDown icon, between Calendar and Droplets). Graceful degradation: backendOffline → offline card with start-backend hint; loading → "Computing Mirror:Divine rate..."; error → red notice + refresh; dataAvailable=false → "no price history yet"; sampleSize<4 → "not enough rate points yet". Backend (iter 108): pure function `compute_mirror_divine_arb()` in `backend/economy/mirror_divine_arb.py` (70 pytest). i18n: 42 keys × 4 locales (`mirrorDivineTitle`, `mirrorDivineSubtitle`, `mirrorDivineSignalSellMirror`/`mirrorDivineSignalSellDivine`/`mirrorDivineSignalNeutral`, `mirrorDivineActionExecute`/`mirrorDivineActionWatch`/`mirrorDivineActionHold`, `mirrorDivineCurrentRate`, `mirrorDivineZScore`, `mirrorDivineDeviation`, `mirrorDivineProfit`, `mirrorDivineMean`/`mirrorDivineStd`/`mirrorDivineMin`/`mirrorDivineMax`/`mirrorDivineSampleSize`, `mirrorDivineSparklineLabel`, `tabMirrorDivineArb`, `fallbackMirrorDivineArb`, etc.). |
| `src/components/dashboard/phase-hints-widget.tsx` | Phase-aware hints widget — «League Phase Hints» (**F6 iter 78**) | Static info banner mounted BELOW ContentPulseWidget in `overview-tab-content.tsx`. Shows current phase badge (emerald/violet/amber for EARLY/MID/LATE) + day count + reference currency + phase summary + bulleted hint list (each row: bullet + title + detail + action). Fetches via `useQuery` (5min staleTime — phase only changes daily) bound to `/api/flipper/phase-hints`. Wrapped in `<ErrorBoundary fallbackTitle={t("fallbackPhaseHints")}>`. Graceful degradation: backendOffline → compact amber notice; loading → spinner; error → card + refresh; data_available=false → "no data" notice; empty hints → "no hints" notice. 26 jest tests in `src/__tests__/phase-hints-widget.test.tsx`. |
| `src/components/dashboard/leveling-uniques-widget.tsx` | Leveling Uniques Lifecycle widget — «Leveling Uniques» (**P3 iter 100**) | Static table widget mounted BELOW PhaseHintsWidget in `overview-tab-content.tsx` (between PhaseHints and MarketOverview). Header: TrendingUp icon + title + Day N (CalendarClock icon) + item count + reference currency. Summary line: depends on dominant stage — AT_PEAK → "X item(s) at peak demand — SELL NOW if you have any of these." / PRE_PEAK → "X item(s) still rising toward peak — BUY or HOLD if you have them." / POST_PEAK → "X item(s) past peak — AVOID BUYING (prices crashing)." Table: 5 columns (Item / Stage / Est. Price / Peak Day / Action) — hidden on mobile (single-column layout). Each row: name (with notes tooltip + line-clamp-2) + stage badge (PRE_PEAK=blue, AT_PEAK=amber, POST_PEAK=muted) + est price ("~X.X exa", font-mono) + peak day short ("Day N · X.X exa") + recommendation badge (BUY_OR_HOLD=emerald, SELL_NOW=red, AVOID_BUYING=muted). Disclaimer: "Estimated prices are planning heuristics based on the league day, NOT live market prices." Footer: fetched-at timestamp + stage breakdown counts ("PRE_PEAK: X · AT_PEAK: Y · POST_PEAK: Z"). Fetches via `useQuery` (5min staleTime — phase only changes daily) bound to `/api/flipper/leveling-uniques?lang={lang}` (forwards `lang=ru` for ru locale). Wrapped in `<ErrorBoundary fallbackTitle={t("fallbackLevelingUniques")}>`. Graceful degradation: backendOffline → compact amber notice; loading → spinner; error → card + refresh; data_available=false → "no data" notice; empty uniques → "no leveling uniques in the static table" notice. **Immune to KI-11** (does NOT depend on DataSnapshot — uses PhaseDetector only). 28 jest tests in `src/__tests__/leveling-uniques-widget.test.tsx`. i18n: 31 keys × 4 locales (`levelingTitle`, `levelingDayCount`, `levelingItemCount`, `levelingReferenceCurrency`, `levelingOffline`, `levelingLoading`, `levelingError`, `levelingNoData`, `levelingNoUniques`, `levelingRefresh`, `levelingFetchedAt`, `levelingStageBreakdown`, `levelingSummaryAtPeak`, `levelingSummaryPrePeak`, `levelingSummaryPostPeak`, `levelingStagePrePeak`/`levelingStageAtPeak`/`levelingStagePostPeak`/`levelingStageUnknown`, `levelingRecBuyOrHold`/`levelingRecSellNow`/`levelingRecAvoidBuying`/`levelingRecUnknown`, `levelingColItem`/`levelingColStage`/`levelingColEstPrice`/`levelingColPeakDay`/`levelingColAction`, `levelingPeakDayShort`, `levelingDisclaimer`, `fallbackLevelingUniques`). |
| `scripts/sync_currency_names_from_poe2db.py` | **F1 sync script (iter 85) + iter 145 `--audit` flag added** — CLI tool to extend `currency_names.json` with new RU translations from `poe2db.tw/ru/`, AND audit existing translations against poe2db. | Standalone Python script (stdlib only). **5-stage pipeline** (iter 145 added Stage 5): `--fetch-ids` (Stage 1 — hits poe2scout.com API, paginates ByCategory) → `--fetch-ru-by-item` (Stage 2b, KI-30 fix — per-item poe2db page fetch + `<title>` extraction) → `--diff` (Stage 3 — writes `scripts/.cache/currency_names_patch.json`) → `--apply --confirm` (Stage 4 — atomic write to JSON, idempotent, NEVER overwrites existing) → `--audit` (Stage 5, iter 145, KI-32 enabler — READ-ONLY audit of existing RU translations against poe2db current RU `<title>`, writes `scripts/.cache/translation_audit.json`). Plus `--from-cache-snapshot` fallback (no network needed). **KI-33 fix (iter 145):** all URL construction goes through `_build_poe2db_url()` which percent-encodes non-ASCII chars (needed for `Mórrigan's Insight`, `Oisín's Oath`). 57 pytest tests in `tests/test_sync_currency_names.py` (43 existing + 14 new in iter 145). **Iter 145 audit results:** 634 audited, 601 match (94.8%), 32 mismatch (5.0% — see KI-32 for the 4 drift patterns), 1 no-Cyrillic (`aldurs-saga`). **NOTE on `normalize_api_id`**: the script PRESERVES hyphens (matches `currency_names.json` key convention); this is DIFFERENT from the backend's `_normalize_api_id` in `poe2scout.py:58` which replaces hyphens with underscores for exchange-pair matching. Don't confuse the two. |

## 2. Build & Run Commands

```bash
npm install && npm run dev        # Frontend (port 3000)
npm run build && npm run test     # Build + Jest
pytest tests/ -v                  # Backend tests (skip e2e: --ignore=tests/e2e)
npx tsc --noEmit                  # TypeScript type check
npx playwright test               # E2E tests

# Backend (start.sh creates .venv automatically)
PYTHONPATH=. .venv/bin/python -m uvicorn backend.main:app --reload --port 8000

# Regenerate OpenAPI schema + TypeScript types (after API contract changes)
python -c "import json; from backend.main import app; from fastapi.openapi.utils import get_open_api; s=get_open_api(title=app.title,version=app.version,routes=app.routes); open('openapi_schema.json','w').write(json.dumps(s,indent=2,ensure_ascii=False))"
npx openapi-typescript openapi_schema.json --output src/lib/api-types.ts
```

## 3. Critical Rules (invariants)

1. **Backend responses use snake_case** — `flipper-proxy.ts` `transformKeys()` converts to camelCase.
2. **All REST paths use `/api/v1/` prefix.** (WS paths removed in iter 58.)
3. **Bridge health check uses `/api/v1/health/ping`** (NOT `/api/health/ping`).
4. **`response_model=` must match route return dict** — mismatch = 500.
5. **ProcessPoolExecutor: picklable args only** — no `sqlite3.Connection`, no `EventManager`, no `DataSnapshot`, no `AppConfig`. Use `FlipComputeBundle` pattern.
6. **CPU-bound Python → `run_in_executor()` with timeout** — never block event loop.
7. **Never hardcode league names or currency categories** — use `config.yaml`.
8. **`FLIPPER_WORKERS` env var** — controls ProcessPoolExecutor workers (default: 1).
9. **SSE proxy: return 200 + error event** — not 503 (prevents retry storms).
10. **Mock provider exchange_rates keys MUST be strings** — `"exalted/chaos"`, NOT `("exalted", "chaos")`.
11. **EventData uses `event_id`** (not `id`), `is_active` (not `active`), `created_at` — matches `StoredEvent.to_dict()`.
12. **EventType enum has 6 values**: `major_patch`, `minor_patch`, `league_start`, `economy_shift`, `streamer_hype`, `other`.
13. **Events POST proxy transforms body**: `eventType`→`event_type`, `affectedCurrencies`→`affected_currencies`, `expiryHours`→`expires_at` (ISO string).
14. **PhaseDetector: only `major_patch` resets phase clock** — `league_start` / `economy_shift` affect scoring only.
15. **Adaptive mode (`"_adaptive"`)**: `baseCurrencyApiId` can be `"_adaptive"` — `useDisplayPrice` auto-selects Div/Exa/Chaos per row.
16. **`pipeline_cache.invalidate()` clears only `pipeline` namespace** — `daily_stats` is separate.
17. **SSE payload contract** (P0-1): `{pair, change_pct, new_price, old_price, timestamp}` per changed currency — matches `SSEPriceUpdate`. `threshold_pct` query param is respected.
18. **Real-time updates = SSE + REST polling only** (iter 58+). WebSocket endpoints were removed in iter 58 — do NOT re-introduce them.
19. **Optimizer negative cycles** (P1-8): a negative cycle in `-log(rate)` space = profitable arbitrage. When `_bellman_ford` detects one and `target` lies on it, returns empty `path` with `data_available: true` — callers fall back to `direct_rate`.
20. **ProcessPoolExecutor is lazy** (P2-13): always call `get_process_pool()` at the call site — never cache the returned reference.
21. **LightGBM adaptive fallback** (P2-9, iter 67): `train()` proceeds with minimal features (`price_lags=[1]`) when `floor (5) <= len(log_prices) < min_points (15)`. Below `floor`, skips. Configurable via `ForecastingConfig.lightgbm_min_data_points_floor`.
22. **Scanner is deleted** (P2-4, iter 68 + iter 69): the standalone `/api/v1/scanner/scan` endpoint and `routes_scanner.py` were removed. All its filter/sort params live on `/api/v1/arbitrage/flips` since iter 67 — use that.
23. **`proxyWithFallback` 5xx handling is mode-aware** (P2-8, iter 69): non-503 5xx (500/502/504) passes through unchanged in dev (`NODE_ENV === "development"`) so devs see the real error; in prod it becomes 200 + fallback data + `X-Flipper-Fallback: <original-status>` header. 503 (backend_offline/insufficient_data) still returns 200 + fallback in both modes (otherwise dev is unusable when backend is down). Use `isFlipperFallbackResponse(res)` / `getFlipperFallbackOriginalStatus(res)` to detect fallback responses from the frontend.

24. **Localized currency/item names live in `backend/data/currency_names.json`** (P2-3, iter 70). `currency_names_ru.py` is a 63-line thin loader — do NOT add names there. Public API unchanged: `CATEGORY_NAMES_RU` / `CATEGORY_NAMES_EN` / `CURRENCY_NAMES_RU` / `CURRENCY_NAMES_EN` dicts + `get_ru_name` / `get_en_name` / `get_category_ru` / `get_category_en` helpers. TS-side mirror `src/lib/currency-names.ts` still exists as an offline fallback — keep both in sync. Regression tests in `tests/test_currency_names_ru.py` enforce RU/EN key parity (run them after every name edit). **Iter 145:** use `scripts/sync_currency_names_from_poe2db.py --audit` to check for drift against poe2db's current RU translations (KI-32). **Caveat (TD-6):** unique items (weapons, armour, accessories) have NO RU translation in the codebase — `mapUniqueItem` in `poe2api.ts:1030` uses `raw.Text || raw.Name` directly. Phase 2 of TD-6 will add unique-items support.

25. **EventManager is thread-safe via `threading.RLock`** (P3-3, iter 71). All in-memory `_events` dict access (CRUD + read-side query interfaces like `is_event_active`, `get_event_score_penalty`, `list_events`, `_prune_expired`) is guarded by `self._lock`. The lock is **never held across an `await`** — SQLite writes (`write_event`, `delete_event`, `deactivate_event`, `clear_all_events`) are awaited OUTSIDE the lock. Multi-worker uvicorn still needs a shared external store (Redis/DB) for cross-process coordination; the lock only protects in-process concurrency.

26. **SnapshotManager atomic state swap** (P3-4, iter 71). `(snapshot, ts)` is wrapped in an immutable `@dataclass(frozen=True) _SnapshotState` and stored as a single `self._state` reference. Replacement is a single Python attribute assignment (atomic under the GIL), so a reader either sees the pre-refresh or post-refresh state — never a mixed (stale snapshot, fresh ts) pair. `_history_cache` and `_active_currencies` are guarded by a separate `_cache_lock` (NOT the asyncio lock — these are mutated from sync code paths inside `_refresh`).

27. **Historical feature invariants (iter 73–101)** — verbose per-feature rules (Storage Value, Content Pulse, Speculation, Phase Hints, Circuit Patterns, Intraday/Weekly Patterns, Leveling Uniques, `useFlipperBackend`/`useRealmsAndLeagues`/`useFilteredExchangePairs`/`useOptimalPayment` extraction, currency-names sync script, i18n leakage fix, `formatLocaleDate`, shortcuts dialog `TAB_MAP` sync, dead i18n key cleanup, iter 91 recon, iter 93 Best Payment, iter 94 Spread Capture, iter 95 Overheat Index) were trimmed iter 125 to keep this file lean. Each feature's invariant is now captured inline in the source file's module-level comment + `git log --grep="iter N"` for the full implementation history. The single invariant that spanned all of them: **each shipped feature follows the pure-function + thin-route-handler + Next.js-proxy + TS-types + UI-tab wiring pattern** — see any `backend/economy/<feature>.py` + `backend/api/routes_<feature>.py` + `src/app/api/flipper/<feature>/route.ts` + `src/lib/types.ts` + `src/components/dashboard/<feature>-tab.tsx` quintuple for the canonical layout.

## 4. Known Issues

**Single source of truth: [`STATUS.md`](./STATUS.md).** Check there before fixing anything — it has the authoritative open/closed KI list + the "Quick Reference — Frequent Problems" table (~20 rows of symptom → cause → fix-location mappings, including closed-iter KIs that are still useful as recovery recipes).

**Historical KI-closure log:** All KIs from iter 54 onward are closed. Per-iter closure summaries live in `git log` (one-line commit messages) and in the closed-KI section of `STATUS.md`. Do not duplicate KI-closure narratives here.

### Common workflow recipes (operational patterns not covered by STATUS.md)

| Task | Recipe |
|------|--------|
| Add a new Russian currency/item translation | Use `scripts/sync_currency_names_from_poe2db.py` (5-stage pipeline: `--fetch-ids` → `--fetch-ru-by-item` → `--diff` → `--apply --confirm`; plus `--audit` for READ-ONLY drift checks). **Do NOT use `--fetch-ru`** — it's broken per KI-30 (category-page parser matches infobox stat rows). For speed, use `scripts/fetch_ru_by_item_parallel.py` (thread-pooled, ~1s when only a few items need fetching). After `--apply`, run `python scripts/sync_currency_names_ts.py` to regenerate the TS mirror, then bump count assertions in `tests/test_currency_names_ru.py`. To audit existing translations for drift vs poe2db official, run `--audit` (writes `scripts/.cache/translation_audit.json` — see KI-32 / TD-6). See invariant #24. |
| Render a localized currency name / category slug in a frontend component | Use `getCurrencyDisplayName(apiId, locale)` / `getCategoryDisplayName(slug, locale)` from `src/lib/currency-names.ts`. Always provide a fallback (`?? apiId` or `\|\| upstreamText`) — the helper returns `null` when the api_id isn't in the mapping. Pull `locale` from `useI18n().locale`. |
| Render a localized date / datetime | Use `formatLocaleDate(value, locale, opts?)` or `formatLocaleDateTime(value, locale)` from `src/lib/utils.ts`. Inline `toLocaleDateString("en-US", ...)` is FORBIDDEN — see iter 88 invariant. |
| Add `?lang=` support to a backend hint endpoint | Add `lang: str = Query("en", ...)` to the FastAPI route signature, forward to the pure function. Keep a parallel Russian table (`_XXX_RU`) with the SAME `id`/`category` slugs. Add the `lang` query param to the Next.js proxy route too. Frontend widget pulls `locale` from `useI18n()`, maps to `lang`, includes in `queryKey`, passes as query param to `fetchApi`. (iter 87 pattern.) |
| Remove dead i18n keys after a tab/dialog/feature removal | Manual pattern (iter 89 recipe — the one-shot `cleanup_dead_i18n_keys.py` was deleted iter 139 as it had hardcoded keys + a stale path). For each key in `src/lib/i18n/locales/*.ts`: `grep -rn "t('keyName')" src/` — if zero hits in `.tsx`/`.ts` files, the key is dead and can be removed from all 4 locale files (en/ru/zh/ko). Leave a `// REMOVED iter NN — <reason>` comment marker in place of the old section header so future agents see the deletion was intentional. |
| Add a new analyst fact template | Backend sends `template_id` + `params`; frontend formats via i18n keys (see `backend/api/routes_analyst.py` + `src/lib/i18n/locales/*.json` `analystFact*`). Old clients fall back to `text`. See iter 88 invariant #40(b). |
| Inspect circuit-breaker state | `GET /api/flipper/health/circuit-breakers` — JSON snapshot of per-endpoint CB state. (P2-6 iter 67.) |
| Frontend shows fallback data — detect it | Check `X-Flipper-Fallback` header via `isFlipperFallbackResponse(res)` / `getFlipperFallbackOriginalStatus(res)` from `src/lib/flipper-proxy.ts`. (P2-8 iter 69.) |

## 5. API Endpoints (all REST under `/api/v1/`)

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/v1/health` | Detailed health check |
| GET | `/api/v1/health/ping` | Ultra-lightweight ping |
| GET | `/api/v1/phase` | League phase info |
| GET | `/api/v1/currencies` | Currency metadata |
| GET | `/api/v1/prices` | All exchange rates + metrics |
| GET | `/api/v1/prices/heatmap` | 24h price change heatmap |
| GET | `/api/v1/prices/stream` | SSE live price updates |
| GET | `/api/v1/prices/{pair}` | Price for specific pair |
| GET | `/api/v1/tiers` | Currency tier classifications |
| GET | `/api/v1/benchmarks/{currency}` | Historical benchmarks |
| GET | `/api/v1/arbitrage/flips` | Scored flip opportunities (**P2-4 iter 67**: +`max_score`, `min_spread`, `max_spread`, `cluster`, `currency`, `sort_by`, `sort_dir`) |
| GET | `/api/v1/arbitrage/triangular` | Triangular arbitrage cycles |
| GET | `/api/v1/arbitrage/triangular/history` | **TD-3 Phase 3 (iter 129)** — persisted triangular cycle snapshots for backtesting. Returns `data_available: false` when table empty (refresh hasn't run yet / no profitable cycles in stable markets). |
| GET | `/api/v1/arbitrage/optimal-currency` | Optimal payment currency |
| POST | `/api/v1/batch` | Batch multiple GET requests |
| POST | `/api/v1/events` | Create event |
| GET | `/api/v1/events` | List events |
| GET | `/api/v1/events/summary` | Event summary |
| GET | `/api/v1/events/{event_id}` | Get event by ID |
| DELETE | `/api/v1/events/{event_id}` | Delete event |
| POST | `/api/v1/events/{event_id}/deactivate` | Deactivate event |
| GET | `/api/v1/anomalies` | Anomaly detection |
| GET | `/api/v1/storage-value/{currency}` | Hold/sell decision |
| GET | `/api/v1/storage-value/{currency}/history` | **F2 follow-up (iter 75)** — time-series of currency/mirror + currency/hinekora ratios |
| GET | `/api/v1/optimizer/path` | Optimal conversion path (P1-8 iter 64) |
| GET | `/api/v1/optimizer/matrix` | Conversion matrix |
| GET | `/api/v1/analyst/summary` | League analyst summary |
| GET | `/api/v1/portfolio/correlation` | Correlation matrix |
| GET | `/api/v1/liquid-chain/analysis` | Liquid chain analysis |
| GET | `/api/v1/liquid-chain/opportunities` | Liquid chain opportunities |
| GET | `/api/v1/market-spreads/history` | **TD-4 Phase 2 (iter 128)** — persisted market spread snapshots. Returns `data_available: false` when table empty (wait 5 min for snapshot refresh). |
| GET | `/api/v1/mirror-divine-arb` | **P7 (iter 109)** — Mirror/Divine arbitrage rate detector. Returns `data_available: false` when scheduler hasn't collected ≥ 4 Mirror + Divine snapshots. |
| GET | `/api/v1/leveling-uniques` | **P9 (iter 110)** — leveling-uniques lifecycle widget data. |
| GET | `/api/v1/items/{item_id}/daily-stats` | **TD-5 Phase 4 (iter 131)** — daily OHLCV per item. Returns `data_available: false` when item_id unknown / hourly refresh hasn't run / lazy-fetch provider call failed. |
| GET | `/api/v1/content-pulse` | **F3 (iter 75)** — per-category turnover + 7d/30d rolling + top movers |
| GET | `/api/v1/speculation` | **F5 (iter 77)** — per-item z-score + BUY/SELL/HOLD signals. Query: `days` (1-90, default 30), `limit` (1-500, default 50), `signal` (ALL/BUY/SELL/HOLD, default ALL). |
| GET | `/api/v1/speculation/backtest` | **F5 follow-up (iter 79)** — backtest z-score signals on historical price_logs. Query: `eval_days_ago` (1-365, default 14), `holding_days` (1-90, default 7), `lookback_days` (1-90, default 30), `limit` (1-500, default 50), `signal` (ALL/BUY/SELL/HOLD, default ALL). Returns per-trade results + per-signal aggregates (win_rate, mean/median/best/worst return_pct). Backend-only — no frontend UI yet. |
| GET | `/api/v1/phase-hints` | **F6 (iter 78)** — phase-aware advisory hints (Temporalis, skill gems, etc.). No query params. Always returns `data_available=True` (hint table is hardcoded). |
| GET | `/api/v1/circuit-patterns` | **F7 / P8 (iter 97)** — per-currency trajectory classification + recommended action. Query: `days` (1-90, default 30), `limit` (1-500, default 50), `trajectory` (ALL/EXPONENTIAL_GROWTH/LINEAR_GROWTH/PEAK_THEN_DECLINE/MEAN_REVERTING/VOLATILE/DECLINING/STABLE, default ALL). Each pattern includes `price_history_short` (up to 14 most-recent points) for the UI mini-sparkline. |
| GET | `/api/v1/intraday-patterns` | **P4 (iter 98)** — per-currency time-of-day (UTC hour) price pattern + buy/sell windows. Query: `days` (1-90, default 14), `limit` (1-500, default 50). Each pattern includes `hourly_stats` (always 24 entries, hour 0..23, with mean/std/count — empty hours have mean=null, count=0) + `buy_window_hour` (min mean) + `sell_window_hour` (max mean) + `intraday_range_pct` (|sell-buy|/overall×100) + `has_significant_pattern` (range ≥ 10%). |
| GET | `/api/v1/weekly-patterns` | **P5 (iter 99)** — per-currency weekday/weekend price pattern + buy/sell days. Query: `weeks` (1-26, default 4 — each week = 7 days, so weeks=4 → 28-day window), `limit` (1-500, default 50). Each pattern includes `daily_stats` (always 7 entries, ISO weekday 1=Mon..7=Sun, with mean/std/count — empty days have mean=null, count=0) + `buy_window_day` (min mean) + `sell_window_day` (max mean) + `weekly_range_pct` (|sell-buy|/overall×100) + `weekday_delta_pct` (signed (weekend_mean - weekday_mean)/overall×100 — positive = weekends more expensive) + `has_significant_pattern` (range ≥ 10%). |

**Frontend-only routes:**

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/flipper/health` | Proxies backend `/health` |
| GET | `/api/flipper/health/circuit-breakers` | **P2-6 iter 67** — JSON snapshot of per-endpoint circuit breaker state |
| GET | `/api/flipper/content-pulse` | **F4 (iter 76)** — Next.js proxy to `/api/v1/content-pulse`. Returns empty `categories: []` + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/speculation` | **F5 (iter 77)** — Next.js proxy to `/api/v1/speculation`. Forwards `days`/`limit`/`signal` query params. Returns empty `signals: []` + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/speculation/backtest` | **F5 follow-up (iter 80)** — Next.js proxy to `/api/v1/speculation/backtest`. Forwards `eval_days_ago`/`holding_days`/`lookback_days`/`limit`/`signal` query params. Returns empty `trades: []` + zeroed stats blocks + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/phase-hints` | **F6 (iter 78)** — Next.js proxy to `/api/v1/phase-hints`. No query params. Returns empty `hints: []` + `dataAvailable: false` when backend is offline. |
| GET | `/api/flipper/{resource}` | Proxies to FastAPI `{resource}` (currencies, prices, flips, etc.) |

(WebSocket endpoints removed in iter 58.)

## 6. Documentation Map

| File | Purpose |
|------|---------|
| `STATUS.md` | **Known issues & refactoring backlog** — read first |
| `PRODUCT_VISION.md` | **Product direction** — analytics helper, NOT a poe2scout/poe2ninja clone. Read before proposing features. |
| `worklog.md` | Shared multi-agent work log (append-only). Each iter appends a section starting with `---`. Read the last 1-2 entries before starting a new iter. |
| `docs/ARCHITECTURE.md` | Layers, data flow, invariants, principles |
| `docs/DATA_CONTRACTS.md` | TypeScript types, API contracts |
| `docs/DATA_FLOW.md` | Data flow traces, field transforms |
| `docs/BACKEND_GUIDE.md` | FastAPI backend internals |
| `docs/CORS_PROXY_GUIDE.md` | CORS proxy setup |
| `docs/design/` | **Architectural design-docs (iter 126+)** — produced before any high-risk implementation. Read these BEFORE implementing the corresponding TD/P item. Current docs: `TD-3-4-5-9-persistence-gaps-design.md` (persistence-layer unification for TD-3/4/5/9 — **ALL 4 PHASES SHIPPED** iter 127/128/129/131; kept as historical reference), `P10-gold-map-roi-design.md` (Gold Map ROI UX + formula — **Phase 1 MVP iter 127 + Phase 2 trend chart iter 132 SHIPPED**; Phase 3 SQLite promotion optional). |
| `PoE2_Flipper_Canonical_Formulas.md` | Mathematical formulas |

> **Note:** `REFACTOR_PLAN.md` was deleted in iter 73 (P3-7) after all P2/P3 issues were closed. `worklog.md` was deleted in iter 73 then re-created in iter 74 as the shared multi-agent work log (append-only, sections separated by `---`). For old task history before iter 73 see `git log`.


