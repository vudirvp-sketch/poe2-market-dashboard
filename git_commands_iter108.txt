# Git commands for iter 108 delivery
# Run from your local poe2-market-dashboard repo root.

# 1. Stage all new and modified files
git add backend/economy/mirror_divine_arb.py
git add backend/api/routes_mirror_divine_arb.py
git add backend/api/response_models.py
git add backend/main.py
git add src/app/api/flipper/mirror-divine-arb/route.ts
git add src/lib/types.ts
git add tests/test_mirror_divine_arb.py
git add STATUS.md
git add docs/MARKET_PLAYBOOK.md
git add AGENT_NAVIGATION.md
git add worklog.md
git add MERGE_INSTRUCTIONS_iter108.md
git add git_commands_iter108.txt

# 2. Verify what's staged
git status

# 3. Commit
git commit -m "iter 108: P7 Mirror/Divine Arb Detector (backend + proxy + TS types)

- New: backend/economy/mirror_divine_arb.py — pure function
  compute_mirror_divine_arb() analyzes the mirror_price/divine_price rate
  series over a lookback window. Returns single-object response with
  current_rate / mean_rate / std_rate / z_score / signal
  (SELL_MIRROR_BUY_DIVINE / SELL_DIVINE_BUY_MIRROR / NEUTRAL) /
  recommended_action (EXECUTE_ARB / WATCH / HOLD) + 14-point sparkline.
  Profit threshold = 100 Div per Mirror (per P7 playbook).
- New: backend/api/routes_mirror_divine_arb.py — thin FastAPI wrapper,
  GET /api/v1/mirror-divine-arb?days=N.
- New: src/app/api/flipper/mirror-divine-arb/route.ts — Next.js proxy.
- New: tests/test_mirror_divine_arb.py — 70 pytest (12 test classes:
  ExtractRateSeries, FilterToWindow, MeanStdZscore, SignalFromZscore,
  RecommendedAction, Empty, Steady, Spike, Watch, PriceHistoryShort,
  Defensive, RouteHandler). All green.
- Modified: backend/api/response_models.py — added MirrorDivineArbRatePoint
  + MirrorDivineArbResponse pydantic models.
- Modified: backend/main.py — registered mirror_divine_arb_router after
  leveling_uniques_router (additive, try/except ImportError).
- Modified: src/lib/types.ts — added MirrorDivineArbSignal /
  MirrorDivineArbAction / MirrorDivineArbRatePoint / MirrorDivineArbResponse.
- KI-13 production-verified: backend log shows 'SSE /stream request received'
  info message — route-registration-order fix from iter 107 is working.
- Docs: STATUS.md, docs/MARKET_PLAYBOOK.md (§C.6 DONE, §B P7, §D.2/D.3),
  AGENT_NAVIGATION.md (header + 2 new module rows), worklog.md (iter 108
  entry, trimmed iter 105).

Verification: pytest 1218 green (70 new + 1148 regression; 13 test_scheduler
skipped due to missing aiosqlite in iteration env). Router registration
verified. tsc/jest not run (OOM-killer at npm install — Known Issue since
iter 99, requires 8GB+ RAM).

Stopping point: P7 backend done. UI tab — iter 109."

# 4. Push to remote
git push origin main

# 5. Verify the push
git log --oneline -1
git status
