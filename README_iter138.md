# iter 138 — F1 periodic re-run (no-op) + repo cleanup

## Summary

Periodic re-run of the F1 translation pipeline that was SHIPPED in iter 137.
Result: **no data changes** — poe2scout item count unchanged (643), poe2db has
not added Russian translations for the 9 previously-untranslated items.
1289 pytest green (0 regressions).

## Archive contents

Modified files (extract at repo root — paths are relative to repo root):

```
AGENT_NAVIGATION.md                  # header bump iter 137 -> iter 138
STATUS.md                            # cleanup + iter 138 update (verbose iter-137 history removed)
worklog.md                           # iter 138 entry + trim iter 135/136 history
scripts/.cache/currency_names_patch.json   # regenerated by --diff (empty patch, no apply needed)
scripts/.cache/poe2db_ru_names.json        # regenerated by parallel fetcher (0 new matches)
scripts/.cache/poe2scout_items.json        # regenerated by --fetch-ids (same 643 items, reordered)
```

Files to DELETE (run `git rm` on these — they are legacy clutter that previous
iters should have cleaned up):

```
ITER135_README.md     # iter 135 archive readme — superseded by worklog.md
ITER136_README.md     # iter 136 archive readme — superseded by worklog.md
DELETIONS.txt         # iter 136 self-referential deletion list — already consumed
```

## What was verified

1. poe2scout.com API: 643 items across 17 categories (UNCHANGED from iter 137).
2. poe2db.tw: 0 new Russian translations for the 9 items still missing
   (`aldurs-legacy`, `betrayal-of-aldur`, `vision-rune`, `rebirth-rune`,
   `ward-rune`, `stone-rune`, `breath-of-aldur`, `ire-of-aldur`,
   `passion-of-aldur`).
3. `--diff` reports 634 already translated, 0 new candidates, 0 conflicts,
   9 no-match.
4. `--apply` SKIPPED (empty patch).
5. TS mirror `src/lib/currency-names.ts` regenerated — byte-identical to iter 137.
6. `pytest tests/test_currency_names_ru.py tests/test_sync_currency_names.py`
   — 57/57 pass.
7. Full `pytest` (excluding the 6 aiosqlite-dependent modules — pre-existing
   env issue) — 1289/1289 pass.

## What was NOT done (deferred)

- **9 items still untranslated** — poe2db has the pages but no Russian
  translation yet. Will be picked up automatically the next time poe2db adds
  translations and this pipeline is re-run.
- **TD-3 runtime log verification** — still deferred since iter 136 (requires
  prod access to check `_refresh` debug log).
- **Jest tests** not run in this env (no `npm install`) — but the TS file is
  byte-identical to iter 137, so jest results would also be identical.

## Stop point

iter 138 SHIPPED (no-op data-wise; cleanup-only). Next iter candidates:
- (a) re-run this pipeline after a patch / monthly cadence;
- (b) TD-3 runtime log verification (requires prod access);
- (c) any new bugs the user identifies.
