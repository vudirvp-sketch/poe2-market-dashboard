# Git commands for iter 99 — Weekly Patterns API + UI wire-up (P5)
#
# Run from the repo root (where this file lives) after merging the archive.
#
# Prerequisite: the archive contents have been merged into your local
# working directory (see MERGE_INSTRUCTIONS_iter99.md for step-by-step
# merge instructions).

# 1. Stage all iter 99 changes (new files + modified files)
git add \
  backend/economy/weekly_patterns.py \
  backend/api/routes_weekly_patterns.py \
  backend/api/response_models.py \
  backend/main.py \
  tests/test_weekly_patterns.py \
  src/app/api/flipper/weekly-patterns/route.ts \
  src/lib/types.ts \
  src/components/dashboard/weekly-patterns-tab.tsx \
  src/components/dashboard/dashboard-page.tsx \
  src/components/dashboard/dashboard-toolbar.tsx \
  src/components/dashboard/shortcuts-dialog.tsx \
  src/lib/i18n/locales/en.ts \
  src/lib/i18n/locales/ru.ts \
  src/lib/i18n/locales/zh.ts \
  src/lib/i18n/locales/ko.ts \
  src/__tests__/weekly-patterns-tab.test.tsx \
  STATUS.md \
  AGENT_NAVIGATION.md \
  docs/MARKET_PLAYBOOK.md \
  worklog.md \
  git_commands_iter99.txt \
  MERGE_INSTRUCTIONS_iter99.md

# 2. Verify what's staged before committing
git status

# 3. Commit with a descriptive message
git commit -m "iter 99: Weekly Patterns API + UI wire-up (P5, weekday/weekend heatmap)

Backend:
- New pure function compute_weekly_patterns() in backend/economy/weekly_patterns.py
  (99 pytest tests). For each currency: aggregates price_logs by ISO weekday
  (1=Mon..7=Sun) over the lookback window (weeks × 7 days) → per-weekday
  mean/std/count. buy_window_day = min mean (mid-week supply). sell_window_day
  = max mean (weekend demand spike). has_significant_pattern = weekly_range_pct
  >= 10%. weekday_delta_pct = signed (weekend_mean - weekday_mean)/overall*100
  (positive = weekends more expensive, negative = weekdays more expensive).
  Filters: MIN_SAMPLE_SIZE=4 AND MIN_DAYS_COVERED=2. Always emits 7
  daily_stats entries per currency for the UI heatmap.
- New route handler GET /api/v1/weekly-patterns in backend/api/routes_weekly_patterns.py
  (thin wrapper, query params weeks 1..26 default 4 — each week = 7 days,
  limit 1..500 default 50).
- New Pydantic models WeeklyDailyStat + WeeklyPatternData + WeeklyPatternsResponse
  in backend/api/response_models.py.
- Router registered in backend/main.py via try/except ImportError wrapper.

Frontend:
- New Next.js proxy src/app/api/flipper/weekly-patterns/route.ts.
- New TS types WeeklyDailyStat + WeeklyPattern + WeeklyPatternsResponse
  in src/lib/types.ts.
- New UI tab src/components/dashboard/weekly-patterns-tab.tsx — heatmap
  (rows = currencies, cols = 7 weekdays Mon..Sun) using dependency-free CSS
  divs (no recharts). Same cellColor helper logic as intraday-patterns-tab.tsx
  (emerald ≥5% below = buy zone, red ≥5% above = sell zone, muted = neutral).
  Buy day cell highlighted with emerald ring, sell day cell with amber ring.
  weekday_delta_pct colored red when positive (sell on weekend), emerald when
  negative (sell on weekday). Filter \"Significant only\" toggle. Weeks selector
  1/2/4/8/12/26. Weekday axis header (Mon..Sun, localized). Legend (6 swatches).
  25 jest tests.
- Tab wiring: dashboard-page.tsx (TAB_MAP idx 11, dynamic import, TabsContent),
  dashboard-toolbar.tsx (Calendar icon, TabsTrigger), shortcuts-dialog.tsx
  (comment update — Weekly Patterns is click-only).

i18n: 50 new keys × 4 locales (en/ru/zh/ko) — full parity. Includes 7
  weekday name keys (weeklyWeekdayMon..weeklyWeekdaySun) localized per locale.

Tests: 99 new pytest (pure function + route smoke) — all green. No regressions.
  Python: 99 weekly + 89 intraday + 79 circuit = 267 pattern tests green +
  292 smoke tests green = 559 total. Babel syntax-check on 11 modified/new
  TS/TSX files → all OK. tsc/jest regression NOT run due to OOM-killer
  during npm install (4GB RAM, no swap) — Known Issue, requires 8GB+ RAM.
  User should run npm install && npx jest && npx tsc --noEmit locally.

Behavioral change: tab count grew to 14. Intraday Patterns (idx 10) +
Weekly Patterns (idx 11) + Liquid Chain (idx 12) + Watchlist (idx 13)
are click-only (only 10 shortcut slots 1-9+0). Shortcuts 1-9+0 still map
to first 10 tabs (overview through circuit-patterns).

Docs: STATUS.md (P5 row + 3 Quick Reference rows), docs/MARKET_PLAYBOOK.md
(P5 row Done + §C.4 DONE + §D.3 stopping point), AGENT_NAVIGATION.md
(new entries + invariant #48), worklog.md.

Closes P5. See docs/MARKET_PLAYBOOK.md §C.4 for the full spec."

# 4. Push to the remote
git push origin main

# 5. (Optional) Verify the push succeeded
git log --oneline -3
