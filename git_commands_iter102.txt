# Git commands for iter 102 — KI-11 + KI-12 fix
# Run from the repo root after extracting iter102-changes.zip
#
# This script:
#   1. Stages 5 modified files + 3 new files
#   2. Removes 2 obsolete iter 100 archive files (per "keep docs light" rule)
#   3. Commits + pushes
#
# Expected `git status --short` after staging (10 changes total):
#   M  STATUS.md
#   M  AGENT_NAVIGATION.md
#   M  instrumentation.ts
#   M  src/lib/poe2api.ts
#   M  worklog.md
#   A  MERGE_INSTRUCTIONS_iter102.md
#   A  git_commands_iter102.txt
#   A  src/__tests__/poe2api-ki11-graceful-4xx.test.ts
#   D  MERGE_INSTRUCTIONS_iter100.md
#   D  git_commands_iter100.txt

# ---- 1. Stage modified + new files ----
git add STATUS.md
git add AGENT_NAVIGATION.md
git add instrumentation.ts
git add src/lib/poe2api.ts
git add worklog.md
git add src/__tests__/poe2api-ki11-graceful-4xx.test.ts
git add MERGE_INSTRUCTIONS_iter102.md
git add git_commands_iter102.txt

# ---- 2. Remove obsolete iter 100 archive files ----
git rm MERGE_INSTRUCTIONS_iter100.md
git rm git_commands_iter100.txt

# ---- 3. Verify staging ----
git status --short

# ---- 4. Commit ----
git commit -m "iter 102: fix KI-11 (502 on /api/poe2/uniques & currencies) + KI-12 (Turbopack NFT warning)

KI-11 (closed): Browser showed 502 Bad Gateway on /api/poe2/uniques?league=runes
and /api/poe2/currencies?league=runes when the upstream POE2Scout API returned
404 for the configured league slug. Root cause (corrected in iter 102): the 502
came from the Next.js route handlers hitting upstream directly via cachedFetch
in src/lib/poe2api.ts — NOT through the Python backend (the Python _do_request
already returns None on 4xx, and all callers already convert None to empty
list/dict). When cachedFetch threw Error('API 404: ...'), getUniquesByCategory /
getUniquesAllCategories / getCurrenciesByCategory / getCurrenciesAllCategories
propagated the throw, and the route handler's catch block returned 502 with
items: [].

Fix: added isUpstream4xxError + emptyPaginatedResponse exports to
src/lib/poe2api.ts. Wrapped the league-scoped cachedFetch calls in those 4
functions in try/catch. On isUpstream4xxError(err) return emptyPaginatedResponse
so the route returns 200 with items: [] (frontend's empty-state UI renders
normally). Non-4xx errors (5xx, network) still propagate so the route handler
returns 502 for genuine upstream failures.

KI-12 (closed): next build emitted 'Encountered unexpected file in NFT list'
warning. The original KI-12 description claimed the fix belonged in
next.config.ts, but the actual import of scripts/flipper-backend-bridge.ts is
in instrumentation.ts (line 25). Added /* turbopackIgnore: true */ magic
comment inside the await import() call. Cosmetic only — build still succeeded
before the fix.

Tests: 37 new jest tests in src/__tests__/poe2api-ki11-graceful-4xx.test.ts
covering isUpstream4xxError predicate (13 tests), emptyPaginatedResponse
shape (5 tests), getUniquesByCategory single-category (5 tests),
getCurrenciesByCategory single-category (4 tests), getUniquesByCategory
(category='all') (4 tests), getCurrenciesByCategory (category='all') (4
tests), sanity checks (2 tests).

Verified: tsc --noEmit clean, npx jest 25 suites / 569 tests green (532
baseline + 37 new, no regressions). No backend changes — Python _do_request
already returns None on 4xx.

Repo cleanup: removed MERGE_INSTRUCTIONS_iter100.md + git_commands_iter100.txt
(per 'keep docs light' rule — only the latest two iter archives kept: iter
101 + iter 102)."

# ---- 5. Push ----
git push origin main

# ---- 6. Verify push ----
git log --oneline -3
